var documenterSearchIndex = {"docs":
[{"category":"section","location":"examples/asset_pricing/haddad/#Haddad:-concentrated-ownership-and-endogenous-volatility","page":"Haddad: endogenous volatility","text":"This asset-pricing model has two state variables — expected consumption growth mu and consumption variance v — and prices the aggregate consumption claim under Epstein–Zin preferences, much like the long-run-risk model. Its distinctive feature is concentrated ownership: only a limited set of active investors bears aggregate risk, and the equilibrium active-capital share alpha^star is determined endogenously,\n\nalpha^star = baralpha - sqrtdfraclambdabaralphatfrac12gammabigl(sigma_c^2 + sigma_pmu^2 + sigma_pv^2bigr)\n\nwhere the return-volatility terms sigma_pmusigma_pv depend on the derivatives of the unknown price function p(mu v). Because alpha^star feeds the market price of risk kappa=alpha^stargammasigma while return volatility feeds back into alpha^star, risk premia and volatility are endogenous and jointly determined. The two states themselves follow mean-reverting square-root processes.","title":"Haddad: concentrated ownership and endogenous volatility"},{"category":"section","location":"examples/asset_pricing/haddad/#The-model","page":"Haddad: endogenous volatility","text":"The parameters live in a struct:\n\nusing EconPDEs, Distributions, Plots\n\nBase.@kwdef struct HaddadModel\n  # consumption process parameters\n  μbar::Float64 = 0.018     # long-run mean of expected consumption growth μ\n  vbar::Float64 = 0.00052   # long-run mean of consumption variance v\n  κμ::Float64 = 0.3         # mean-reversion speed of μ\n  νμ::Float64 = 0.456       # volatility loading of the μ process\n  κv::Float64 = 0.012       # mean-reversion speed of v\n  νv::Float64 = 0.00472     # volatility loading of the v process\n\n  # active capital\n  αbar::Float64 = 1.2       # maximum active-capital share\n  λ::Float64 = 0.018        # cost of active participation (ownership friction)\n\n  # utility parameters\n  ρ::Float64 = 0.0132       # rate of time preference (discount rate)\n  γ::Float64 = 10.0         # relative risk aversion\n  ψ::Float64 = 1.5          # elasticity of intertemporal substitution\nend","title":"The model"},{"category":"section","location":"examples/asset_pricing/haddad/#The-state-space","page":"Haddad: endogenous volatility","text":"We build the grid and the initial guess first, because they fix the names used everywhere else. This model has two state variables, so the grid is a NamedTuple with two keys (μ and v); the guess is a NamedTuple whose key is the unknown function (p, the price–consumption ratio), a matrix over the (mu v) grid. These names reappear inside the equation below — e.g. pμ_up is the forward finite difference of p in μ, and pμv is the cross-partial. The grid spans the ergodic ranges of mu (Normal) and v (Gamma). The sqrt v diffusion vanishes at v = 0, a degenerate boundary where no condition is imposed.\n\nm = HaddadModel()\nσ = sqrt(m.νμ^2 * m.vbar / (2 * m.κμ))\nμmin = quantile(Normal(m.μbar, σ), 0.001)\nμmax = quantile(Normal(m.μbar, σ), 0.999)\nμs = range(μmin,  μmax, length = 30)\nα = 2 * m.κv * m.vbar / m.νv^2\nβ = m.νv^2 / (2 * m.κv)\nvmin = quantile(Gamma(α, β), 0.001)\nvmax = quantile(Gamma(α, β), 0.999)\nvs = range(vmin,  vmax, length = 30)\nstategrid = (; μ = μs, v = vs)\nyend =   (; p = ones(length(stategrid[:μ]), length(stategrid[:v])))","title":"The state space"},{"category":"section","location":"examples/asset_pricing/haddad/#The-equation","page":"Haddad: endogenous volatility","text":"We now write the function encoding the HJB equation. Following the package convention, it takes the current state (a grid point) and u (each unknown together with its finite-difference derivatives there) and returns the time derivative of each unknown.\n\nfunction (m::HaddadModel)(state::NamedTuple, u::NamedTuple)\n  (; μbar, vbar, κμ, νμ, κv, νv, αbar, λ, ρ, γ, ψ) = m\n  (; μ, v) = state\n  (; p, pμ_up, pμ_down, pv_up, pv_down, pμμ, pμv, pvv) = u\n\n  # drifts and volatilities of consumption, μ, and v\n  μc = μ\n  σc = sqrt(v)\n  μμ = κμ * (μbar - μ)\n  σμ = νμ * sqrt(v)\n  μv = κv * (vbar - v)\n\n  pμ = (μμ >= 0) ? pμ_up : pμ_down\n  pv = (μv >= 0) ? pv_up : pv_down\n  σv = νv * sqrt(v)\n  σp_Zμ = pμ / p * σμ\n  σp_Zv = pv / p * σv\n  σp2 = σp_Zμ^2 + σp_Zv^2\n  μp = pμ / p * μμ + pv / p * μv + 0.5 * pμμ / p * σμ^2 + 0.5 * pvv / p * σv^2\n\n  # market price of risk, with endogenous active-capital share α*\n  αstar = αbar - sqrt(λ * αbar / (0.5 * γ * (σc^2 + σp_Zμ^2 + σp_Zv^2)))\n  αstar = clamp(αstar, 0.0, 1.0)\n  κ_Zc = αstar * γ * σc\n  κ_Zμ = (αstar - (1 / γ - 1) / (ψ - 1)) * γ * σp_Zμ\n  κ_Zv = (αstar - (1 / γ - 1) / (ψ - 1)) * γ * σp_Zv\n  κ2 = κ_Zc^2 + κ_Zμ^2 + κ_Zv^2\n\n  # pricing equation for the price–consumption ratio (interest rate substituted out)\n  pt = - p * (1 / p - ρ + (1 - 1 / ψ) * (μc - 0.5 * γ * σc^2 * (1 - (αstar - 1)^2)) + μp + (0.5 * (1 / ψ - γ) / (1 - 1 / ψ) + 0.5 * γ * (1 - 1 / ψ) * (αstar - 1)^2) * σp2)\n  return (; pt)\nend\n\nWith the equation, grid, and guess in hand, pdesolve solves the stationary system:\n\nresult = pdesolve(m, stategrid, yend)","title":"The equation"},{"category":"section","location":"examples/asset_pricing/haddad/#The-solution","page":"Haddad: endogenous volatility","text":"Holding one state fixed at its median, the price–consumption ratio rises with expected growth mu and falls with consumption variance v. The growth effect reflects the elasticity of intertemporal substitution (psi1): investors pay more for the claim when future growth is high. The variance effect is the risk-premium channel — and here it is amplified, because only a concentrated set of active owners bears aggregate risk, raising the effective price of risk alpha^stargammasigma and making valuations more sensitive to volatility than under full risk sharing.\n\np = result.zero[:p]\niμ = div(length(stategrid[:μ]), 2)\niv = div(length(stategrid[:v]), 2)\np1 = plot(stategrid[:μ], p[:, iv]; xlabel = \"expected growth μ\", ylabel = \"price–consumption ratio p\", legend = false)\np2 = plot(stategrid[:v], p[iμ, :]; xlabel = \"consumption variance v\", ylabel = \"price–consumption ratio p\", legend = false)\nplot(p1, p2; layout = (1, 2), size = (800, 300))\n\n\n\nThis page was generated using Literate.jl.","title":"The solution"},{"category":"section","location":"examples/asset_pricing/tuckman_vila/#Tuckman–Vila-(1992):-a-finite-horizon-problem","page":"Tuckman–Vila: finite horizon","text":"So far every model has been stationary. Tuckman–Vila is time-dependent: an arbitrageur faces holding costs over a finite horizon 0 T, so the value function F(z tau) depends on calendar time. pdesolve handles this by taking a time grid τs as a fourth argument and solving backward from a terminal condition. The PDE function then takes a third argument τ:\n\npartial_tau F + max_iBigl (mu + sigma^2 F_z) a i - tfrac12 sigma^2 (a i)^2 Bigr\n  - rho z F_z + tfrac12 sigma^2 (F_zz - F_z^2) = 0","title":"Tuckman–Vila (1992): a finite-horizon problem"},{"category":"section","location":"examples/asset_pricing/tuckman_vila/#The-model","page":"Tuckman–Vila: finite horizon","text":"The parameters live in a struct:\n\nusing EconPDEs, Distributions, Plots\n\nBase.@kwdef struct TuckmanVilaModel\n    c::Float64 = 0.06     # coupon rate of the bond\n    r::Float64 = 0.09     # risk-free rate\n    ρ::Float64 = 5.42     # mean-reversion speed of the state z\n    σ::Float64 = 26.72    # volatility of the state z\n    a::Float64 = 26.72    # position (holding-cost) scaling parameter\n    T::Float64 = 100      # horizon (terminal date)\nend","title":"The model"},{"category":"section","location":"examples/asset_pricing/tuckman_vila/#The-state-space","page":"Tuckman–Vila: finite horizon","text":"We build the grid, the initial guess, and the time grid first, because they fix the names used everywhere else. The grid is a NamedTuple whose key is the state variable (z); the guess is a NamedTuple whose key is the unknown function (F), which here doubles as the terminal condition at τs[end]. These names reappear inside the equation below — e.g. Fz_up is the forward finite difference of F in z. The grid spans the stationary range of z (a mean-reverting Gaussian). Because the problem is finite-horizon, we also build a time grid τs over 0 T, which pdesolve will march backward along.\n\nfunction initialize_stategrid(m::TuckmanVilaModel; zn = 200)\n    d = Normal(0, sqrt(m.σ^2 / (2 * m.ρ)))\n    (; z = range(quantile(d, 0.00001), quantile(d, 0.99999), length = zn))\nend\n\nfunction initialize_y(m::TuckmanVilaModel, stategrid)\n    zn = length(stategrid[:z])\n    (; F = zeros(zn))\nend\n\nm = TuckmanVilaModel()\nstategrid = initialize_stategrid(m)\nyend = initialize_y(m, stategrid)\nτs = range(0, m.T, length = 10)","title":"The state space"},{"category":"section","location":"examples/asset_pricing/tuckman_vila/#The-equation","page":"Tuckman–Vila: finite horizon","text":"We now write the function encoding the HJB equation. Following the package convention, it takes the current state (a grid point) and u (each unknown together with its finite-difference derivatives there) and returns the time derivative of each unknown. Because the problem is time-dependent, it also takes a third argument, the time τ.\n\nfunction (m::TuckmanVilaModel)(state::NamedTuple, u::NamedTuple, τ::Number)\n    (; c, r, ρ, σ, a, T) = m\n    (; z) = state\n    (; F, Fz_up, Fz_down, Fzz) = u\n    Fz = (z >= 0) ? Fz_up : Fz_down\n    ϕ = z * (1 + z^2)^(-1 / 2)\n    ϕz = (1 + z^2)^(-3 / 2)\n    ϕzz = -3 * z * (1 + z^2)^(-5 / 2)\n    sτ = c / r * (1 - exp(-r * (T - τ)))\n    μL = ρ * z - 0.5 * σ^2 * ϕzz / ϕz + c / sτ * (ϕ - 1) / ϕz\n    iL = 1 / a * (μL / σ^2 + Fz)\n    μS = ρ * z - 0.5 * σ^2 * ϕzz / ϕz + c / sτ * (ϕ + 1) / ϕz\n    iS = 1 / a * (μS / σ^2 + Fz)\n    μ, i = 0.0, 0.0\n    if iL > 0\n        i, μ = iL, μL\n    elseif iS < 0\n        i, μ = iS, μS\n    end\n    Ft = -((μ + σ^2 * Fz) * a * i - 0.5 * σ^2 * (a * i)^2 - ρ * z * Fz + 0.5 * σ^2 * (Fzz - Fz^2))\n    return (; Ft)\nend\n\npdesolve takes the time grid τs as a fourth argument and marches backward from the terminal condition:\n\nresult = pdesolve(m, stategrid, yend, τs)","title":"The equation"},{"category":"section","location":"examples/asset_pricing/tuckman_vila/#The-solution","page":"Tuckman–Vila: finite horizon","text":"For a time-dependent problem result.zero[i] is the solution at time τs[i] (so result.zero[end] is the terminal condition). Plotting a few time slices shows the value building up as the remaining horizon lengthens — from the flat terminal F = 0 toward the long-horizon profile.\n\nzs = stategrid[:z]\nplt = plot(; xlabel = \"state z\", ylabel = \"value F(z)\", legend = :topright)\nfor i in (1, 4, 7, 10)\n    plot!(plt, zs, result.zero[i][:F]; label = \"τ = $(round(τs[i], digits = 0))\")\nend\nplt\n\n\n\nThis page was generated using Literate.jl.","title":"The solution"},{"category":"section","location":"examples/asset_pricing/bansal_yaron/#Bansal–Yaron-(2004):-long-run-risk-(two-states)","page":"Bansal–Yaron: long-run risk (2D)","text":"The long-run-risk model has two state variables, so it exercises the package's 2D machinery — cross-derivatives and a sparse Jacobian on a genuine grid. Consumption growth has a small, persistent expected-growth component mu and a stochastic variance v:\n\ndmu = kappa_mu(barmu - mu)dt + nu_musqrtvdW_mu qquad\ndv = kappa_v(bar v - v)dt + nu_vsqrtvdW_v\n\nWith Epstein–Zin preferences the unknown is the wealth–consumption ratio p(mu v), which solves a nonlinear elliptic PDE. The gradient-nonlinear risk terms sigma_mu^2 p_mu^2p and sigma_v^2 p_v^2p come from the market prices of long-run-risk and variance risk.","title":"Bansal–Yaron (2004): long-run risk (two states)"},{"category":"section","location":"examples/asset_pricing/bansal_yaron/#The-model","page":"Bansal–Yaron: long-run risk (2D)","text":"The parameters live in a struct:\n\nusing EconPDEs, Distributions, Plots\n\nBase.@kwdef struct BansalYaronModel\n    μbar::Float64 = 0.018    # long-run mean of expected consumption growth\n    vbar::Float64 = 0.00073  # long-run mean of consumption variance\n    κμ::Float64 = 0.252      # mean-reversion speed of expected growth\n    νμ::Float64 = 0.528      # volatility of expected growth\n    κv::Float64 = 0.156      # mean-reversion speed of variance\n    νv::Float64 = 0.00354    # volatility of variance (vol of vol)\n    ρ::Float64 = 0.024       # discount rate\n    γ::Float64 = 7.5         # relative risk aversion\n    ψ::Float64 = 1.5         # elasticity of intertemporal substitution\nend","title":"The model"},{"category":"section","location":"examples/asset_pricing/bansal_yaron/#The-state-space","page":"Bansal–Yaron: long-run risk (2D)","text":"We build the grid and the initial guess first, because they fix the names used everywhere else. This model has two state variables, so the grid is a NamedTuple with two keys (μ and v); the guess is a NamedTuple whose key is the unknown function (p, the wealth–consumption ratio), a matrix over the (mu v) grid. These names reappear inside the equation below — e.g. pμ_up is the forward finite difference of p in μ, and pμv is the cross-partial. The grid spans the ergodic ranges of mu (Normal) and v (Gamma). The sqrt v diffusion vanishes at v = 0, a degenerate boundary where no condition is imposed.\n\nm = BansalYaronModel()\nμn, vn = 30, 30\nμdistribution = Normal(m.μbar, sqrt(m.νμ^2 * m.vbar / (2 * m.κμ)))\nμs = range(quantile(μdistribution, 0.01), quantile(μdistribution, 0.99), length = μn)\nνdistribution = Gamma(2 * m.κv * m.vbar / m.νv^2, m.νv^2 / (2 * m.κv))\nvs = range(quantile(νdistribution, 0.0), quantile(νdistribution, 0.99), length = vn)\nstategrid = (; μ = μs, v = vs)\nyend = (; p = ones(μn, vn))","title":"The state space"},{"category":"section","location":"examples/asset_pricing/bansal_yaron/#The-equation","page":"Bansal–Yaron: long-run risk (2D)","text":"We now write the function encoding the HJB equation. Following the package convention, it takes the current state (a grid point) and u (each unknown together with its finite-difference derivatives there) and returns the time derivative of each unknown.\n\nfunction (m::BansalYaronModel)(state::NamedTuple, u::NamedTuple)\n    (; μbar, vbar, κμ, νμ, κv, νv, ρ, γ, ψ) = m\n    (; μ, v) = state\n    (; p, pμ_up, pμ_down, pv_up, pv_down, pμμ, pμv, pvv) = u\n\n    # drifts and volatilities of consumption, μ and v\n    μc = μ\n    σc = sqrt(v)\n    μμ = κμ * (μbar - μ)\n    σμ = νμ * sqrt(v)\n    μv = κv * (vbar - v)\n    σv = νv * sqrt(v)\n\n    # upwind each first derivative on the sign of its drift\n    pμ = (μμ >= 0) ? pμ_up : pμ_down\n    pv = (μv >= 0) ? pv_up : pv_down\n\n    σp_Zμ = pμ / p * σμ\n    σp_Zv = pv / p * σv\n    σp2 = σp_Zμ^2 + σp_Zv^2\n    μp = pμ / p * μμ + pv / p * μv + 0.5 * pμμ / p * σμ^2 + 0.5 * pvv / p * σv^2\n\n    # market prices of risk\n    κ_Zc = γ * σc\n    κ_Zμ = -(1 - γ * ψ) / (ψ - 1) * σp_Zμ\n    κ_Zv = -(1 - γ * ψ) / (ψ - 1) * σp_Zv\n\n    r = ρ + μc / ψ - (1 + 1 / ψ) / 2 * γ * σc^2 - (γ * ψ - 1) / (2 * (ψ - 1)) * σp2\n    pt = -p * (1 / p + μc + μp - r - κ_Zc * σc - κ_Zμ * σp_Zμ - κ_Zv * σp_Zv)\n    return (; pt)\nend\n\nWith the equation, grid, and guess in hand, pdesolve solves the stationary system:\n\nresult = pdesolve(m, stategrid, yend)","title":"The equation"},{"category":"section","location":"examples/asset_pricing/bansal_yaron/#The-solution","page":"Bansal–Yaron: long-run risk (2D)","text":"The wealth–consumption ratio increases in expected growth mu and falls with variance v: investors pay more for the claim when future growth is high, and less when the economy is riskier. Because p is defined over the (mu v) plane, we can show it as a surface and as a heatmap.\n\np = result.zero[:p]\ns1 = surface(vs, μs, p; xlabel = \"variance v\", ylabel = \"growth μ\", zlabel = \"p\", colorbar = false, title = \"wealth–consumption ratio\")\ns2 = heatmap(vs, μs, p; xlabel = \"variance v\", ylabel = \"growth μ\", title = \"p(μ, v)\")\nplot(s1, s2; size = (900, 360))\n\n\n\nThis page was generated using Literate.jl.","title":"The solution"},{"category":"section","location":"examples/asset_pricing/wachter/#Wachter-(2013):-time-varying-rare-disaster-risk","page":"Wachter: rare disasters","text":"Wachter asks whether a time-varying probability of rare consumption disasters can explain the high mean and volatility of the equity premium. Consumption follows a jump–diffusion: most of the time it grows smoothly, but at Poisson rate lambda_t a disaster strikes and lowers consumption by a random factor e^Z (with Z0). The single state is the disaster intensity lambda_t, which mean-reverts as a square-root (CIR) process:\n\ndlambda_t = kappa_lambda(barlambda - lambda_t)dt + nu_lambdasqrtlambda_tdB_t\n\nWith Epstein–Zin preferences the unknown is the wealth–consumption ratio p(lambda). Because disaster risk moves over time, so do risk premia and valuations — even though realized consumption is smooth outside disasters.","title":"Wachter (2013): time-varying rare-disaster risk"},{"category":"section","location":"examples/asset_pricing/wachter/#The-model","page":"Wachter: rare disasters","text":"The parameters live in a struct:\n\nusing EconPDEs, Distributions, Plots\n\nBase.@kwdef struct WachterModel{T<: Distribution}\n    # consumption process parameters\n    μ::Float64 = 0.025      # expected consumption growth\n    σ::Float64 = 0.02       # volatility of consumption growth\n\n    # disaster process parameters\n    λbar::Float64 = 0.0355  # long-run mean disaster intensity\n    κλ::Float64 = 0.08      # mean-reversion speed of disaster intensity\n    νλ::Float64 = 0.067     # volatility of disaster intensity\n    ZDistribution::T = Normal(-0.4, 0.25) # distribution of disaster jump size Z (log consumption drop); from Ian Martin higher order cumulants paper\n\n    # utility parameters\n    ρ::Float64 = 0.012      # discount rate\n    γ::Float64 = 3.0        # relative risk aversion\n    ψ::Float64 = 1.1        # elasticity of intertemporal substitution\n    ϕ::Float64 = 2.6        # leverage of dividends on consumption\nend","title":"The model"},{"category":"section","location":"examples/asset_pricing/wachter/#The-state-space","page":"Wachter: rare disasters","text":"We build the grid and the initial guess first, because they fix the names used everywhere else. The grid is a NamedTuple whose key is the state variable (λ, the disaster intensity); the guess is a NamedTuple whose key is the unknown function (p, the wealth–consumption ratio). These names reappear inside the equation below — e.g. pλ_up will be the forward finite difference of p in λ. The state lambda ranges over intensities from zero up to well above its long-run mean barlambda. The sqrtlambda diffusion vanishes at lambda = 0, a degenerate boundary where no condition is imposed.\n\nfunction initialize_stategrid(m::WachterModel; λn = 30)\n  (; λ = range(0.0, 0.1, length = λn))\nend\n\nfunction initialize_y(m::WachterModel, stategrid)\n    λn = length(stategrid[:λ])\n    (; p = ones(λn))\nend\n\nm = WachterModel()\nstategrid = initialize_stategrid(m)\nyend = initialize_y(m, stategrid)","title":"The state space"},{"category":"section","location":"examples/asset_pricing/wachter/#The-equation","page":"Wachter: rare disasters","text":"We now write the function encoding the HJB equation. Following the package convention, it takes the current state (a grid point) and u (each unknown together with its finite-difference derivatives there) and returns the time derivative of each unknown.\n\nfunction (m::WachterModel)(state::NamedTuple, u::NamedTuple)\n    (; μ, σ, λbar, κλ, νλ, ZDistribution, ρ, γ, ψ, ϕ) = m\n    (; λ) = state\n    (; p, pλ_up, pλ_down, pλλ) = u\n\n    # drift and volatility of λ and p\n    μλ = κλ * (λbar - λ)\n    σλ = νλ * sqrt(λ)\n    # upwind on the sign of the drift of λ\n    pλ = (μλ >= 0) ? pλ_up : pλ_down\n    μp = pλ / p * μλ + 0.5 * pλλ / p * σλ^2\n    σp_Zλ = pλ / p * σλ\n\n    # market prices of risk\n    κ_Zc = γ * σ\n    κ_Zλ = (γ * ψ - 1) / (ψ - 1) * σp_Zλ\n    η =  λ * (mgf(ZDistribution, 1) - 1 + mgf(ZDistribution, -γ) - 1 - (mgf(ZDistribution, 1 - γ) - 1))\n\n    # interest rate\n    r = ρ + μ / ψ - (1 + 1 / ψ) / 2 * γ * σ^2 - λ * (mgf(ZDistribution, -γ) - 1) + (1 / ψ - γ) / (1 - γ) * λ * (mgf(ZDistribution, 1 - γ) - 1) - (γ * ψ - 1) / (2 * (ψ - 1)) * σp_Zλ^2\n\n    # market pricing of the consumption claim\n    pt = - p * (1 / p  + μ + μp + λ * (mgf(ZDistribution, 1) - 1) - r - κ_Zc * σ - κ_Zλ * σp_Zλ - η)\n\n    return (; pt)\nend\n\nWith the equation, grid, and guess in hand, pdesolve solves the stationary system:\n\nresult = pdesolve(m, stategrid, yend)","title":"The equation"},{"category":"section","location":"examples/asset_pricing/wachter/#The-solution","page":"Wachter: rare disasters","text":"The wealth–consumption ratio falls steeply as the disaster intensity lambda rises: when a disaster is more likely, the representative agent discounts the consumption claim more heavily and valuations drop. Because lambda is persistent and itself volatile, this single channel generates large, time-varying risk premia and stock-market volatility out of an otherwise smooth consumption process — Wachter's central result.\n\nλs = stategrid[:λ]\nplot(λs, result.zero[:p]; xlabel = \"disaster intensity λ\", ylabel = \"wealth–consumption ratio p\", legend = false)\n\n\n\nThis page was generated using Literate.jl.","title":"The solution"},{"category":"section","location":"examples/asset_pricing/gomez/#Gomez-(2025):-wealth-inequality-and-asset-prices","page":"Gomez: wealth distribution","text":"An asset-pricing model with a wealth-distribution state. Households and entrepreneurs differ in preferences and in the risk they bear; entrepreneurs hold a leveraged position in the risky asset, so shifts in the wealth distribution move the price of risk and the volatility of returns. The single state x is the entrepreneurs' consumption share, and the unknown is the households' wealth–consumption ratio p_H. Demographics (birth, death, and entry of entrepreneurs) keep the distribution stationary.","title":"Gomez (2025): wealth inequality and asset prices"},{"category":"section","location":"examples/asset_pricing/gomez/#The-model","page":"Gomez: wealth distribution","text":"The parameters:\n\nusing EconPDEs, Plots\n\nBase.@kwdef struct GomezModel\n    # utility, households\n    γ::Float64 = 10.3      # households' relative risk aversion\n    ψ::Float64 = 0.05      # households' elasticity of intertemporal substitution\n    ρ::Float64 = 0.1       # households' discount rate\n\n    # utility, entrepreneurs\n    ρE::Float64 = 0.022    # entrepreneurs' discount rate\n    αE::Float64 = 2        # entrepreneurs' risk aversion\n    ν::Float64 = 0.1       # entrepreneurs' preference parameter\n\n    # demography\n    η::Float64 = 0.015     # birth rate\n    δ::Float64 = 0.025     # death rate\n    ϕ::Float64 = 0.01      # demographic transition rate\n    πE::Float64 = 0.09     # entry rate into entrepreneurship\n\n    # endowment process\n    g::Float64 = 0.02      # expected consumption growth\n    σ::Float64 = 0.04      # consumption volatility\n    λ::Float64 = 2.3       # endowment-process parameter\n    τ::Float64 = 0.0       # tax rate\nend","title":"The model"},{"category":"section","location":"examples/asset_pricing/gomez/#The-state-space","page":"Gomez: wealth distribution","text":"We build the grid and the initial guess first, because they fix the names used everywhere else. The grid is a NamedTuple whose key is the state variable (x, the entrepreneurs' consumption share); the guess is a NamedTuple whose key is the unknown function (pH), holding one starting value at each grid point. These names are what reappear inside the equation below — e.g. pHx_up will be the forward finite difference of pH in x.\n\nOne state x in 0 1 on a squared grid of 300 points. The single unknown p_H is initialized flat at one.\n\nm = GomezModel()\nstategrid = (; x = range(0, 1, 300) .^ 2)\nyend = (; pH = ones(length(stategrid[:x])))","title":"The state space"},{"category":"section","location":"examples/asset_pricing/gomez/#The-equation","page":"Gomez: wealth distribution","text":"We now write the function encoding the equilibrium conditions. Following the package convention, it takes the current state (a grid point) and u — the local bundle holding the unknown and its finite-difference derivatives there — and returns the time derivative of the unknown (pHt).\n\nThe volatility of the state sigma_x is endogenous, feeding back through prices, so the upwind direction is picked from the drift mu_x: forward difference pHx_up unless the drift turns negative, then backward pHx_down.\n\nfunction (m::GomezModel)(state, u)\n  (; γ, ψ, ρ, ρE, αE, ν, η, δ, ϕ, πE, g, σ, λ, τ) = m\n  (; x) = state\n  (; pH, pHx_up, pHx_down, pHxx) = u\n  p = x / ρE + (1 - x) * pH\n  xW = x / ρE / p\n  if xW * αE >= 1\n    αE = 1 / xW\n  end\n  iter = 0\n  pHx = pHx_up\n  @label start\n  px = 1 / ρE  - pH + (1 - x) * pHx\n  σx = x * (αE - 1) * σ / (1 - x * αE * px / p)\n  σpH = pHx / pH * σx\n  σp = px / p * σx\n  σCE = αE * (σ + σp)\n  σCH = x < 1 ? (σ - x * σCE) / (1 - x) : 0.0\n  σWH = σCH + σpH\n  κ = γ * σCH + (γ * ψ - 1) / (ψ - 1) * σpH\n  ΦE = αE * κ * (σ + σp)\n  ΦH = κ^2 * (1 + ψ) / (2 * γ) + (1 - ψ * γ) / (γ * (ψ - 1)) * κ * σpH - (1 - γ * ψ) / (2 * γ * (ψ - 1)) * σpH^2\n  arriving_wealth = (η + δ + ϕ + τ) / (η + δ) * p\n  wedge = (η + δ) * ((ρE * πE + (1 - πE) / pH) * arriving_wealth - 1)\n  Ψ = x + (1 - x) * ψ\n  P = (x * ρE + (1 - x) * ψ * ρ) / Ψ\n  r = P + 1 / Ψ * (g - x * ΦE - (1 - x) * ΦH - wedge) + τ\n  μCE = r - τ -  ρE + ΦE\n  μCH = ψ * (r - τ - ρ) + ΦH\n  μx = x * (μCE - g) + (η + δ) * (ρE * πE * arriving_wealth - x) - σ * σx\n  if (iter == 0) & (μx <= 0)\n    iter += 1\n    pHx = pHx_down\n    @goto start\n  end\n  μpH = pHx / pH * μx + 0.5 * pHxx / pH * σx^2\n  pxx = - 2 * pHx + (1 - x) * pHxx\n  μp = px / p * μx + 0.5 * pxx / p * σx^2\n  μWH = μCH + μpH + σCH * σpH\n  pHt = - pH * (1 / pH + μWH - r + τ - κ * σWH)\n  σR = σ + σp\n  μR = r + κ * σR\n  σWE = σCE\n  σErelative = (αE - 1) * (σ + σp)\n  μErelative = μCE - (g + μp + σ * σp) - σErelative * (σ + σp)\n  α = x < 1 ? (1 - xW * αE) / (1 - xW) : 0.0\n  σHrelative = (α - 1) * (σ + σp)\n  μHrelative = μWH - (g + μp + σ * σp) - σHrelative * (σ + σp)\n  μxW = xW * (μCE - (g + μp + σ * σp) - σErelative * (σ + σp)) + (η + δ) * (πE * (η + δ + ϕ) / (η + δ) - xW)\n  σxW = xW * (αE - 1) * (σ + σp)\n  μxW2 = xW * ((αE - 1) * κ * (σ + σp) - ρE + 1 / p - ϕ - (αE - 1) * (σ + σp)^2) + (η + δ) * (πE * (η + δ + ϕ) / (η + δ) - xW)\n  α = σWH / (σ + σp)\n  σp = px / p * σx\n  μRλ = r + λ * (μR - r)\n  σRλ = λ * σR\n  return (; pHt,), (; x, μx, σx, κ, r, σWH, μWH, σp, p, px, σR, μR, μRλ, σRλ, μErelative, σErelative, μHrelative, σHrelative, μCE, μxW, σxW, pH, μCH, wedge, xW, σWE, σCH, αE, pE = 1 / m.ρE, μp = μp)\nend\n\nWith the equation, grid, and guess in hand, pdesolve solves the stationary system:\n\n@time result = pdesolve(m, stategrid, yend)","title":"The equation"},{"category":"section","location":"examples/asset_pricing/gomez/#The-solution","page":"Gomez: wealth distribution","text":"We saved the volatility of the risky return sigma_R. It differs from the fundamental volatility sigma because prices themselves move with the wealth distribution — endogenous amplification — and the size of that wedge varies with the entrepreneurs' consumption share x.\n\nxs = stategrid[:x]\nplot(xs, result.optional[:σR]; xlabel = \"entrepreneurs' consumption share x\", ylabel = \"return volatility σR\", legend = false)\n\n\n\nThis page was generated using Literate.jl.","title":"The solution"},{"category":"section","location":"examples/asset_pricing/di_tella/#Di-Tella-(2017):-uncertainty-shocks-and-balance-sheet-recessions","page":"Di Tella: balance-sheet risk (2D)","text":"A two-state general-equilibrium model with a financial sector. Experts and households have recursive preferences and an agency friction (moral hazard) forces experts to bear idiosyncratic risk. There are two states: the experts' wealth share x and a stochastic idiosyncratic variance nu — the uncertainty shock. Three functions are solved jointly: the two agents' value functions p_A p_B and the capital price p, which is pinned down by an algebraic market-clearing constraint (is_algebraic) rather than by its own time derivative.","title":"Di Tella (2017): uncertainty shocks and balance-sheet recessions"},{"category":"section","location":"examples/asset_pricing/di_tella/#The-model","page":"Di Tella: balance-sheet risk (2D)","text":"The parameters:\n\nusing EconPDEs, Distributions, Plots\n\nBase.@kwdef mutable struct DiTellaModel\n  # Utility Function\n  γ::Float64 = 5.0        # relative risk aversion\n  ψ::Float64 = 1.5        # elasticity of intertemporal substitution\n  ρ::Float64 = 0.05       # rate of time preference (discount rate)\n  τ::Float64 = 0.4        # transition rate between experts and households\n\n  # Technology\n  A::Float64 = 200.0      # investment adjustment-cost (technology) parameter\n  σ::Float64 = 0.03       # aggregate (fundamental) volatility of capital\n\n  # MoralHazard\n  ϕ::Float64 = 0.2        # moral-hazard idiosyncratic-risk retention parameter\n\n  # Idiosyncratic\n  νbar::Float64 = 0.24    # long-run mean of idiosyncratic variance ν\n  κν::Float64 = 0.22      # mean-reversion speed of ν (uncertainty process)\n  σνbar::Float64 = -0.13  # volatility loading of the ν process\nend","title":"The model"},{"category":"section","location":"examples/asset_pricing/di_tella/#The-state-space","page":"Di Tella: balance-sheet risk (2D)","text":"We build the grid and the initial guess first, because they fix the names used everywhere else. The grid is a NamedTuple whose keys are the two state variables (x, the experts' wealth share, and ν, the idiosyncratic variance); the guess is a NamedTuple whose keys are the unknown functions (pA, pB, p), holding one starting value at each grid point. These names are what reappear inside the equation below — e.g. pAx_up will be the forward finite difference of pA in x.\n\nTwo helpers build a 30×30 grid over (x nu) and a flat guess. The variance state nu follows a Gamma-distributed CIR process, so its grid spans that ergodic range.\n\nfunction initialize_stategrid(m::DiTellaModel; xn = 30, νn = 30)\n  xs = range(0.01, 0.99, length = xn)\n  distribution = Gamma(2 * m.κν * m.νbar / m.σνbar^2, m.σνbar^2 / (2 * m.κν))\n  νs = range(quantile(distribution, 0.001), quantile(distribution, 0.999), length = νn)\n  (; x = xs, ν = νs)\nend\n\nfunction initialize_y(m::DiTellaModel, stategrid)\n  xn = length(stategrid[:x])\n  νn = length(stategrid[:ν])\n  (; pA = 20 * ones(xn, νn), pB = 20 * ones(xn, νn), p = 20 * ones(xn, νn))\nend\n\nm = DiTellaModel()\nstategrid = initialize_stategrid(m)\nyend = initialize_y(m, stategrid)","title":"The state space"},{"category":"section","location":"examples/asset_pricing/di_tella/#The-equation","page":"Di Tella: balance-sheet risk (2D)","text":"We now write the function encoding the equilibrium conditions. Following the package convention, it takes the current state (a grid point) and u — the local bundle holding each unknown and its finite-difference derivatives there — and returns the time derivative of each unknown (pAt, pBt, pt).\n\nWith two states, both first derivatives and the cross derivative are upwinded — the first derivatives on the sign of their drifts, the cross term on the sign of the x–nu covariance.\n\nfunction (m::DiTellaModel)(state::NamedTuple, u::NamedTuple)\n  (; γ, ψ, ρ, τ, A, σ, ϕ, νbar, κν, σνbar) = m\n  (; x, ν) = state\n  (; pA, pAx_up, pAx_down, pAν_up, pAν_down, pAxx, pAxν_up, pAxν_down, pAνν, pB, pBx_up, pBx_down, pBν_up, pBν_down, pBxx, pBxν_up, pBxν_down, pBνν, p, px_up, px_down, pν_up, pν_down, pxx, pxν_up, pxν_down, pνν) = u\n\n  # drift and volatility of state variable ν\n  g = p / (2 * A)\n  i = A * g^2\n  μν = κν * (νbar - ν)\n  σν = σνbar * sqrt(ν)\n  pAν = (μν >= 0) ? pAν_up : pAν_down\n  pBν = (μν >= 0) ? pBν_up : pBν_down\n  pν = (μν >= 0) ? pν_up : pν_down\n\n  pAx, pBx, px = pAx_up, pBx_up, px_up\n  iter = 0\n  @label start\n  σX = x * (1 - x) * (1 - γ) / (γ * (ψ - 1)) * (pAν / pA - pBν / pB) * σν / (1 - x * (1 - x) * (1 - γ) / (γ * (ψ - 1)) * (pAx / pA - pBx / pB))\n  σpA = pAx / pA * σX + pAν / pA * σν\n  σpB = pBx / pB * σX + pBν / pB * σν\n  σp = px / p * σX + pν / p * σν\n  κ = (σp + σ - (1 - γ) / (γ * (ψ - 1)) * (x * σpA + (1 - x) * σpB)) / (1 / γ)\n  κν = γ * ϕ * ν / x\n  σA = κ / γ + (1 - γ) / (γ * (ψ - 1)) * σpA\n  νA = κν / γ\n  σB = κ / γ + (1 - γ) / (γ * (ψ - 1)) * σpB\n\n  # Interest rate r\n  μX = x * (1 - x) * ((σA * κ + νA * κν - 1 / pA - τ) - (σB * κ -  1 / pB + τ * x / (1 - x)) - (σA - σB) * (σ + σp))\n\n  # upwinding\n  if (iter == 0) && (μX <= 0)\n    iter += 1\n    pAx, pBx, px = pAx_down, pBx_down, px_down\n    @goto start\n  end\n\n  # upwind the cross derivative on the sign of its coefficient σX * σν (the x-ν covariance)\n  pAxν = (σX * σν >= 0) ? pAxν_up : pAxν_down\n  pBxν = (σX * σν >= 0) ? pBxν_up : pBxν_down\n  pxν = (σX * σν >= 0) ? pxν_up : pxν_down\n\n  μpA = pAx / pA * μX + pAν / pA * μν + 0.5 * pAxx / pA * σX^2 + 0.5 * pAνν / pA * σν^2 + pAxν / pA * σX * σν\n  μpB = pBx / pB * μX + pBν / pB * μν + 0.5 * pBxx / pB * σX^2 + 0.5 * pBνν / pB * σν^2 + pBxν / pB * σX * σν\n  μp = px / p * μX + pν / p * μν + 0.5 * pxx / p * σX^2 + 0.5 * pνν / p * σν^2 + pxν / p * σX * σν\n  r = (1 - i) / p + g + μp + σ * σp - κ * (σ + σp) - γ / x * (ϕ * ν)^2\n\n  # Market Pricing\n  pAt = - pA * (1 / pA  + (ψ - 1) * τ / (1 - γ) * ((pA / pB)^((1 - γ) / (1 - ψ)) - 1) - ψ * ρ + (ψ - 1) * (r + κ * σA + κν * νA) + μpA - (ψ - 1) * γ / 2 * (σA^2 + νA^2) + (2 - ψ - γ) / (2 * (ψ - 1)) * σpA^2 + (1 - γ) * σpA * σA)\n  pBt = - pB * (1 / pB - ψ * ρ + (ψ - 1) * (r + κ * σB) + μpB - (ψ - 1) * γ / 2 * σB^2 + (2 - ψ - γ) / (2 * (ψ - 1)) * σpB^2 + (1 - γ) * σpB * σB)\n  # algebraic constraint\n  pt = - p * ((1 - i) / p - x / pA - (1 - x) / pB)\n  return (; pAt, pBt, pt)\nend\n\np enters as an algebraic (constraint) variable, and a small time step Δ is used:\n\nresult = pdesolve(m, stategrid, yend; is_algebraic = (; pA = false, pB = false, p = true), Δ = 1e-2)","title":"The equation"},{"category":"section","location":"examples/asset_pricing/di_tella/#The-solution","page":"Di Tella: balance-sheet risk (2D)","text":"We show the equilibrium capital price p as a heatmap over the two states — the wealth distribution x and the idiosyncratic-uncertainty state nu. The price varies across the whole state space, reflecting how balance sheets and uncertainty jointly move asset prices.\n\nxs = stategrid[:x]\nνs = stategrid[:ν]\nheatmap(νs, xs, result.zero[:p]; xlabel = \"idiosyncratic variance ν\", ylabel = \"experts' wealth share x\", title = \"capital price p(x, ν)\")\n\n\n\nThis page was generated using Literate.jl.","title":"The solution"},{"category":"section","location":"examples/neoclassical_growth/#Neoclassical-growth-model","page":"Neoclassical growth","text":"The deterministic neoclassical (Ramsey–Cass–Koopmans) growth model is the gentlest introduction: one state variable, one value function, and a closed-form steady state to check the solution against. A representative household chooses consumption to solve\n\nmax_c int_0^infty e^-rho t fracc^1-gamma1-gamma dt\nqquad textsubject to qquad dot k = A k^alpha - delta k - c\n\nand the value function v(k) solves the Hamilton–Jacobi–Bellman equation\n\nrho v(k) = max_c fracc^1-gamma1-gamma + v(k)bigl(A k^alpha - delta k - cbigr)\n\nwith first-order condition c = v(k)^-1gamma.","title":"Neoclassical growth model"},{"category":"section","location":"examples/neoclassical_growth/#The-model","page":"Neoclassical growth","text":"The parameters live in a struct:\n\nusing EconPDEs, Plots\n\nBase.@kwdef struct NeoclassicalGrowthModel\n    A::Float64 = 0.5     # productivity\n    α::Float64 = 0.3     # capital share\n    δ::Float64 = 0.05    # depreciation\n    ρ::Float64 = 0.05    # discount rate\n    γ::Float64 = 2.0     # relative risk aversion\nend","title":"The model"},{"category":"section","location":"examples/neoclassical_growth/#The-state-space","page":"Neoclassical growth","text":"We build the grid and the initial guess first, because they fix the names used everywhere else. The grid is a NamedTuple whose key is the state variable (k); the guess is a NamedTuple whose key is the unknown function (v), holding one starting value at each grid point. These names are what reappear inside the equation below — e.g. vk_up will be the forward finite difference of v in k.\n\nWe center the grid on the closed-form steady state bar k (where alpha A bar k^alpha-1 = rho + delta) and start from the value of consuming gross output forever.\n\nm = NeoclassicalGrowthModel()\n(; A, α, δ, ρ, γ) = m\nk̄ = (α * A / (ρ + δ))^(1 / (1 - α))\nstategrid = (; k = range(0.1 * k̄, 5 * k̄, length = 1000))\nyend = (; v = [(A * k^α)^(1 - γ) / (1 - γ) / ρ for k in stategrid[:k]])","title":"The state space"},{"category":"section","location":"examples/neoclassical_growth/#The-equation","page":"Neoclassical growth","text":"We now write the function encoding the HJB equation. Following the package convention, it takes the current state (a grid point) and u — the local bundle holding each unknown and its finite-difference derivatives there (here v, vk_up, vk_down) — and returns the time derivative vt of each unknown.\n\nThe capital drift dot k can point either way, so the first derivative is upwinded: forward (vk_up) where the implied drift is positive, backward (vk_down) where it is negative, and the consumption that sets the drift to zero at the steady state in between. A second NamedTuple saves consumption c and the drift μk on the grid.\n\nfunction (m::NeoclassicalGrowthModel)(state::NamedTuple, u::NamedTuple)\n    (; A, α, δ, ρ, γ) = m\n    (; k) = state\n    (; v, vk_up, vk_down) = u\n    c_up = vk_up >= 0 ? min(vk_up^(-1 / γ), 10 * A * k^α) : 10 * A * k^α\n    μk_up = A * k^α - δ * k - c_up\n    if μk_up > 0\n        c, vk, μk = c_up, vk_up, μk_up\n    else\n        c_down = vk_down >= 0 ? min(vk_down^(-1 / γ), 10 * A * k^α) : 10 * A * k^α\n        μk_down = A * k^α - δ * k - c_down\n        if μk_down < 0\n            c, vk, μk = c_down, vk_down, μk_down\n        else\n            μk = 0.0\n            c = A * k^α - δ * k\n            vk = c^(-γ)\n        end\n    end\n    vt = -(c^(1 - γ) / (1 - γ) + μk * vk - ρ * v)\n    return (; vt), (; c, μk)\nend\n\nWith the equation, grid, and guess in hand, pdesolve solves the stationary system:\n\nresult = pdesolve(m, stategrid, yend)","title":"The equation"},{"category":"section","location":"examples/neoclassical_growth/#The-solution","page":"Neoclassical growth","text":"The value function is increasing and concave in capital:\n\nks = stategrid[:k]\nplot(ks, result.zero[:v]; xlabel = \"capital k\", ylabel = \"value v(k)\", legend = false)\n\nConsumption rises with capital (left). The capital drift mu_k (right) is positive below the steady state and negative above it, crossing zero exactly at bar k — the saddle-path-stable steady state the economy is drawn toward.\n\np1 = plot(ks, result.optional[:c]; xlabel = \"capital k\", ylabel = \"consumption c(k)\", legend = false)\np2 = plot(ks, result.optional[:μk]; xlabel = \"capital k\", ylabel = \"capital drift μk\", legend = false)\nhline!(p2, [0.0]; color = :gray, linestyle = :dash)\nvline!(p2, [k̄]; color = :red, linestyle = :dot)\nplot(p1, p2; layout = (1, 2), size = (800, 300))\n\n\n\nThis page was generated using Literate.jl.","title":"The solution"},{"category":"section","location":"examples/consumption_saving/consumption_saving_one_asset/#Achdou–Han–Lasry–Lions–Moll:-consumption-and-saving","page":"Consumption–saving: one asset","text":"This is the diffusive-income version of the Achdou–Han–Lasry–Lions–Moll (2022) consumption– saving problem, the workhorse building block of continuous-time heterogeneous-agent (HACT) models. A household earns stochastic labor income y, saves in a single riskless asset a at rate r, and cannot borrow past a limit a ge a_min. Income mean-reverts as a diffusion, and the value function v(y a) solves the Hamilton–Jacobi–Bellman equation\n\nrho v = max_c fracc^1-gamma1-gamma\n  + partial_a vbigl(y + r a - cbigr)\n  + partial_y vkappa_y(bar y - y)\n  + tfrac12 sigma_y^2 partial_yy v\n\nwith first-order condition c = (partial_a v)^-1gamma and the borrowing constraint a ge a_min enforced as a state constraint (saving cannot be negative at the limit).","title":"Achdou–Han–Lasry–Lions–Moll: consumption and saving"},{"category":"section","location":"examples/consumption_saving/consumption_saving_one_asset/#The-model","page":"Consumption–saving: one asset","text":"The parameters live in a struct:\n\nusing EconPDEs, Distributions, Plots\n\nBase.@kwdef struct AchdouHanLasryLionsMollModel_Diffusion\n    κy::Float64 = 0.1      # income mean-reversion speed\n    ybar::Float64 = 1.0    # mean income level\n    σy::Float64 = 0.07     # income volatility\n    r::Float64 = 0.03      # risk-free rate\n    ρ::Float64 = 0.05      # discount rate\n    γ::Float64 = 2.0       # relative risk aversion\n    amin::Float64 = 0.0    # borrowing limit\n    amax::Float64 = 500.0  # top of the asset grid\nend","title":"The model"},{"category":"section","location":"examples/consumption_saving/consumption_saving_one_asset/#The-state-space","page":"Consumption–saving: one asset","text":"We build the grid and the initial guess first, because they fix the names used everywhere else. The grid is a NamedTuple whose keys are the two state variables (y and a); the guess is a NamedTuple whose key is the unknown function (v), holding one starting value at each grid point. These names are what reappear inside the equation below — e.g. va_up will be the forward finite difference of v in a.\n\nIncome lives on a grid spanning the bulk of its ergodic (Gamma) distribution, and assets on a grid from the borrowing limit up to a_max. The initial guess is the value of consuming the annuity value of total wealth a + yr forever.\n\nm = AchdouHanLasryLionsMollModel_Diffusion()\ndistribution = Gamma(2 * m.κy * m.ybar / m.σy^2, m.σy^2 / (2 * m.κy))\nstategrid = (; y = range(quantile(distribution, 0.001), quantile(distribution, 0.999), length = 10),\n                        a =  range(m.amin, m.amax, length = 200)\n                        )\nyend = (; v = [(m.ρ / m.γ + (1 - 1 / m.γ) * m.r)^(-m.γ) * (a + y / m.r)^(1 - m.γ) / (1 - m.γ) for y in stategrid[:y], a in stategrid[:a]])","title":"The state space"},{"category":"section","location":"examples/consumption_saving/consumption_saving_one_asset/#The-equation","page":"Consumption–saving: one asset","text":"We now write the function encoding the HJB equation. Following the package convention, it takes the current state (a grid point) and u (each unknown together with its finite-difference derivatives there) and returns the time derivative of each unknown.\n\nIncome drift is exogenous, so its derivative is upwinded on the sign of mu_y. The asset drift mu_a = y + r a - c is endogenous: we try the forward derivative, fall back to the backward one, and — when both fail or the borrowing constraint binds — set mu_a = 0 and consume out of income and interest. We save the resulting saving rate mu_a on the grid.\n\nfunction (m::AchdouHanLasryLionsMollModel_Diffusion)(state::NamedTuple, u::NamedTuple)\n    (; κy, σy, ybar, r, ρ, γ, amin, amax) = m\n    (; y, a) = state\n    (; v, vy_up, vy_down, va_up, va_down, vyy, vya, vaa) = u\n    μy = κy * (ybar - y)\n    # Newton can try negative marginal values, so cap implied consumption instead of flooring derivatives.\n    cmax = 100.0 * (y + r * max(a, 0.0))\n\n    # upwinding for income direction (easy because exogenous income drift)\n    vy = (μy >= 0) ? vy_up : vy_down\n\n    # upwinding for asset direction (harder because endogeneous asset drift)\n    c_up = va_up > 0 ? min(va_up^(-1 / γ), cmax) : cmax\n    μa_up = y + r * a - c_up\n    if μa_up >= 0.0\n        va = va_up\n        c = c_up\n        μa = μa_up\n    else\n        c_down = va_down > 0 ? min(va_down^(-1 / γ), cmax) : cmax\n        μa_down = y + r * a - c_down\n        if (μa_down <= 0.0) && (a > amin)\n            va = va_down\n            c = c_down\n            μa = μa_down\n        else\n            # If the two candidates straddle zero OR drift is negative at minimum asset threshold\n            # (i.e. borrowing constraint), then, we must have drift μa = 0.\n            c = y + r * a\n            va = c^(-γ)\n            μa = 0.0\n        end\n    end\n    vt = - (c^(1 - γ) / (1 - γ) + μa * va + μy * vy + 0.5 * vyy * σy^2 - ρ * v)\n    return (; vt), (; μa, c)\nend\n\nWith the equation, grid, and guess in hand, pdesolve solves the stationary system:\n\nresult = pdesolve(m, stategrid, yend)","title":"The equation"},{"category":"section","location":"examples/consumption_saving/consumption_saving_one_asset/#The-solution","page":"Consumption–saving: one asset","text":"We plot policies against assets for three income levels (low, median, high). Consumption is recovered from the budget identity c = y + r a - mu_a.\n\nas = stategrid[:a]\nys = stategrid[:y]\nidx = 1:div(length(as), 3)          # left third of the asset grid, where the curvature is\niys = round.(Int, range(1, length(ys), length = 3))\nμa = result.optional[:μa]\nc = result.optional[:c]\n\np1 = plot(xlabel = \"assets a\", ylabel = \"consumption c\")\nfor iy in iys\n    plot!(p1, as[idx], c[iy, idx], label = \"y = $(round(ys[iy], digits = 2))\")\nend\np2 = plot(xlabel = \"assets a\", ylabel = \"saving μa\")\nfor iy in iys\n    plot!(p2, as[idx], μa[iy, idx], label = \"y = $(round(ys[iy], digits = 2))\")\nend\nhline!(p2, [0.0]; color = :gray, linestyle = :dash, label = \"\")\nplot(p1, p2; layout = (1, 2), size = (800, 300))\n\nConsumption rises with both assets and income (left). Saving (right) tells the story of the borrowing constraint: at the limit a = a_min a low-income household would like to borrow but cannot, so its saving is pinned at zero and it consumes all of its income and interest. Because households are impatient relative to the interest rate (rho  r), saving turns negative at high wealth — every income level has a finite target above which it runs its assets down — which is exactly what keeps the stationary wealth distribution from spreading without bound.\n\n\n\nThis page was generated using Literate.jl.","title":"The solution"},{"category":"section","location":"examples/asset_pricing/garleanu_panageas/#Gârleanu–Panageas-(2015):-heterogeneous-agents","page":"Gârleanu–Panageas: heterogeneous agents","text":"This is the package's most demanding kind of problem: a general-equilibrium model with endogenous volatility and several coupled value functions. Two types of agents differ in risk aversion and elasticity of intertemporal substitution — type A is \"bold\" (low gamma_A), type B \"conservative\" (high gamma_B). The single state x is the consumption share of type A. Four claims are priced jointly: the wealth–consumption ratios p_A p_B of the two agents and two human-capital claims phi_1 phi_2.\n\nWhat makes it hard is that the volatility of the state, sigma_x, is endogenous — it depends on the very derivatives p_A p_B we are solving for — so the upwind direction must be recomputed from the resulting drift mu_x each iteration.","title":"Gârleanu–Panageas (2015): heterogeneous agents"},{"category":"section","location":"examples/asset_pricing/garleanu_panageas/#The-model","page":"Gârleanu–Panageas: heterogeneous agents","text":"The parameters:\n\nusing EconPDEs, Plots\n\nBase.@kwdef struct GarleanuPanageasModel\n    γA::Float64 = 1.5      # relative risk aversion, type A (bold)\n    ψA::Float64 = 0.7      # elasticity of intertemporal substitution, type A\n    γB::Float64 = 10.0     # relative risk aversion, type B (conservative)\n    ψB::Float64 = 0.05     # elasticity of intertemporal substitution, type B\n    ρ::Float64 = 0.001     # rate of time preference (discount rate)\n    δ::Float64 = 0.02      # death (turnover) rate\n    νA::Float64 = 0.01     # share of type A among newborns\n    μ::Float64 = 0.02      # aggregate consumption growth (drift)\n    σ::Float64 = 0.041     # aggregate consumption volatility\n    B1::Float64 = 30.72    # loading on first component of labor-income profile\n    δ1::Float64 = 0.0525   # decay rate of first labor-income component\n    B2::Float64 = -30.29   # loading on second component of labor-income profile\n    δ2::Float64 = 0.0611   # decay rate of second labor-income component\n    ω::Float64 = 0.92      # labor share of aggregate output\nend","title":"The model"},{"category":"section","location":"examples/asset_pricing/garleanu_panageas/#The-state-space","page":"Gârleanu–Panageas: heterogeneous agents","text":"We build the grid and the initial guess first, because they fix the names used everywhere else. The grid is a NamedTuple whose key is the state variable (x, the consumption share of type A); the guess is a NamedTuple whose keys are the unknown functions (pA, pB, ϕ1, ϕ2), holding one starting value at each grid point. These names are what reappear inside the equation below — e.g. pAx_up will be the forward finite difference of pA in x.\n\nOne state x in 0 1, four coupled unknowns, all initialized flat at one.\n\nm = GarleanuPanageasModel()\nstategrid = (; x = range(0.0, 1.0, length = 100))\nyend = (; pA = ones(length(stategrid[:x])), pB = ones(length(stategrid[:x])), ϕ1 = ones(length(stategrid[:x])), ϕ2 = ones(length(stategrid[:x])))","title":"The state space"},{"category":"section","location":"examples/asset_pricing/garleanu_panageas/#The-equation","page":"Gârleanu–Panageas: heterogeneous agents","text":"We now write the function encoding the equilibrium conditions. Following the package convention, it takes the current state (a grid point) and u — the local bundle holding each unknown and its finite-difference derivatives there — and returns the time derivative of each unknown (pAt, pBt, ϕ1t, ϕ2t).\n\nBecause sigma_x is endogenous, the loop recomputes the upwind direction from the drift mu_x: it starts with forward differences (*_up) and switches to backward (*_down) when the drift turns negative.\n\nfunction (m::GarleanuPanageasModel)(state::NamedTuple, u::NamedTuple)\n    (; γA, ψA, γB, ψB, ρ, δ, νA, μ, σ, B1, δ1, B2, δ2, ω) = m\n    (; x) = state\n    (; pA, pAx_up, pAx_down, pAxx, pB, pBx_up, pBx_down, pBxx, ϕ1, ϕ1x_up, ϕ1x_down, ϕ1xx, ϕ2, ϕ2x_up, ϕ2x_down, ϕ2xx) = u\n\n    scale = δ / (δ + δ1) * B1 + δ / (δ + δ2) * B2\n    B1 = B1 / scale\n    B2 = B2 / scale\n\n    # endogenous σx depends on the unknown derivatives, so pick the upwind direction from μx\n    pAx, pBx, ϕ1x, ϕ2x = pAx_up, pBx_up, ϕ1x_up, ϕ2x_up\n    iter = 0\n    @label start\n    Γ = 1 / (x / γA + (1 - x) / γB)\n    p = x * pA + (1 - x) * pB\n    σx = σ * x * (Γ / γA - 1) / (1 + Γ * x * (1 - x) / (γA * γB) * ((1 - γB * ψB) / (ψB - 1) * (pBx / pB) - (1 - γA * ψA) / (ψA - 1) * (pAx / pA)))\n    σpA = pAx / pA * σx\n    σpB = pBx / pB * σx\n    σϕ1 = ϕ1x / ϕ1 * σx\n    σϕ2 = ϕ2x / ϕ2 * σx\n    κ = Γ * (σ - x * (1 - γA * ψA) / (γA * (ψA - 1)) * σpA - (1 - x) * (1 - γB * ψB) / (γB * (ψB - 1)) * σpB)\n    σCA = κ / γA + (1 - γA * ψA) / (γA * (ψA - 1)) * σpA\n    σCB = κ / γB + (1 - γB * ψB) / (γB * (ψB - 1)) * σpB\n    mcA = κ^2 * (1 + ψA) / (2 * γA) + (1 - ψA * γA) / (γA * (ψA - 1)) * κ * σpA - (1 - γA * ψA) / (2 * γA * (ψA - 1)) * σpA^2\n    mcB = κ^2 * (1 + ψB) / (2 * γB) + (1 - ψB * γB) / (γB * (ψB - 1)) * κ * σpB - (1 - γB * ψB) / (2 * γB * (ψB - 1)) * σpB^2\n    r = ρ + 1 / (ψA * x + ψB * (1 - x)) * (μ - x * mcA - (1 - x) * mcB - δ * ((νA / pA + (1 - νA) / pB) * ω * (B1 * ϕ1 + B2 * ϕ2) - 1))\n    μCA = ψA * (r - ρ) + mcA\n    μCB = ψB * (r - ρ) + mcB\n    μx = x * (μCA - μ) + δ * (νA / pA * ω * (B1 * ϕ1 + B2 * ϕ2) - x) - σ * σx\n    if (iter == 0) && (μx <= 0)\n        iter += 1\n        pAx, pBx, ϕ1x, ϕ2x = pAx_down, pBx_down, ϕ1x_down, ϕ2x_down\n        @goto start\n    end\n\n    μpA = pAx / pA * μx + 0.5 * pAxx / pA * σx^2\n    μpB = pBx / pB * μx + 0.5 * pBxx / pB * σx^2\n    μϕ1 = ϕ1x / ϕ1 * μx + 0.5 * ϕ1xx / ϕ1 * σx^2\n    μϕ2 = ϕ2x / ϕ2 * μx + 0.5 * ϕ2xx / ϕ2 * σx^2\n\n    pAt = -pA * (1 / pA + μCA + μpA + σCA * σpA - r - δ - κ * (σpA + σCA))\n    pBt = -pB * (1 / pB + μCB + μpB + σCB * σpB - r - δ - κ * (σpB + σCB))\n    ϕ1t = -ϕ1 * (1 / ϕ1 + μ - δ - δ1 + μϕ1 + σ * σϕ1 - r - κ * (σϕ1 + σ))\n    ϕ2t = -ϕ2 * (1 / ϕ2 + μ - δ - δ2 + μϕ2 + σ * σϕ2 - r - κ * (σϕ2 + σ))\n\n    return (; pAt, pBt, ϕ1t, ϕ2t), (; r, κ)\nend\n\nWith the equation, grid, and guess in hand, pdesolve solves the stationary system:\n\nresult = pdesolve(m, stategrid, yend)","title":"The equation"},{"category":"section","location":"examples/asset_pricing/garleanu_panageas/#The-solution","page":"Gârleanu–Panageas: heterogeneous agents","text":"We saved the equilibrium interest rate r and the price of risk kappa on the grid. The price of risk is high when the conservative agents (type B, high gamma) hold most of the economy's wealth (low x) and falls as the bold agents (type A) come to dominate — risk is borne more willingly when the less risk-averse agents are wealthier.\n\nxs = stategrid[:x]\np1 = plot(xs, result.optional[:κ]; xlabel = \"consumption share of bold agents x\", ylabel = \"price of risk κ\", legend = false)\np2 = plot(xs, result.optional[:r]; xlabel = \"consumption share of bold agents x\", ylabel = \"interest rate r\", legend = false)\nplot(p1, p2; layout = (1, 2), size = (800, 300))\n\n\n\nThis page was generated using Literate.jl.","title":"The solution"},{"category":"section","location":"examples/asset_pricing/brunnermeier_sannikov/#Brunnermeier–Sannikov-(2014):-a-macroeconomic-model-with-a-financial-sector","page":"Brunnermeier–Sannikov: macro-finance","text":"Experts and households both hold capital, but experts are more productive and can issue equity only if they retain at least a fraction chi of it — a skin-in-the-game constraint. The state x is the experts' wealth share. The model has a crisis region (experts constrained, Psi  1) and a normal region (Psi = 1); the capital price q and the experts' capital share Psi are recovered with a root-find, sweeping x from the lower boundary upward so each step reuses the previous q to build q_x by finite difference. That order-dependent sweep is why the model struct is mutable and carries q_old.","title":"Brunnermeier–Sannikov (2014): a macroeconomic model with a financial sector"},{"category":"section","location":"examples/asset_pricing/brunnermeier_sannikov/#The-model","page":"Brunnermeier–Sannikov: macro-finance","text":"The parameters:\n\nusing EconPDEs, Roots, Plots\n\nBase.@kwdef mutable struct BrunnermeierSannikov\n  # Calibration uses Section 3.6 of the textbook chapter (I think r is typo for ρH)\n  ρE::Float64 = 0.06      # experts' rate of time preference (discount rate)\n  ρH::Float64 = 0.05      # households' rate of time preference (discount rate)\n  γ::Float64 = 2.0        # relative risk aversion\n  ψ::Float64 = 0.5        # elasticity of intertemporal substitution\n  δ::Float64 = 0.05       # depreciation rate of capital\n  σ::Float64 = 0.1        # exogenous (fundamental) volatility of capital\n  κ::Float64 = 10.0       # investment adjustment-cost parameter\n  a::Float64 = 0.11       # experts' productivity (output per unit capital)\n  alow::Float64 = 0.03    # households' productivity (output per unit capital)\n  χlow::Float64 = 0.5     # minimum equity retention (skin-in-the-game floor)\n  q_old::Float64 = 1.0    # previous capital price q (carried across the sweep)\n  qx_old::Float64 = 0.0   # previous q_x (carried across the sweep)\n  Ψ_old::Float64 = 1.0    # previous experts' capital share Ψ (carried across the sweep)\nend","title":"The model"},{"category":"section","location":"examples/asset_pricing/brunnermeier_sannikov/#The-state-space","page":"Brunnermeier–Sannikov: macro-finance","text":"We build the grid and the initial guess first, because they fix the names used everywhere else. The grid is a NamedTuple whose key is the state variable (x, the experts' wealth share); the guess is a NamedTuple whose keys are the unknown functions (pE, pH), holding one starting value at each grid point. These names are what reappear inside the equation below — e.g. pEx_up will be the forward finite difference of pE in x.\n\nOne state x in (0 1) on 200 interior grid points. Because the sweep marches from the lower boundary upward, the grid spacing Δx and its minimum xmin are computed here and later passed into the model.\n\nm = BrunnermeierSannikov()\nxn = 200\nstategrid =  (; x = range(0, 1.0, length = xn+2)[2:(end-1)])\nΔx = step(stategrid[:x])\nxmin = minimum(stategrid[:x])\nyend = (; pE =  9 .* ones(xn), pH =   10 .* ones(xn))","title":"The state space"},{"category":"section","location":"examples/asset_pricing/brunnermeier_sannikov/#The-equation","page":"Brunnermeier–Sannikov: macro-finance","text":"We now write the function encoding the equilibrium conditions. Following the package convention, it takes the current state (a grid point) and u — the local bundle holding each unknown and its finite-difference derivatives there — plus the grid spacing Δx and lower bound xmin, and returns the time derivative of each unknown (pEt, pHt). Inside, a root-find recovers the capital price q and the experts' capital share Psi, distinguishing the crisis region (Psi  1) from the normal region (Psi = 1).\n\nfunction (m::BrunnermeierSannikov)(state::NamedTuple, u::NamedTuple, Δx, xmin)\n  (; ρE, ρH, γ, ψ, δ, σ, κ, a, alow, χlow) = m\n  (; x) = state\n  (; pE, pEx_up, pEx_down, pExx, pH, pHx_up, pHx_down, pHxx) = u\n  pE = max(pE, 1e-3)\n  pH = max(pH, 1e-3)\n  χ = max(χlow, x)\n  pEx, pHx = pEx_up, pHx_up\n  iter = 0\n  @label start\n  q_old = m.q_old\n  qx_old = m.qx_old\n  Ψ_old = m.Ψ_old\n\n  # First, solve for q, qx, and qxx\n  if abs(x - xmin) < 1e-9\n    # initial condition at lower boundery of x grid (point outside the grid technically)\n    # We must have that expert consumption approaches zero as xt approaches zero (linked to transversality condition)\n    Ψ_old = 0.0\n    q_old = (alow + 1 / κ) / (1 / pH + 1 / κ)\n    qx_old = 0.0\n  end\n  if Ψ_old < 1.0\n    # crisis region.\n    # in this case, we do not have Ψ = 1.0 but we have\n    # (a - alow) / q = (κE - κH) * (σ + σq)\n    # note that\n    out = find_zero(Ψ_old) do Ψ\n        local q = (Ψ * a + (1 - Ψ) * alow + 1 / κ) / (x / pE + (1 - x) / pH + 1 / κ)\n        local qx = (q - q_old) / Δx\n        local αE = χ * Ψ / x\n        local σx = x * (αE - 1) * σ / (1 - x * (αE - 1) * qx / q)\n        local σpE = pEx / pE * σx\n        local σpH = pHx / pH * σx\n        local σq = qx / q * σx\n        local σE = αE * (σ + σq)\n        local αH = (1 - αE * x) / (1 - x)\n        local σH = αH * (σ + σq)\n        local κE = γ * (σE - (1 / γ - 1) / (ψ - 1) * σpE)\n        local κH = γ * (σH - (1 / γ - 1) / (ψ - 1) * σpH)\n        return (a - alow) / q -  χ * (κE - κH) * (σ + σq)\n      end\n    Ψ = out[1]\n    q = (Ψ * a + (1 - Ψ) * alow + 1 / κ) / (x / pE + (1 - x) / pH + 1 / κ)\n    qx = (q - q_old) / Δx\n    if Ψ > 1\n      Ψ_old = 1.0\n    end\n  end\n  if Ψ_old ≥ 1.0\n    # normal region.\n    Ψ = 1.0\n    q = (a + 1 / κ) / ((x / pE + (1 - x) / pH) + 1 / κ)\n    qx = - (a + 1 / κ) / ((x / pE + (1 - x) / pH) + 1 / κ)^2 * (1 / pE - 1 / pH - x / pE^2 * pEx - (1-x) / pH^2 * pHx)\n  end\n  qxx = (qx - qx_old) / Δx\n  m.q_old = q\n  m.qx_old = qx\n  m.Ψ_old = Ψ\n\n\n  # having solved for q, do the time step\n  q = max(q, sqrt(eps()))\n  ι = (q - 1) / κ\n  Φ = log(q) / κ\n  αE = χ * Ψ / x\n  σx = x * (αE - 1) * σ / (1 - x * (αE - 1) * qx / q)\n  σpE = pEx / pE * σx\n  σpH = pHx / pH * σx\n  σq = qx / q * σx\n  σE = αE * (σ + σq)\n  αH = (1 - αE * x) / (1 - x)\n  σH = αH * (σ + σq)\n  κE = γ * (σE - (1 / γ - 1) / (ψ - 1) * σpE)\n  κH = γ * (σH - (1 / γ - 1) / (ψ - 1) * σpH)\n  μx = x * (1 - x) * (κE * σE - κH * σH + 1 / pH - 1 / pE - (σE - σH) * (σ + σq))\n  μx2 = x * ((a - ι) / q - 1 / pE) + σx * (κE - σ - σq) + x * (1 - χ) * (κE - κH) * (σ + σq)\n  if (iter == 0) && (μx < 0)\n    # upwinding\n    iter += 1\n    pEx, pHx = pEx_down, pHx_down\n    @goto start\n  end\n  μpE = pEx / pE * μx + 0.5 * pExx / pE * σx^2\n  μpH = pHx / pH * μx + 0.5 * pHxx / pH * σx^2\n  μq = qx / q * μx + 0.5 * qxx / q * σx^2\n\n  r = clamp((a - ι) / q + Φ - δ + μq + σ * σq - (χ * κE + (1 - χ) * κH) * (σ + σq), -3.0, 3.0)\n  # Market Pricing\n  pEt = -  pE * (1 / pE - ψ * ρE + (ψ - 1) * (r + κE * σE) + μpE - (ψ - 1) * γ / 2 * σE^2 + (2 - ψ - γ) / (2 * (ψ - 1)) * σpE^2 + (1 - γ) * σpE * σE)\n  pHt = -  pH * (1 / pH - ψ * ρH + (ψ - 1) * (r + κH * σH) + μpH - (ψ - 1) * γ / 2 * σH^2 + (2 - ψ - γ) / (2 * (ψ - 1)) * σpH^2 + (1 - γ) * σpH * σH)\n  d = Ψ * a + (1 - Ψ) * alow - ι\n  μR = r + (χ * κE + (1 - χ) * κH) * (σ + σq)\n  (; pEt, pHt), (; x, r, μx, σx, Ψ, κ, κE, κH, σE, σH, σq, pEt, pHt, q, qx, qxx, μq, μx2, Φ, χ, d, μR, pExx, pHxx)\nend\n\nThe sweep is order-dependent — it marches from the lower boundary xmin upward, each step reusing the previous q carried in the mutable model — so Δx and xmin are passed into the model and finite-difference autodiff is used:\n\n@time result = pdesolve((state, u) -> m(state, u, Δx, xmin), stategrid, yend; autodiff = :finite)","title":"The equation"},{"category":"section","location":"examples/asset_pricing/brunnermeier_sannikov/#The-solution","page":"Brunnermeier–Sannikov: macro-finance","text":"We saved the capital price q. It is depressed when experts are poorly capitalized (low x) — the crisis region — and tends to rise with the experts' wealth share as the retention constraint slackens.\n\nxs = stategrid[:x]\nplot(xs, result.optional[:q]; xlabel = \"experts' wealth share x\", ylabel = \"capital price q\", legend = false)\n\n\n\nThis page was generated using Literate.jl.","title":"The solution"},{"category":"section","location":"api/#API-reference","page":"API reference","text":"","title":"API reference"},{"category":"function","location":"api/#EconPDEs.pdesolve","page":"API reference","text":"pdesolve(f, grid, yend [, τs]; kwargs...)\n\nSolve a system of nonlinear ODEs/PDEs — typically Hamilton–Jacobi–Bellman equations — by finite differences. Handles an arbitrary number of coupled unknown functions on a state grid of one, two, or three state variables.\n\nPositional arguments\n\nf: the local equation, a function (state, u) -> out (or (state, u, t) -> out for a time-dependent equation), where\nstate is a NamedTuple with the current grid point (one entry per state variable),\nu is a NamedTuple with each unknown function and its finite-difference derivatives at that point (e.g. v, vk_up, vk_down, vkk, …),\nout is a NamedTuple with one time derivative per unknown (e.g. (; vt)).\nf may also return a second NamedTuple of extra objects to save on the grid; these are returned in result.optional.\ngrid: a NamedTuple (or OrderedDict) mapping each state variable name to an AbstractVector (its grid), e.g. (; k = range(...)).\nyend: a NamedTuple (or OrderedDict) mapping each unknown name to an array of initial values with the same shape as the grid. For a time-dependent problem, it is the terminal value at τs[end].\nτs (optional): an increasing time grid. The equation is then solved backward from τs[end], and result.zero[i] is the solution at time τs[i].\n\nKeyword arguments\n\nbc: boundary derivatives, a NamedTuple (or OrderedDict) mapping each Symbol(unknown, state) to a (lower, upper) tuple. Defaults to reflecting boundaries (zero outward first derivative).\nis_algebraic: a NamedTuple (or OrderedDict) of Bools marking equations that are algebraic (no time derivative) rather than PDEs. Defaults to all false.\ny̲, ȳ: lower/upper bounds for HJB variational inequalities (optimal stopping), solved as a mixed complementarity problem. Default to -Inf/Inf (unbounded).\nmethod: :newton (default) or :trust_region.\nmaxdist: convergence tolerance on the residual. Defaults to sqrt(eps()).\niterations: maximum number of pseudo-transient iterations. Defaults to 100.\nΔ: initial pseudo-transient time step. Defaults to 1.0; pass Δ = Inf to solve in a single Newton step (no continuation).\nautodiff: :forward (default), :finite, or :central.\nverbose: print convergence progress. Defaults to true.\ncheck_monotonicity: if true, warn when the assembled residual Jacobian has same-variable spatial off-diagonal entries with the wrong monotonicity sign. Defaults to false.\nmonotonicity_tol: tolerance for the monotonicity check. Defaults to 1e-6.\nmonotonicity_max_warnings: maximum number of monotonicity warnings to print. Defaults to 5.\n\nReturns an EconPDEResult with fields zero (the solved unknowns), residual_norm, and optional (the saved objects, together with the solved unknowns).\n\n\n\n\n\n","title":"EconPDEs.pdesolve"},{"category":"function","location":"api/#EconPDEs.finiteschemesolve","page":"API reference","text":"finiteschemesolve(F!, y0; kwargs...)\n\nLower-level nonlinear solver behind pdesolve. Finds y such that F!(ydot, y) writes the residual into ydot and returns zero, using Newton's method with pseudo-transient continuation from the initial guess y0.\n\npdesolve assembles F! (the finite-difference residual of the PDE) and calls this function, so most users should call pdesolve instead. It accepts the same solver keyword arguments — Δ, iterations, method, maxdist, autodiff, y̲, ȳ, … — and returns the tuple (y, residual_norm).\n\n\n\n\n\n","title":"EconPDEs.finiteschemesolve"},{"category":"type","location":"api/#EconPDEs.EconPDEResult","page":"API reference","text":"EconPDEResult\n\nThe value returned by pdesolve, with fields:\n\nzero: the solved unknown functions (an OrderedDict, or a vector of them for a time-dependent problem).\nresidual_norm: the norm of the residual at the solution.\noptional: the extra objects saved by the PDE function, together with the solved unknowns.\n\n\n\n\n\n","title":"EconPDEs.EconPDEResult"},{"category":"section","location":"examples/corporate_finance/leland/#Leland-(1994):-optimal-default","page":"Leland: optimal default","text":"Leland's model of a levered firm is a classic optimal-stopping problem. Cash flow (EBIT) delta follows a geometric Brownian motion ddelta = mudeltadt + sigmadeltadW. The firm has issued perpetual debt with coupon C and faces a tax rate tau; equity holders receive the after-tax flow delta-(1-tau)C but may default (walk away) whenever they choose. Equity value E(delta) is thus the value of the option to keep the firm alive, and solves the HJB variational inequality\n\nminBigl r E - bigl(delta-(1-tau)Cbigr) - mudeltaE(delta) - tfrac12sigma^2delta^2 E(delta) E(delta)Bigr = 0\n\nwith default payoff 0 (limited liability). This delivers the value-matching E(delta^*)=0 and smooth-pasting E(delta^*)=0 conditions at the endogenous default threshold delta^*.","title":"Leland (1994): optimal default"},{"category":"section","location":"examples/corporate_finance/leland/#The-model","page":"Leland: optimal default","text":"The parameters live in a struct:\n\nusing EconPDEs, Plots\n\nBase.@kwdef struct LelandModel\n    r::Float64 = 0.02    # risk-free rate\n    μ::Float64 = 0.0     # drift of cash flow (EBIT growth)\n    σ::Float64 = 0.1     # volatility of cash flow\n    C::Float64 = 0.05    # coupon rate\n    τ::Float64 = 0.2     # tax rate\nend","title":"The model"},{"category":"section","location":"examples/corporate_finance/leland/#The-state-space","page":"Leland: optimal default","text":"We build the grid and the initial guess first, because they fix the names used everywhere else. The grid is a NamedTuple whose key is the state variable (δ, the cash flow); the guess is a NamedTuple whose key is the unknown function (E, equity value), holding one starting value at each grid point. These names reappear inside the equation below — e.g. Eδ_up will be the forward finite difference of E in δ. We start the guess from the value of the cash-flow claim net of the after-tax coupon, floored at zero.\n\nm = LelandModel()\nstategrid = (; δ = range(0, 0.2, step = 0.005))\nyend = (; E = max.(stategrid[:δ] ./ (m.r - m.μ) .- (1 .- m.τ) .* m.C ./ m.r, 0.0))","title":"The state space"},{"category":"section","location":"examples/corporate_finance/leland/#The-equation","page":"Leland: optimal default","text":"We now write the function encoding the HJB equation. Following the package convention, it takes the current state (a grid point) and u (each unknown together with its finite-difference derivatives there) and returns the time derivative of each unknown.\n\nfunction (m::LelandModel)(state::NamedTuple, u::NamedTuple)\n    (; r, μ, σ, C, τ) = m\n    (; δ) = state\n    (; E, Eδ_up, Eδ_down, Eδδ) = u\n    μδ, σδ = μ * δ, σ * δ\n    Eδ = (μδ >= 0) ? Eδ_up : Eδ_down\n    f = δ - (1 - τ) * C\n    Et = -(f + Eδ * μδ + 0.5 * Eδδ * σδ^2 - r * E)\n    return (; Et)\nend\n\nThe stopping (default) region is imposed with the lower bound y̲ = 0: equity can never be worth less than zero. We also pin the slope at the top boundary to that of a debt-free claim, 1(r-mu). With these in hand, pdesolve solves the variational inequality:\n\ny̲ = zeros(length(stategrid[:δ]))\nbc = (; Eδ = (0.0, 1 / (m.r - m.μ)))\nresult = pdesolve(m, stategrid, yend; y̲ = y̲, bc = bc, reformulation = :smooth)","title":"The equation"},{"category":"section","location":"examples/corporate_finance/leland/#The-solution","page":"Leland: optimal default","text":"Equity is worthless below an endogenous default threshold delta^* (red line) and rises smoothly above it. Shareholders keep servicing the debt only while cash flows are high enough to justify the coupon; below delta^* they optimally default.\n\nδs = stategrid[:δ]\nE = result.zero[:E]\nδstar = δs[findfirst(>(1e-8), E)]\nplot(δs, E; xlabel = \"cash flow δ\", ylabel = \"equity value E(δ)\", legend = false)\nvline!([δstar]; color = :red, linestyle = :dot)\n\n\n\nThis page was generated using Literate.jl.","title":"The solution"},{"category":"section","location":"examples/asset_pricing/he_krishnamurthy/#He–Krishnamurthy-(2013):-intermediary-asset-pricing","page":"He–Krishnamurthy: intermediaries","text":"Households cannot hold the risky asset directly — they invest only through specialist intermediaries, and their equity stake in an intermediary is capped at a multiple m of the intermediary's net worth. The single state x is the specialists' wealth share. When x falls the sector's risk-bearing capacity shrinks, the constraint binds, and the price of risk rises. The unknown is the specialists' wealth–consumption ratio p_S; goods-market clearing ties the aggregate price–consumption ratio p to it.","title":"He–Krishnamurthy (2013): intermediary asset pricing"},{"category":"section","location":"examples/asset_pricing/he_krishnamurthy/#The-model","page":"He–Krishnamurthy: intermediaries","text":"The parameters:\n\nusing EconPDEs, Plots\n\nBase.@kwdef mutable struct HeKrishnamurthy\n  # See Table 2—Parameters and Targets\n  m::Float64 = 4  # intermediation multiplier\n  λ::Float64 = 0.6 # Debt/assets of intermediary sector\n  g::Float64 = 0.02 # dividend growth\n  σ::Float64 = 0.09 # dividend volatility\n  ρ::Float64 = 0.04 # time discount rate\n  γ::Float64 = 2.0 # risk aversion specialist\n  l::Float64 = 1.84 # labor income ratio\nend","title":"The model"},{"category":"section","location":"examples/asset_pricing/he_krishnamurthy/#The-state-space","page":"He–Krishnamurthy: intermediaries","text":"We build the grid and the initial guess first, because they fix the names used everywhere else. The grid is a NamedTuple whose key is the state variable (x, the specialists' wealth share); the guess is a NamedTuple whose key is the unknown function (pS), holding one starting value at each grid point. These names are what reappear inside the equation below — e.g. pSx_up will be the forward finite difference of pS in x.\n\nOne state x in (0 1) on a grid stretched toward the lower boundary where the constraint bites. The single unknown p_S is initialized flat at one.\n\nm = HeKrishnamurthy()\nxn = 100\nstategrid =  (; x = range(0, 1, length = xn+2)[2:(end-1)].^1.5)\nyend = (; pS = ones(length(stategrid[:x])))","title":"The state space"},{"category":"section","location":"examples/asset_pricing/he_krishnamurthy/#The-equation","page":"He–Krishnamurthy: intermediaries","text":"We now write the function encoding the equilibrium conditions. Following the package convention, it takes the current state (a grid point) and u — the local bundle holding the unknown and its finite-difference derivatives there (pS, pSx_up, pSx_down, pSxx) — and returns the time derivative pSt.\n\nGoods-market clearing expresses the aggregate price–consumption ratio p (and its derivatives) in terms of p_S. The state volatility sigma_x feeds back through prices, so the upwind direction is chosen from the drift mu_x: forward pSx_up unless the drift turns negative, then backward pSx_down.\n\nfunction (m::HeKrishnamurthy)(state::NamedTuple, u::NamedTuple)\n  (; m, λ, g, σ, ρ, γ, l) = m\n  (; pS, pSx_up, pSx_down, pSxx) = u\n  (; x) = state\n  iter = 0\n  pSx = pSx_up\n  @label start\n  # market clearing for goods gives x / pS + (1 - x) * ρ = (1 + l) / p\n  # This allows to obtain p (and its derivatives) in terms of pS (and its derivatives)\n  p = (1 + l) / (x / pS + (1 - x) * ρ)\n  px = - (1 + l) / (x / pS + (1 - x) * ρ)^2 * (1 / pS - ρ - x / pS^2 * pSx)\n  pxx = - (1 + l) / (x / pS + (1 - x) * ρ)^2 * (- 1 / pS^2 * pSx - 1 / pS^2 * pSx - x / pS^2 * pSxx + 2 * x / pS^3 * pSx^2) + 2 * (1 + l) / (x / pS + (1 - x) * ρ)^3 * (1 / pS - ρ - x / pS^2 * pSx)^2\n  @assert x / pS + (1 - x) * ρ ≈ (1 + l) / p\n\n\n  αI = 1 / (1 - λ * (1 - x))\n  if x * (1 + m) * αI < 1\n    αI = 1 / (x * (1 + m))\n  end\n  # we have the usual feeback loop between asset prices and wealth\n  # σx = x * (αI - 1) * (σ + σp)\n  σx = x * (αI - 1) * σ / (1 -  x * (αI - 1) * px / p)\n  σpS = pSx / pS * σx\n  σp = px / p * σx\n  @assert σx ≈ x * (αI - 1) * (σ + σp)\n  σR = σ + σp\n  κ = γ * (αI * σR - σpS)\n  μx = x * (1 - x) * ((αI - (1 - αI * x) / (1-x)) * κ * σR - 1 / pS + ρ - (αI - (1 - αI * x) / (1 - x)) * σR^2 - l / ((1 - x) * p))\n  if (iter == 0) && (μx < 0)\n    iter += 1\n    pSx = pSx_down\n    @goto start\n  end\n  μpS = pSx / pS * μx + 0.5 * pSxx / pS * σx^2\n  μp = px / p * μx + 0.5 * pxx / p * σx^2\n  r = 1 / p + g + μp + σ * σp - κ * σR\n  σCS = κ / γ\n  μCS = (r - ρ) / γ + (1 + 1 / γ) / (2 * γ) * κ^2\n  pSt = - (1 / pS + μCS + μpS + σCS * σpS - r - κ * (σCS + σpS))\n  μR = r + κ * σR\n  σI = αI * σR\n  (; pSt), (; pS, p, r, κ, σp, μR, σR, αI, μx, σx, x, px, pxx, σI)\nend\n\nWith the equation, grid, and guess in hand, pdesolve solves the stationary system:\n\nresult = pdesolve(m, stategrid, yend)","title":"The equation"},{"category":"section","location":"examples/asset_pricing/he_krishnamurthy/#The-solution","page":"He–Krishnamurthy: intermediaries","text":"We saved the price of risk kappa on the grid. It tends to be elevated when specialists are poorly capitalized (low x) — exactly the region where the intermediation constraint binds and the sector can absorb little risk.\n\nxs = stategrid[:x]\nplot(xs, result.optional[:κ]; xlabel = \"specialists' wealth share x\", ylabel = \"price of risk κ\", legend = false)\n\n\n\nThis page was generated using Literate.jl.","title":"The solution"},{"category":"section","location":"examples/consumption_saving/consumption_saving_two_assets/#Achdou–Han–Lasry–Lions–Moll:-portfolio-choice-with-two-assets","page":"Consumption–saving: two assets","text":"This extends the Achdou–Han–Lasry–Lions–Moll consumption–saving problem to a portfolio choice between a riskless asset (return r) and a risky asset (return mu_R, volatility sigma_R). A household with total wealth a chooses consumption c and how much of its wealth k to hold in the risky asset, subject to no short-selling and no leverage (0 le k le a - a_min). Labor income y follows the same mean-reverting diffusion. Wealth evolves as da = bigl(y + r a + (mu_R - r) k - cbigr)dt + sigma_R k dW and the value function v(y a) solves\n\nrho v = max_ck fracc^1-gamma1-gamma\n  + partial_a vbigl(y + r a + (mu_R - r) k - cbigr)\n  + tfrac12 sigma_R^2 k^2 partial_aa v\n  + partial_y vkappa_y(bar y - y)\n  + tfrac12 sigma_y^2 partial_yy v\n\nThe first-order conditions are c = (partial_a v)^-1gamma for consumption and the Merton rule k = -tfracmu_R - rsigma_R^2partial_a v  partial_aa v for the risky holding, clamped to 0 a - a_min.","title":"Achdou–Han–Lasry–Lions–Moll: portfolio choice with two assets"},{"category":"section","location":"examples/consumption_saving/consumption_saving_two_assets/#The-model","page":"Consumption–saving: two assets","text":"The parameters live in a struct:\n\nusing EconPDEs, Distributions, Plots\n\nBase.@kwdef struct AchdouHanLasryLionsMoll_DiffusionTwoAssetsModel\n    κy::Float64 = 0.1        # mean-reversion speed of labor income\n    ybar::Float64 = 1.0      # long-run mean of labor income\n    σy::Float64 = 0.07       # volatility of labor income\n\n    r::Float64 = 0.03        # risk-free rate\n    μR::Float64 = 0.04       # expected return on the risky asset\n    σR::Float64 = 0.1        # volatility of the risky asset\n\n    ρ::Float64 = 0.05        # discount rate\n    γ::Float64 = 2.0         # relative risk aversion\n\n    amin::Float64 = 0.0      # borrowing limit (minimum wealth)\n    amax::Float64 = 1000.0   # maximum wealth (grid upper bound)\nend","title":"The model"},{"category":"section","location":"examples/consumption_saving/consumption_saving_two_assets/#The-state-space","page":"Consumption–saving: two assets","text":"We build the grid and the initial guess first, because they fix the names used everywhere else. The grid is a NamedTuple whose keys are the two state variables (y and a); the guess is a NamedTuple whose key is the unknown function (v), holding one starting value at each grid point. These names are what reappear inside the equation below — e.g. va_up will be the forward finite difference of v in a.\n\nIncome spans the bulk of its ergodic (Gamma) distribution and wealth runs from the borrowing limit up to a_max. As before, the initial guess is the value of consuming the annuity value of total wealth a + yr forever.\n\nm = AchdouHanLasryLionsMoll_DiffusionTwoAssetsModel()\ndistribution = Gamma(2 * m.κy * m.ybar / m.σy^2, m.σy^2 / (2 * m.κy))\nys = range(quantile(distribution, 0.001), quantile(distribution, 0.999), length = 5)\nas = range(m.amin, m.amax, length = 100)\nstategrid = (; y = ys, a = as)\nyend = (; v = [(m.ρ / m.γ + (1 - 1 / m.γ) * m.r)^(-m.γ) * (a + y / m.r)^(1 - m.γ) / (1 - m.γ) for y in stategrid[:y], a in stategrid[:a]])","title":"The state space"},{"category":"section","location":"examples/consumption_saving/consumption_saving_two_assets/#The-equation","page":"Consumption–saving: two assets","text":"We now write the function encoding the HJB equation. Following the package convention, it takes the current state (a grid point) and u (each unknown together with its finite-difference derivatives there) and returns the time derivative of each unknown.\n\nAs in the one-asset model, income is upwinded on mu_y and the endogenous asset drift picks the forward/backward derivative that keeps its sign consistent. At each candidate we also solve the Merton portfolio rule for k. The boundary branches impose the borrowing constraint at a_min and a homothetic tail at a_max. We save consumption c, the risky holding k, and the saving rate mu_a on the grid.\n\nfunction (m::AchdouHanLasryLionsMoll_DiffusionTwoAssetsModel)(state::NamedTuple, u::NamedTuple)\n    (; κy, σy, ybar, r, μR, σR, ρ, γ, amin, amax) = m\n    (; y, a) = state\n    (; v, vy_up, vy_down, va_up, va_down, vyy, vya, vaa) = u\n    μy = κy * (ybar - y)\n    # Newton can try negative marginal values, so cap implied consumption instead of flooring derivatives.\n    cmax = 100.0 * (y + r * max(a, 0.0))\n\n    # upwinding for income direction (easy because exogenous income drift)\n    vy = (μy >= 0) ? vy_up : vy_down\n\n    # upwinding for asset direction (harder because endogeneous asset drift)\n    c_up = va_up > 0 ? min(va_up^(-1 / γ), cmax) : cmax\n    k_up = (μR - r) / σR^2 * (-va_up / vaa)\n    k_up = clamp(k_up, 0.0, a - amin)\n    μa_up = y + r * a + (μR - r) * k_up - c_up\n    if μa_up >= 0.0\n        va = va_up\n        c = c_up\n        k = k_up\n        μa = μa_up\n    else\n        c_down = va_down > 0 ? min(va_down^(-1 / γ), cmax) : cmax\n        k_down = (μR - r) / σR^2 * (-va_down / vaa)\n        k_down = clamp(k_down, 0.0, a - amin)\n        μa_down = y + r * a + (μR - r) * k_down - c_down\n        if (μa_down <= 0.0) && (a >= amin)\n            va = va_down\n            c = c_down\n            k = k_down\n            μa = μa_down\n        else\n            # If the two candidates straddle zero OR drift is negative at minimum asset threshold\n            # (i.e. borrowing constraint), then, we must have drift μa = 0.\n            c = y + r * a\n            for _ in 1:30\n                va = c^(-γ)\n                k = (μR - r) / σR^2 * (-va / vaa)\n                k = clamp(k, 0.0, a - amin)\n                c = y + r * a + (μR - r) * k\n            end\n            va = c^(-γ)\n            k = (μR - r) / σR^2 * (-va / vaa)\n            k = clamp(k, 0.0, a - amin)\n            c = y + r * a + (μR - r) * k\n            μa = 0.0\n        end\n    end\n    σa = k * σR\n\n    # There is no second derivative at 0 so just specify first order derivative\n    if (a ≈ amin) && (μa <= 0.0)\n        va = (y + r * a)^(-γ)\n        k = 0.0\n        c = y + r * a\n        μa = 0.0\n    end\n    # this branch is unnecessary when individuals dissave at the top (default)\n    if (a ≈ amax) && (μa >= 0.0)\n        va = ((ρ - r) / γ + r - (1-γ) / (2 * γ) * (μR - r)^2 / (γ * σR^2))^(-γ) * a^(-γ)\n    end\n    # Since first derivative is upwinded, I can directly impose value of second derivative\n    if (a ≈ amax)\n        vaa = - m.γ * va / a\n    end\n    c = va > 0 ? min(va^(-1 / γ), cmax) : cmax\n    k = (μR - r) / σR^2 * (- va / vaa)\n    k = clamp(k, 0.0, a - amin)\n    μa = y + r * a + (μR - r) * k - c\n    σa = k * σR\n    vt = - (c^(1 - γ) / (1 - γ) + va * μa + 0.5 * vaa * σa^2 + vy * μy + 0.5 * vyy * σy^2 - ρ * v)\n    return (; vt), (; v, c, k, va, vaa, vy, y, a, μa)\nend\n\nWith the equation, grid, and guess in hand, pdesolve solves the stationary system:\n\nresult = pdesolve(m, stategrid, yend)","title":"The equation"},{"category":"section","location":"examples/consumption_saving/consumption_saving_two_assets/#The-solution","page":"Consumption–saving: two assets","text":"We plot the two policies against wealth for three income levels (low, median, high).\n\nas = stategrid[:a]\nys = stategrid[:y]\nidx = 1:div(length(as), 3)          # left third of the wealth grid, where the curvature is\niys = round.(Int, range(1, length(ys), length = 3))\n\np1 = plot(xlabel = \"wealth a\", ylabel = \"consumption c\")\nfor iy in iys\n    plot!(p1, as[idx], result.optional[:c][iy, idx], label = \"y = $(round(ys[iy], digits = 2))\")\nend\np2 = plot(xlabel = \"wealth a\", ylabel = \"saving μa\")\nfor iy in iys\n    plot!(p2, as[idx], result.optional[:μa][iy, idx], label = \"y = $(round(ys[iy], digits = 2))\")\nend\nhline!(p2, [0.0]; color = :gray, linestyle = :dash, label = \"\")\nplot(p1, p2; layout = (1, 2), size = (800, 300))\n\nConsumption rises with both wealth and income (left); the saving rate (right) is highest for the wealth-poor and turns negative as households approach their target wealth. On the portfolio side (not shown), away from the constraint the household holds the Merton fraction (mu_R - r)(gammasigma_R^2) of wealth in the risky asset, and tilts toward the safe asset near the constraint, where it cannot lever and labor-income risk crowds out financial risk-taking.\n\n\n\nThis page was generated using Literate.jl.","title":"The solution"},{"category":"section","location":"examples/asset_pricing/campbell_cochrane/#Campbell–Cochrane-(1999):-habit-formation","page":"Campbell–Cochrane: habit","text":"Campbell and Cochrane explain the equity premium with an external habit. The single state is the log surplus consumption ratio s = logfracC-XC — how far consumption sits above the habit X — which mean-reverts around bar s. The price–consumption ratio p(s) solves a linear second-order ODE (a special case of the HJB machinery):\n\n0 = 1 + bigl(mu + mu_p(s) + sigma_p(s)sigma - r(s) - kappa(s)(sigma+sigma_p(s))bigr) p(s)\n\nwhere the drift mu_p, volatility sigma_p, price of risk kappa, and riskless rate r are all functions of s through the habit.","title":"Campbell–Cochrane (1999): habit formation"},{"category":"section","location":"examples/asset_pricing/campbell_cochrane/#The-model","page":"Campbell–Cochrane: habit","text":"The parameters live in a struct:\n\nusing EconPDEs, Plots\n\nBase.@kwdef struct CampbellCochraneModel\n    μ::Float64 = 0.0189    # mean consumption growth\n    σ::Float64 = 0.015     # volatility of consumption growth\n    γ::Float64 = 2.0       # utility curvature (relative risk aversion)\n    ρ::Float64 = 0.116     # discount rate\n    κs::Float64 = 0.138    # mean-reversion speed of log surplus consumption ratio\n    b::Float64 = 0.0       # sensitivity of riskless rate to surplus ratio (b=0 ⇒ constant r)\nend","title":"The model"},{"category":"section","location":"examples/asset_pricing/campbell_cochrane/#The-state-space","page":"Campbell–Cochrane: habit","text":"We build the grid and the initial guess first, because they fix the names used everywhere else. The grid is a NamedTuple whose key is the state variable (s, the log surplus consumption ratio); the guess is a NamedTuple whose key is the unknown function (p, the price–consumption ratio). These names reappear inside the equation below — e.g. ps_up will be the forward finite difference of p in s. The grid concentrates points near the reflecting upper bound s_max, where the surplus ratio is highest, and stretches far into the left tail.\n\nfunction initialize_stategrid(m::CampbellCochraneModel; sn = 1000)\n    (; μ, σ, γ, ρ, κs, b) = m\n    Sbar = σ * sqrt(γ / (κs - b / γ))\n    sbar = log(Sbar)\n    smax = sbar + 0.5 * (1 - Sbar^2)\n    shigh = log.(range(0.0, exp(smax), length = div(sn, 10)))\n    slow = range(-300.0, shigh[2], length = sn - div(sn, 10))\n    (; s = vcat(slow[1:(end-1)], shigh[2:end]))\nend\n\nm = CampbellCochraneModel()\nstategrid = initialize_stategrid(m)\nyend = (; p = ones(length(stategrid[:s])))","title":"The state space"},{"category":"section","location":"examples/asset_pricing/campbell_cochrane/#The-equation","page":"Campbell–Cochrane: habit","text":"We now write the function encoding the HJB equation. Following the package convention, it takes the current state (a grid point) and u (each unknown together with its finite-difference derivatives there) and returns the time derivative of each unknown.\n\nfunction (m::CampbellCochraneModel)(state::NamedTuple, u::NamedTuple)\n    (; μ, σ, γ, ρ, κs, b) = m\n    (; s) = state\n    (; p, ps_up, ps_down, pss) = u\n    Sbar = σ * sqrt(γ / (κs - b / γ))\n    sbar = log(Sbar)\n    λ = 1 / Sbar * sqrt(1 - 2 * (s - sbar)) - 1\n    μs = -κs * (s - sbar)\n    σs = λ * σ\n    # pricing uses the risk-adjusted drift of surplus consumption, not the physical one\n    μs_effective = μs + σs * (σ - γ * (σ + σs))\n    ps = (μs_effective >= 0) ? ps_up : ps_down\n    σp = ps / p * σs\n    μp = ps / p * μs + 0.5 * pss / p * σs^2\n    κ = γ * (σ + σs)\n    r = ρ + γ * μ - (γ * κs - b) / 2 + b * (sbar - s)\n    pt = -p * (1 / p + μ + μp + σp * σ - r - κ * (σ + σp))\n    return (; pt)\nend\n\nWith the equation, grid, and guess in hand, pdesolve solves the stationary system:\n\nresult = pdesolve(m, stategrid, yend)","title":"The equation"},{"category":"section","location":"examples/asset_pricing/campbell_cochrane/#The-solution","page":"Campbell–Cochrane: habit","text":"Plotted against the surplus consumption ratio S = e^s, the price–consumption ratio rises steeply in good times: as consumption pulls away from the habit, effective risk aversion falls, discount rates drop, and valuations climb. This state-dependent risk aversion is what lets the model generate a large, countercyclical equity premium.\n\nSbar = m.σ * sqrt(m.γ / (m.κs - m.b / m.γ))\nsbar = log(Sbar)\nss = stategrid[:s]\nmask = ss .>= (sbar - 4)\nplot(exp.(ss[mask]), result.zero[:p][mask]; xlabel = \"surplus consumption ratio S = exp(s)\", ylabel = \"price–consumption ratio p\", legend = false)\n\n\n\nThis page was generated using Literate.jl.","title":"The solution"},{"category":"section","location":"examples/corporate_finance/bolton_chen_wang/#Bolton–Chen–Wang-(2009):-investment-with-costly-external-finance","page":"Bolton–Chen–Wang: financing constraint","text":"Bolton, Chen, and Wang build a unified theory of investment, financing, and cash management. A firm faces costly external finance, so it hoards cash as a buffer against shocks. The single state is the cash–capital ratio w, and the unknown is firm value per unit of capital v(w) — a generalized Tobin's q. It solves the HJB equation\n\nrv(w) = max_i(i-delta)bigl(v - wvbigr) + bigl((r-lambda)w + A - i - tfractheta2i^2bigr)v + tfracsigma^22v\n\nwith optimal investment i = tfrac1thetaleft(tfracvv - w - 1right). Two endogenous boundaries close the model: a payout boundary, where the marginal value of cash v falls to one, and a refinancing boundary, where the firm issues equity at marginal cost gamma and fixed cost phi.","title":"Bolton–Chen–Wang (2009): investment with costly external finance"},{"category":"section","location":"examples/corporate_finance/bolton_chen_wang/#The-model","page":"Bolton–Chen–Wang: financing constraint","text":"The parameters:\n\nusing EconPDEs, NLsolve, Plots\n\nBase.@kwdef struct BoltonChenWangModel\n  r::Float64 = 0.06       # risk-free rate\n  δ::Float64 = 0.10       # depreciation rate of capital\n  A::Float64 = 0.18       # productivity (expected profitability per unit capital)\n  σ::Float64 = 0.09       # cash-flow (capital) volatility\n  θ::Float64 = 1.5        # investment adjustment-cost parameter\n  λ::Float64 = 0.01       # carrying cost of holding cash\n  l::Float64 = 0.9        # liquidation value of capital\n  γ::Float64 = 0.06       # marginal (proportional) cost of external finance\n  ϕ::Float64 = 0.01       # fixed cost of external finance\nend","title":"The model"},{"category":"section","location":"examples/corporate_finance/bolton_chen_wang/#The-state-space","page":"Bolton–Chen–Wang: financing constraint","text":"We build the grid and the initial guess first, because they fix the names used everywhere else. The grid is a NamedTuple whose key is the state variable (w, the cash–capital ratio); the guess is a NamedTuple whose key is the unknown function (v), holding one starting value at each grid point (firm value is initialized to the cash ratio w itself). These names are what reappear inside the equation below — e.g. vw_up will be the forward finite difference of v in w.\n\nm = BoltonChenWangModel()\nstategrid = (; w = range(0.0, 0.3, length = 100))\nyend = (; v = stategrid[:w])","title":"The state space"},{"category":"section","location":"examples/corporate_finance/bolton_chen_wang/#The-equation","page":"Bolton–Chen–Wang: financing constraint","text":"We now write the function encoding the HJB equation. Following the package convention, it takes the current state (a grid point) and u — the local bundle holding the unknown and its finite-difference derivatives there (v, vw_up, vw_down, vww) — and returns the time derivative vt.\n\nThe cash drift mu_w can point either way, so the marginal value of cash is upwinded: forward (vw_up) where the drift is positive, backward (vw_down) where it is negative.\n\nfunction (m::BoltonChenWangModel)(state::NamedTuple, u::NamedTuple)\n  (; r, δ, A, σ, θ, λ, l, γ, ϕ) = m\n  (; w) = state\n  (; v, vw_up, vw_down, vww) = u\n  vw = vw_up\n  iter = 0\n  @label start\n  i = 1 / θ * (v / vw - w - 1)\n  μw = (r - λ) * w + A - i - θ * i^2 / 2 - (i - δ) * w\n  if (iter == 0) && (μw <= 0)\n    iter += 1\n    vw = vw_down\n    @goto start\n  end\n  vt = - ((i - δ) * (v - vw * w) + ((r - λ) * w + A - i - θ * i^2 / 2) * vw + σ^2 / 2 * vww - r * v)\n  return (; vt), (; v, vw, vww, w)\nend\n\nWe first solve the HJB with guessed slopes for the marginal value of cash at the two boundaries.\n\nresult = pdesolve(m, stategrid, yend; bc = (; vw = (1.5, 1.0)))\n\nThe guessed slopes are only a starting point. We then use NLsolve to find the boundary slopes that satisfy the value-matching and optimality conditions of Bolton–Chen–Wang (2009) — one at the refinancing boundary (marginal value of cash 1+gamma) and one at the payout boundary (marginal value of cash 1) — and re-solve at the fixed point.\n\nfunction f(m, x, stategrid, y)\n  result = pdesolve(m, stategrid, y; bc = (; vw = (x[1], x[2])), verbose = false)\n  v, vw, w = result.optional[:v], result.optional[:vw], result.optional[:w]\n  out = zeros(2)\n  mi = argmin(abs.(1 + m.γ .- vw))\n  out[1] =  v[mi] - m.ϕ - (1 + m.γ) * w[mi] - v[1]\n  wi = argmin(abs.(1 .- vw))\n  i = 1 / m.θ * (v[wi] - w[wi] - 1)\n  out[2] = m.r * v[wi] - (i - m.δ) * (v[wi] - w[wi]) - ((m.r - m.λ) * w[wi] + m.A - i - m.θ * i^2 / 2)\n  return out\nend\n\nnewsol = nlsolve(x -> f(m, x, stategrid, yend), [1.0,  1.0])\nresult = pdesolve(m, stategrid, yend; bc = (; vw = (newsol.zero[1], newsol.zero[2])))","title":"The equation"},{"category":"section","location":"examples/corporate_finance/bolton_chen_wang/#The-solution","page":"Bolton–Chen–Wang: financing constraint","text":"Firm value v(w) is increasing and concave in the cash–capital ratio: an extra dollar of internal cash is worth more than one (v(w)1) because external finance is costly, and this marginal value declines as the buffer grows. Investment i(w) rises with cash, so a cash-poor firm underinvests — internal liquidity, not just fundamentals, drives real investment. Both panels use the solution at the boundary slopes pinned down by NLsolve.\n\nws = stategrid[:w]\nv = result.optional[:v]\nvw = result.optional[:vw]\ni = (v ./ vw .- ws .- 1) ./ m.θ\np1 = plot(ws, v; xlabel = \"cash–capital ratio w\", ylabel = \"firm value v(w)\", legend = false)\np2 = plot(ws, i; xlabel = \"cash–capital ratio w\", ylabel = \"investment rate i(w)\", legend = false)\nplot(p1, p2; layout = (1, 2), size = (800, 300))\n\n\n\nThis page was generated using Literate.jl.","title":"The solution"},{"category":"section","location":"examples/consumption_saving/consumption_saving_two_income_states/#Achdou–Han–Lasry–Lions–Moll:-consumption–saving-with-two-income-states","page":"Consumption–saving: two income states","text":"A household saves in a riskless asset a at rate r and earns a labor income that switches between a low value y_l and a high value y_h as a two-state Poisson process (intensities lambda_lh, lambda_hl). There is one continuous state (assets a) but two coupled value functions v_l(a) and v_h(a), one per income state, linked by the income transitions:\n\nrho v_j(a) = max_c fracc^1-gamma1-gamma + v_j(a)(y_j + r a - c) + lambda_jkbigl(v_k(a)-v_j(a)bigr) qquad j ne k\n\nA borrowing constraint a ge underline a is enforced by preventing the asset drift from turning negative at the lower bound.","title":"Achdou–Han–Lasry–Lions–Moll: consumption–saving with two income states"},{"category":"section","location":"examples/consumption_saving/consumption_saving_two_income_states/#The-model","page":"Consumption–saving: two income states","text":"The parameters live in a struct:\n\nusing EconPDEs, Plots\n\nBase.@kwdef mutable struct AchdouHanLasryLionsMoll_TwoStatesModel\n    yl::Float64 = 0.5        # low income state\n    yh::Float64 = 1.5        # high income state\n    λlh::Float64 = 0.2       # low → high transition intensity\n    λhl::Float64 = 0.2       # high → low transition intensity\n    r::Float64 = 0.03        # risk-free rate\n    ρ::Float64 = 0.04        # discount rate\n    γ::Float64 = 2.0         # relative risk aversion\n    amin::Float64 = -yl / r  # borrowing limit (natural limit)\n    amax::Float64 = 50.0     # top of the asset grid\nend","title":"The model"},{"category":"section","location":"examples/consumption_saving/consumption_saving_two_income_states/#The-state-space","page":"Consumption–saving: two income states","text":"We build the grid and the initial guess first, because they fix the names used everywhere else. The grid is a NamedTuple whose key is the single continuous state (a); the guess is a NamedTuple whose keys are the two unknown functions (vl and vh), one starting value per grid point. These names are what reappear inside the equation below — e.g. vla_up will be the forward finite difference of vl in a.\n\nThe asset grid is finer near the borrowing limit, and we start from an autarky-style guess. The borrowing limit itself is nudged just inside the natural limit -y_lr to keep consumption strictly positive there.\n\nm = AchdouHanLasryLionsMoll_TwoStatesModel()\nm.amin += 0.001\nstategrid = (; a = m.amin .+ range(0, (m.amax - m.amin)^(1 / 2), length = 200) .^ 2)\nyend = (;\n    vl = (m.ρ ./ m.γ .+ (1 .- 1 / m.γ) .* m.r)^(-m.γ) .* (stategrid[:a] .+ m.yl ./ m.r) .^ (1 - m.γ) ./ (1 - m.γ),\n    vh = (m.ρ ./ m.γ .+ (1 .- m.γ) .* m.r)^(-m.γ) .* (stategrid[:a] .+ m.yh ./ m.r) .^ (1 - m.γ) ./ (1 - m.γ),\n)","title":"The state space"},{"category":"section","location":"examples/consumption_saving/consumption_saving_two_income_states/#The-equation","page":"Consumption–saving: two income states","text":"We now write the function encoding the HJB equation. Following the package convention, it takes the current state (a grid point) and u (each unknown together with its finite-difference derivatives there) and returns the time derivative of each unknown.\n\nIn each income state we upwind the asset drift on its sign, capping the implied consumption rather than flooring the marginal value (Newton may try negative marginal values). At the borrowing constraint the drift is set to zero. We save consumption cl, ch to plot.\n\nfunction (m::AchdouHanLasryLionsMoll_TwoStatesModel)(state::NamedTuple, u::NamedTuple)\n    (; yl, yh, λlh, λhl, r, ρ, γ, amin, amax) = m\n    (; a) = state\n    (; vl, vla_up, vla_down, vh, vha_up, vha_down) = u\n    clmax = 100.0 * (yl + r * max(a, 0.0))\n    chmax = 100.0 * (yh + r * max(a, 0.0))\n\n    # upwind the low-income value function\n    cl_up = vla_up > 0 ? min(vla_up^(-1 / γ), clmax) : clmax\n    μla_up = yl + r * a - cl_up\n    if μla_up >= 0.0\n        vla, cl, μla = vla_up, cl_up, μla_up\n    else\n        cl_down = vla_down > 0 ? min(vla_down^(-1 / γ), clmax) : clmax\n        μla_down = yl + r * a - cl_down\n        if μla_down <= 0.0 && a > amin\n            vla, cl, μla = vla_down, cl_down, μla_down\n        else\n            cl = yl + r * a          # borrowing constraint binds: drift is zero\n            μla = 0.0\n            vla = cl^(-γ)\n        end\n    end\n    vlt = -(cl^(1 - γ) / (1 - γ) + μla * vla + λlh * (vh - vl) - ρ * vl)\n\n    # upwind the high-income value function\n    ch_up = vha_up > 0 ? min(vha_up^(-1 / γ), chmax) : chmax\n    μha_up = yh + r * a - ch_up\n    if μha_up >= 0.0\n        vha, ch, μha = vha_up, ch_up, μha_up\n    else\n        ch_down = vha_down > 0 ? min(vha_down^(-1 / γ), chmax) : chmax\n        μha_down = yh + r * a - ch_down\n        if μha_down <= 0.0 && a > amin\n            vha, ch, μha = vha_down, ch_down, μha_down\n        else\n            ch = yh + r * a\n            μha = 0.0\n            vha = ch^(-γ)\n        end\n    end\n    vht = -(ch^(1 - γ) / (1 - γ) + μha * vha + λhl * (vl - vh) - ρ * vh)\n\n    return (; vlt, vht), (; cl, ch, μla, μha)\nend\n\nWith the equation, grid, and guess in hand, pdesolve solves the stationary system:\n\nresult = pdesolve(m, stategrid, yend)","title":"The equation"},{"category":"section","location":"examples/consumption_saving/consumption_saving_two_income_states/#The-solution","page":"Consumption–saving: two income states","text":"Consumption rises with wealth in both income states and is higher when income is high (left). The saving rate (right) is pinned at zero at the borrowing limit for the low-income household — which is forced to consume its income y_l + r a — and turns positive as wealth rises.\n\nas = stategrid[:a]\nidx = 1:div(length(as), 3)          # left third of the asset grid, where the curvature is\np1 = plot(as[idx], [result.optional[:cl][idx] result.optional[:ch][idx]]; label = [\"low income\" \"high income\"], xlabel = \"assets a\", ylabel = \"consumption c(a)\", legend = :bottomright)\np2 = plot(as[idx], [result.optional[:μla][idx] result.optional[:μha][idx]]; label = [\"low income\" \"high income\"], xlabel = \"assets a\", ylabel = \"saving μa(a)\", legend = :topright)\nhline!(p2, [0.0]; color = :gray, linestyle = :dash, label = \"\")\nplot(p1, p2; layout = (1, 2), size = (800, 300))\n\n\n\nThis page was generated using Literate.jl.","title":"The solution"},{"category":"section","location":"examples/consumption_saving/wang_wang_yang/#Wang–Wang–Yang-(2016):-consumption-and-saving-with-recursive-utility","page":"Wang–Wang–Yang: liquidity management","text":"Wang, Wang, and Yang (2016) study optimal consumption and saving when labor income follows a geometric Brownian motion, dY = mu Ydt + sigma YdZ, and the household has recursive (Epstein–Zin) preferences with elasticity of intertemporal substitution psi and risk aversion gamma. Wealth accumulates as dX = (r X + Y - C)dt subject to a borrowing limit. Because income is a GBM and preferences are homothetic, the value function is homogeneous and the problem collapses to a single ODE in the wealth-to-income ratio w = XY. Writing p(w) for the scaled value function — equivalently, total wealth (financial plus human) per unit of income — it solves\n\n0 = left(frac(r + psi(rho - r))p^1-psi - psirhopsi - 1 + mu - tfracgammasigma^22right) p\n  + bigl((r - mu + gammasigma^2)w + 1bigr) p\n  + tfrac12 sigma^2 w^2left(p - gammafracp^2pright)\n\nwith the consumption–income ratio recovered from c = (r + psi(rho - r)) p p^-psi.","title":"Wang–Wang–Yang (2016): consumption and saving with recursive utility"},{"category":"section","location":"examples/consumption_saving/wang_wang_yang/#The-model","page":"Wang–Wang–Yang: liquidity management","text":"The parameters live in a struct:\n\nusing EconPDEs, Plots\n\nBase.@kwdef mutable struct WangWangYangModel\n    μ::Float64 = 0.015       # expected growth rate of labor income\n    σ::Float64 = 0.1         # volatility of labor income\n    r::Float64 = 0.035       # risk-free rate\n    ρ::Float64 = 0.04        # discount rate\n    γ::Float64 = 3.0         # relative risk aversion\n    ψ::Float64 = 1.1         # elasticity of intertemporal substitution\n    wmin::Float64 = 0.0      # borrowing limit (minimum wealth-income ratio)\n    wmax::Float64 = 1000.0   # maximum wealth-income ratio (grid upper bound)\nend","title":"The model"},{"category":"section","location":"examples/consumption_saving/wang_wang_yang/#The-state-space","page":"Wang–Wang–Yang: liquidity management","text":"We build the grid and the initial guess first, because they fix the names used everywhere else. The grid is a NamedTuple whose key is the state variable (w, the wealth-to-income ratio); the guess is a NamedTuple whose key is the unknown function (p), one starting value per grid point. These names are what reappear inside the equation below — e.g. pw_up will be the forward finite difference of p in w.\n\nThe state w runs from the borrowing limit to a large upper bound on a grid that is denser near the constraint. The initial guess p = 1 + w reflects total wealth as financial plus one unit of human wealth.\n\nm = WangWangYangModel()\nstategrid = (; w = range(m.wmin^(1/2), m.wmax^(1/2), length = 100).^2)\nyend = (; p = 1 .+ stategrid[:w])","title":"The state space"},{"category":"section","location":"examples/consumption_saving/wang_wang_yang/#The-equation","page":"Wang–Wang–Yang: liquidity management","text":"We now write the function encoding the HJB equation. Following the package convention, it takes the current state (a grid point) and u (each unknown together with its finite-difference derivatives there) and returns the time derivative of each unknown.\n\nWe upwind on the physical wealth drift mu_w of the controlled state, falling back to mu_w = 0 when the borrowing constraint binds.\n\nfunction (m::WangWangYangModel)(state::NamedTuple, u::NamedTuple)\n    (; μ, σ, r, ρ, γ, ψ, wmin, wmax) = m\n    (; w) = state\n    (; p, pw_up, pw_down, pww) = u\n    # Newton can try negative marginal values, so cap implied consumption instead of flooring derivatives.\n    cmax = 100.0 * (1 + max((r - μ + σ^2) * w, 0.0))\n\n    # Upwind on the physical wealth drift of the controlled state process.\n    c_up = pw_up > 0 ? min((r + ψ * (ρ - r)) * p * pw_up^(-ψ), cmax) : cmax\n    μw_up = (r - μ + σ^2) * w + 1 - c_up\n    if μw_up >= 0\n        pw = pw_up\n        c = c_up\n        μw = μw_up\n    else\n        c_down = pw_down > 0 ? min((r + ψ * (ρ - r)) * p * pw_down^(-ψ), cmax) : cmax\n        μw_down = (r - μ + σ^2) * w + 1 - c_down\n        if (μw_down <= 0) && (w > wmin)\n            pw = pw_down\n            c = c_down\n            μw = μw_down\n        else\n            # If the two candidates straddle zero OR drift is negative at minimum asset threshold\n            # we impose drift μw = 0.\n            μw = 0.0\n            c = 1 + (r - μ + σ^2) * w\n            pw = (c / ((r + ψ * (ρ - r)) * p))^(-1 / ψ)\n        end\n    end\n    # At the top, I use the solution of the unconstrainted, i.e. pw = 1 (I could also do reflecting boundary but less elegant)\n    pt = - ((((r + ψ * (ρ - r)) * pw^(1 - ψ) - ψ * ρ) / (ψ - 1) + μ - γ * σ^2 / 2) * p + ((r - μ + γ * σ^2) * w + 1) * pw + σ^2 * w^2 / 2  * (pww - γ * pw^2 / p))\n    return (; pt), (; c, μw)\nend\n\nWith the equation, grid, and guess in hand, pdesolve solves the stationary system, imposing the boundary condition p(w) = 1 — which pins the marginal value of wealth — at both ends:\n\nresult = pdesolve(m, stategrid, yend, bc = (; pw = (1.0, 1.0)))","title":"The equation"},{"category":"section","location":"examples/consumption_saving/wang_wang_yang/#The-solution","page":"Wang–Wang–Yang: liquidity management","text":"The consumption–income ratio and the saving rate, over the low-wealth region:\n\nws = stategrid[:w]\nidx = 1:div(length(ws), 3)          # left third of the grid, where the curvature is\np1 = plot(ws[idx], result.optional[:c][idx]; xlabel = \"wealth-income ratio w\", ylabel = \"consumption c\", legend = false)\np2 = plot(ws[idx], result.optional[:μw][idx]; xlabel = \"wealth-income ratio w\", ylabel = \"saving μw\", legend = false)\nhline!(p2, [0.0]; color = :gray, linestyle = :dash)\nplot(p1, p2; layout = (1, 2), size = (800, 300))\n\nConsumption (left) is increasing and concave in the wealth-to-income ratio, steepest near the borrowing constraint where the marginal propensity to consume is highest. The saving rate (right) is largest for constrained, low-wealth households and declines as wealth rises toward its target — the shadow value of the borrowing constraint fading as financial wealth accumulates.\n\n\n\nThis page was generated using Literate.jl.","title":"The solution"},{"category":"section","location":"#EconPDEs.jl","page":"Home","text":"EconPDEs.jl solves nonlinear ODEs and PDEs that arise in economic models, especially Hamilton–Jacobi–Bellman equations. You write the local equation; the package supplies the finite-difference derivatives, upwinding, sparse Jacobians, and pseudo-transient Newton iteration.","title":"EconPDEs.jl"},{"category":"section","location":"#Installation","page":"Home","text":"] add EconPDEs","title":"Installation"},{"category":"section","location":"#Getting-started","page":"Home","text":"The README documents the modeling conventions, boundary conditions, and optimal-stopping support; the pdesolve reference lists every solver option.\n\nThe Examples in the sidebar each solve a model, plot the solution, and explain what it means economically. They are grouped into macro and finance models and ordered roughly from the simplest to the most involved — start with Neoclassical growth.","title":"Getting started"}]
}
