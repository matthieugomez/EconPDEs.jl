var documenterSearchIndex = {"docs":
[{"category":"section","location":"examples/asset_pricing/haddad/#Haddad:-concentrated-ownership-and-endogenous-volatility","page":"Haddad: endogenous volatility","text":"This asset-pricing model has two state variables — expected consumption growth mu and consumption variance v — and prices the aggregate consumption claim under Epstein–Zin preferences, much like the long-run-risk model. Its distinctive feature is concentrated ownership: only a limited set of active investors bears aggregate risk, and the equilibrium active-capital share alpha^star is determined endogenously,\n\nalpha^star = baralpha - sqrtdfraclambdabaralphatfrac12gammabigl(sigma_c^2 + sigma_pmu^2 + sigma_pv^2bigr)\n\nwhere the return-volatility terms sigma_pmusigma_pv depend on the derivatives of the unknown price function p(mu v). Because alpha^star feeds the market price of risk kappa=alpha^stargammasigma while return volatility feeds back into alpha^star, risk premia and volatility are endogenous and jointly determined. The two states themselves follow mean-reverting square-root processes.","title":"Haddad: concentrated ownership and endogenous volatility"},{"category":"section","location":"examples/asset_pricing/haddad/#The-model","page":"Haddad: endogenous volatility","text":"The parameters live in a struct:\n\nusing EconPDEs, Distributions, Plots\n\nBase.@kwdef struct HaddadModel\n  # consumption process parameters\n  μbar::Float64 = 0.018     # long-run mean of expected consumption growth μ\n  vbar::Float64 = 0.00052   # long-run mean of consumption variance v\n  κμ::Float64 = 0.3         # mean-reversion speed of μ\n  νμ::Float64 = 0.456       # volatility loading of the μ process\n  κv::Float64 = 0.012       # mean-reversion speed of v\n  νv::Float64 = 0.00472     # volatility loading of the v process\n\n  # active capital\n  αbar::Float64 = 1.2       # maximum active-capital share\n  λ::Float64 = 0.018        # cost of active participation (ownership friction)\n\n  # utility parameters\n  ρ::Float64 = 0.0132       # rate of time preference (discount rate)\n  γ::Float64 = 10.0         # relative risk aversion\n  ψ::Float64 = 1.5          # elasticity of intertemporal substitution\nend","title":"The model"},{"category":"section","location":"examples/asset_pricing/haddad/#The-state-space","page":"Haddad: endogenous volatility","text":"We build the grid and the initial guess first, because they fix the names used everywhere else. This model has two state variables, so the grid is a NamedTuple with two keys (μ and v); the guess is a NamedTuple whose key is the unknown function (p, the price–consumption ratio), a matrix over the (mu v) grid. These names reappear inside the equation below — e.g. pμ_up is the forward finite difference of p in μ, and pμμ is the second derivative. The grid spans the ergodic ranges of mu (Normal) and v (Gamma). The sqrt v diffusion vanishes at v = 0, a degenerate boundary where no condition is imposed.\n\nm = HaddadModel()\nσ = sqrt(m.νμ^2 * m.vbar / (2 * m.κμ))\nμmin = quantile(Normal(m.μbar, σ), 0.001)\nμmax = quantile(Normal(m.μbar, σ), 0.999)\nμs = range(μmin,  μmax, length = 30)\nα = 2 * m.κv * m.vbar / m.νv^2\nβ = m.νv^2 / (2 * m.κv)\nvmin = quantile(Gamma(α, β), 0.001)\nvmax = quantile(Gamma(α, β), 0.999)\nvs = range(vmin,  vmax, length = 30)\nstategrid = (; μ = μs, v = vs)\nguess =   (; p = ones(length(stategrid[:μ]), length(stategrid[:v])))","title":"The state space"},{"category":"section","location":"examples/asset_pricing/haddad/#The-equation","page":"Haddad: endogenous volatility","text":"We now write the function encoding the HJB equation. Following the package convention, it takes the current state (a grid point) and u (each unknown together with its finite-difference derivatives there) and returns the time derivative of each unknown.\n\nfunction (m::HaddadModel)(state::NamedTuple, u::NamedTuple)\n  (; μbar, vbar, κμ, νμ, κv, νv, αbar, λ, ρ, γ, ψ) = m\n  (; μ, v) = state\n  (; p, pμ_up, pμ_down, pv_up, pv_down, pμμ, pvv) = u\n\n  # drifts and volatilities of consumption, μ, and v\n  μc = μ\n  σc = sqrt(v)\n  μμ = κμ * (μbar - μ)\n  σμ = νμ * sqrt(v)\n  μv = κv * (vbar - v)\n\n  pμ = (μμ >= 0) ? pμ_up : pμ_down\n  pv = (μv >= 0) ? pv_up : pv_down\n  σv = νv * sqrt(v)\n  σp_Zμ = pμ / p * σμ\n  σp_Zv = pv / p * σv\n  σp2 = σp_Zμ^2 + σp_Zv^2\n  μp = pμ / p * μμ + pv / p * μv + 0.5 * pμμ / p * σμ^2 + 0.5 * pvv / p * σv^2\n\n  # market price of risk, with endogenous active-capital share α*\n  αstar = αbar - sqrt(λ * αbar / (0.5 * γ * (σc^2 + σp_Zμ^2 + σp_Zv^2)))\n  αstar = clamp(αstar, 0.0, 1.0)\n  κ_Zc = αstar * γ * σc\n  κ_Zμ = (αstar - (1 / γ - 1) / (ψ - 1)) * γ * σp_Zμ\n  κ_Zv = (αstar - (1 / γ - 1) / (ψ - 1)) * γ * σp_Zv\n  κ2 = κ_Zc^2 + κ_Zμ^2 + κ_Zv^2\n\n  # pricing equation for the price–consumption ratio (interest rate substituted out)\n  pt = - p * (1 / p - ρ + (1 - 1 / ψ) * (μc - 0.5 * γ * σc^2 * (1 - (αstar - 1)^2)) + μp + (0.5 * (1 / ψ - γ) / (1 - 1 / ψ) + 0.5 * γ * (1 - 1 / ψ) * (αstar - 1)^2) * σp2)\n  return (; pt)\nend\n\nWith the equation, grid, and guess in hand, pdesolve solves the stationary system:\n\nresult = pdesolve(m, stategrid, guess)","title":"The equation"},{"category":"section","location":"examples/asset_pricing/haddad/#The-solution","page":"Haddad: endogenous volatility","text":"Holding one state fixed at its median, the price–consumption ratio rises with expected growth mu and falls with consumption variance v. The growth effect reflects the elasticity of intertemporal substitution (psi1): investors pay more for the claim when future growth is high. The variance effect is the risk-premium channel — and here it is amplified, because only a concentrated set of active owners bears aggregate risk, raising the effective price of risk alpha^stargammasigma and making valuations more sensitive to volatility than under full risk sharing.\n\np = result.zero[:p]\niμ = div(length(stategrid[:μ]), 2)\niv = div(length(stategrid[:v]), 2)\np1 = plot(stategrid[:μ], p[:, iv]; xlabel = \"expected growth μ\", ylabel = \"price–consumption ratio p\", legend = false)\np2 = plot(stategrid[:v], p[iμ, :]; xlabel = \"consumption variance v\", ylabel = \"price–consumption ratio p\", legend = false)\nplot(p1, p2; layout = (1, 2), size = (800, 300))\n\n\n\nThis page was generated using Literate.jl.","title":"The solution"},{"category":"section","location":"examples/asset_pricing/tuckman_vila/#Tuckman–Vila-(1992):-a-finite-horizon-problem","page":"Tuckman–Vila: finite horizon","text":"So far every model has been stationary. Tuckman–Vila is time-dependent: an arbitrageur faces holding costs over a finite horizon 0 T, so the value function F(z tau) depends on calendar time. pdesolve handles this by taking a time grid τs as a fourth argument and solving backward from a terminal condition. The PDE function then takes a third argument τ:\n\npartial_tau F + max_iBigl (mu + sigma^2 F_z) a i - tfrac12 sigma^2 (a i)^2 Bigr\n  - rho z F_z + tfrac12 sigma^2 (F_zz - F_z^2) = 0","title":"Tuckman–Vila (1992): a finite-horizon problem"},{"category":"section","location":"examples/asset_pricing/tuckman_vila/#The-model","page":"Tuckman–Vila: finite horizon","text":"The parameters live in a struct:\n\nusing EconPDEs, Distributions, Plots\n\nBase.@kwdef struct TuckmanVilaModel\n    c::Float64 = 0.06     # coupon rate of the bond\n    r::Float64 = 0.09     # risk-free rate\n    ρ::Float64 = 5.42     # mean-reversion speed of the state z\n    σ::Float64 = 26.72    # volatility of the state z\n    a::Float64 = 26.72    # position (holding-cost) scaling parameter\n    T::Float64 = 100      # horizon (terminal date)\nend","title":"The model"},{"category":"section","location":"examples/asset_pricing/tuckman_vila/#The-state-space","page":"Tuckman–Vila: finite horizon","text":"We build the grid, the initial guess, and the time grid first, because they fix the names used everywhere else. The grid is a NamedTuple whose key is the state variable (z); the guess is a NamedTuple whose key is the unknown function (F), which here doubles as the terminal condition at τs[end]. These names reappear inside the equation below — e.g. Fz_up is the forward finite difference of F in z. The grid spans the stationary range of z (a mean-reverting Gaussian). Because the problem is finite-horizon, we also build a time grid τs over 0 T, which pdesolve will march backward along.\n\nfunction initialize_stategrid(m::TuckmanVilaModel; zn = 200)\n    d = Normal(0, sqrt(m.σ^2 / (2 * m.ρ)))\n    (; z = range(quantile(d, 0.00001), quantile(d, 0.99999), length = zn))\nend\n\nfunction initialize_y(m::TuckmanVilaModel, stategrid)\n    zn = length(stategrid[:z])\n    (; F = zeros(zn))\nend\n\nm = TuckmanVilaModel()\nstategrid = initialize_stategrid(m)\nguess = initialize_y(m, stategrid)\nτs = range(0, m.T, length = 10)","title":"The state space"},{"category":"section","location":"examples/asset_pricing/tuckman_vila/#The-equation","page":"Tuckman–Vila: finite horizon","text":"We now write the function encoding the HJB equation. Following the package convention, it takes the current state (a grid point) and u (each unknown together with its finite-difference derivatives there) and returns the time derivative of each unknown. Because the problem is time-dependent, it also takes a third argument, the time τ.\n\nfunction (m::TuckmanVilaModel)(state::NamedTuple, u::NamedTuple, τ::Number)\n    (; c, r, ρ, σ, a, T) = m\n    (; z) = state\n    (; F, Fz_up, Fz_down, Fzz) = u\n    Fz = (z >= 0) ? Fz_up : Fz_down\n    ϕ = z * (1 + z^2)^(-1 / 2)\n    ϕz = (1 + z^2)^(-3 / 2)\n    ϕzz = -3 * z * (1 + z^2)^(-5 / 2)\n    sτ = c / r * (1 - exp(-r * (T - τ)))\n    μL = ρ * z - 0.5 * σ^2 * ϕzz / ϕz + c / sτ * (ϕ - 1) / ϕz\n    iL = 1 / a * (μL / σ^2 + Fz)\n    μS = ρ * z - 0.5 * σ^2 * ϕzz / ϕz + c / sτ * (ϕ + 1) / ϕz\n    iS = 1 / a * (μS / σ^2 + Fz)\n    μ, i = 0.0, 0.0\n    if iL > 0\n        i, μ = iL, μL\n    elseif iS < 0\n        i, μ = iS, μS\n    end\n    Ft = -((μ + σ^2 * Fz) * a * i - 0.5 * σ^2 * (a * i)^2 - ρ * z * Fz + 0.5 * σ^2 * (Fzz - Fz^2))\n    return (; Ft)\nend\n\npdesolve takes the time grid τs as a fourth argument and marches backward from the terminal condition:\n\nresult = pdesolve(m, stategrid, guess, τs)","title":"The equation"},{"category":"section","location":"examples/asset_pricing/tuckman_vila/#The-solution","page":"Tuckman–Vila: finite horizon","text":"For a time-dependent problem result.zero[i] is the solution at time τs[i] (so result.zero[end] is the terminal condition). To make the backward time dimension visible, we fix a representative state near z = 0 and plot its value over calendar time. The value falls to the terminal condition F = 0 as tau approaches T.\n\nzs = stategrid[:z]\nz0_index = argmin(abs.(zs))\nF_at_z0 = [result.zero[i][:F][z0_index] for i in eachindex(τs)]\nplot(τs, F_at_z0; xlabel = \"time τ\", ylabel = \"value F(z ≈ 0, τ)\", legend = false)\n\n\n\nThis page was generated using Literate.jl.","title":"The solution"},{"category":"section","location":"pde_function/#Writing-the-PDE-function","page":"Writing the PDE function","text":"The PDE function is where the model lives: it encodes the equation at one grid point, and pdesolve assembles it into a global nonlinear system. This page is the reference for writing it. Everything in EconPDEs.jl is keyed by names: the names of the state variables and the names of the unknown functions. You choose them once, in the grid and the guess, and they determine the field names of u, the required return names, and the keys of the result. Getting started shows these conventions in action on a first model.","title":"Writing the PDE function"},{"category":"section","location":"pde_function/#Grids-and-guesses","page":"Writing the PDE function","text":"The state grid is a NamedTuple mapping each state variable to an AbstractVector; the guess is a NamedTuple mapping each unknown function to an array of starting values with the same shape as the grid:\n\nstategrid = (; y = range(0.5, 1.5, length = 10), a = range(0.0, 100.0, length = 500))\nguess = (; v = [initial_value(y, a) for y in stategrid[:y], a in stategrid[:a]])\n\nGrid entries can be different vector containers and element types: for example, one dimension can be a range and another can be a Vector.\n\nUse the (; name = value) form; an OrderedDict is also accepted. A plain Dict is rejected, because its iteration order is arbitrary and the order of names determines how the solution arrays are laid out (the first state is the first array dimension, and so on).\n\nWith one, two, or three state variables, the guess is a Vector, Matrix, or 3-dimensional Array, and so are the solved unknowns.","title":"Grids and guesses"},{"category":"section","location":"pde_function/#The-function-signature","page":"Writing the PDE function","text":"The PDE function must have the form\n\nf(state::NamedTuple, u::NamedTuple) -> NamedTuple\n\n(or f(state, u, t) for a time-dependent equation, see Time-dependent problems). state gives the current grid point, one entry per state variable. u is the local finite-difference bundle: the value of each unknown and all the derivative stencils the equation may need at that point. The model then selects the economically correct stencil, usually by the sign of a drift (see Upwinding).","title":"The function signature"},{"category":"section","location":"pde_function/#Derivative-naming","page":"Writing the PDE function","text":"Field names concatenate the unknown name, the state name or names, and an optional direction suffix. If the unknown is v and the states are k and l, then u contains:\n\nField Meaning\nv value of the unknown at this grid point\nvk_up, vk_down forward and backward first derivatives in k\nvl_up, vl_down forward and backward first derivatives in l\nvkk, vll own second derivatives (central)\nvkl_up, vkl_down directional cross derivatives, for positive and negative cross-diffusion terms\n\nNote there is no plain vk: the package never chooses a first-derivative direction for you. Cross-derivative names use the states in grid order (vkl_up, not vlk_up).\n\nWith several unknowns, the same rule applies to each one: unknowns v and w on states k and l give fields such as v, vk_up, vkl_up, w, wk_up, and wkl_up. The argument name u is only a convention; the fields are what matter.\n\nThe examples typically unpack the fields they need at the top of the function with Julia's NamedTuple destructuring syntax:\n\n(; v, vk_up, vk_down, vkk) = u\n\nwhich is shorthand for v = u.v; vk_up = u.vk_up; …. The same syntax unpacks the current grid point ((; k, l) = state) and, when the model is a struct, its parameters ((; ρ, γ) = m). Writing u.vk_up directly, as in Getting started, is equivalent.\n\nBecause names are formed by concatenation, some combinations are ambiguous — states (k, a) with unknowns (v, vk) would both generate a field vka_up. pdesolve checks for this upfront and throws an error asking you to rename.\n\nThis naming step is the one piece of magic in the package, so it is worth knowing what happens behind it. Before solving, pdesolve reads the state names off the grid and the unknown names off the guess, and generates a function specialized to those names that, at every grid point, fills each field in the table with the corresponding finite-difference stencil — one-sided differences from the neighboring grid points for _up/_down, central differences for the second derivatives, ghost-node values at the boundaries (see Boundary conditions). By the time your equation runs, u is an ordinary NamedTuple of numbers: every derivative the equation might ask for has already been computed, and the function simply picks which ones to use.","title":"Derivative naming"},{"category":"section","location":"pde_function/#The-return-value:-one-time-derivative-per-unknown","page":"Writing the PDE function","text":"The PDE function must return one time derivative per unknown, named Symbol(unknown, :t) — e.g. (; vt) for one unknown, (; pAt, pBt) for two. Write the stationary equation as 0 = RHS - ρ v and return vt = -(RHS - ρ v):\n\nvt = -(c^(1 - γ) / (1 - γ) + μk * vk - ρ * u.v)\nreturn (; vt)\n\nThe minus sign is the package's core convention. pdesolve finds the stationary solution by integrating the time-dependent equation partial_t v = textvt (a false transient) until it stops moving, and it is the backward-in-time HJB — with exactly this sign — whose transient is stable and converges to the stationary solution. If the sign is flipped, the iteration diverges — pdesolve recognizes the typical signature (a residual Jacobian whose diagonal is negative at every grid point) and warns on the first iteration. The same convention is what lets a single PDE function serve both stationary solves and genuinely time-dependent problems.","title":"The return value: one time derivative per unknown"},{"category":"section","location":"pde_function/#Saving-intermediate-outputs","page":"Writing the PDE function","text":"To store objects computed inside the PDE function (policies, drifts, prices of risk), return a second NamedTuple:\n\nreturn (; vt), (; c, μa, r)\n\nEach object in the second NamedTuple is evaluated at every grid point and stored as an array with the same shape as the grid, available in result.saved.","title":"Saving intermediate outputs"},{"category":"section","location":"pde_function/#The-result","page":"Writing the PDE function","text":"pdesolve returns an EconPDEResult with three fields:\n\nzero: the solved unknowns, indexed by name (result.zero[:v]);\nresidual_norm: the norm of the residual at the solution — check it is small before using the output;\nsaved: the saved objects, together with the solved unknowns for convenience.\n\nFor a time-dependent problem, zero is instead a vector of solutions, one per time point (result.zero[i][:v]), and residual_norm a vector of residual norms. For older code, result.optional remains an alias for result.saved.","title":"The result"},{"category":"section","location":"pde_function/#Upwinding","page":"Writing the PDE function","text":"u gives you two first derivatives per unknown and state — vk_up (forward) and vk_down (backward) — and never chooses between them. This section explains why, and gives the three patterns that cover essentially every model.","title":"Upwinding"},{"category":"section","location":"pde_function/#Why-upwind-at-all","page":"Writing the PDE function","text":"A finite-difference scheme for an HJB converges to the correct (viscosity) solution when it is monotone (Barles–Souganidis): loosely, the value at a grid point must be a weighted average of its neighbors with nonnegative weights, so the discrete solution inherits the maximum principle of the continuous equation. For the first-derivative (drift) term, this requires taking the derivative in the direction the state moves:\n\ndrift mu  0 → use the forward difference (_up);\ndrift mu  0 → use the backward difference (_down).\n\nChoosing the wrong direction puts a negative weight on a neighbor. The practical symptom is not subtle: oscillating iterates, a residual that stalls, or a pseudo-time step that collapses (see Solver and troubleshooting). Central differences for the second derivative are always fine (their weights are automatically positive), which is why vkk comes in only one flavor.\n\nMonotonicity also has a probabilistic reading: a monotone scheme is exactly one whose discretized operator is a valid Markov generator, with non-negative jump rates to neighboring grid points. That is the condition under which InfinitesimalGenerators.jl turns the same discretization into a Markov chain — and why an upwinded solution hands off exactly to its distributions and expectations.","title":"Why upwind at all"},{"category":"section","location":"pde_function/#Pattern-1:-exogenous-drift","page":"Writing the PDE function","text":"When the drift of a state does not depend on the derivative you are choosing, upwinding is one line. For example, income mean-reverting at rate mu_y = kappa(bar y - y):\n\nμy = κ * (ybar - y)\nvy = (μy >= 0) ? u.vy_up : u.vy_down","title":"Pattern 1: exogenous drift"},{"category":"section","location":"pde_function/#Pattern-2:-endogenous-drift-(consumption-saving)","page":"Writing the PDE function","text":"In a consumption-saving problem the asset drift mu_a = y + ra - c depends on consumption, which comes from the first-order condition c = (partial_a v)^-1gamma — which depends on the derivative you are choosing. The standard resolution tries each direction and keeps the one consistent with itself:\n\ncompute c from va_up; if the implied drift is positive, forward was the right direction — keep it;\notherwise compute c from va_down; if the implied drift is negative, backward was right — keep it;\notherwise (the two candidates straddle zero, or the borrowing constraint binds at the bottom of the grid), the state does not move: set mu_a = 0, i.e. consume exactly c = y + ra, and read the marginal value off the FOC, v_a = c^-gamma.\n\nc_up = va_up > 0 ? min(va_up^(-1 / γ), cmax) : cmax\nμa_up = y + r * a - c_up\nif μa_up >= 0\n    va, c, μa = va_up, c_up, μa_up\nelse\n    c_down = va_down > 0 ? min(va_down^(-1 / γ), cmax) : cmax\n    μa_down = y + r * a - c_down\n    if (μa_down <= 0) && (a > amin)\n        va, c, μa = va_down, c_down, μa_down\n    else\n        c = y + r * a\n        va, μa = c^(-γ), 0.0\n    end\nend\n\nTwo remarks on the guards:\n\ncmax caps consumption when the marginal value is nonpositive. Newton iterates can visit economically meaningless regions (negative va) on the way to the solution; the cap keeps the FOC well-defined there without affecting the converged answer.\nCase 3 with a == amin is precisely how a borrowing constraint is imposed: the household would like to dissave past the limit, but the constraint pins mu_a = 0. No separate boundary condition is needed — see Boundary conditions.\n\nThe consumption-saving examples use exactly this block.","title":"Pattern 2: endogenous drift (consumption-saving)"},{"category":"section","location":"pde_function/#Pattern-3:-general-equilibrium-(recompute-the-direction)","page":"Writing the PDE function","text":"In general-equilibrium models the drift of the state is pinned down by equilibrium objects — prices, volatilities — that themselves depend on the derivatives being solved for. The logic is the same as Pattern 2, but the derivative now enters through a whole block of equilibrium computations rather than a single first-order condition, so switching direction means redoing the block. The pattern: compute everything with _up derivatives; if the resulting drift is negative, recompute once with _down:\n\npAx, pBx = u.pAx_up, u.pBx_up\niter = 0\n@label start\n# ... compute σx, prices, and finally the drift μx from pAx, pBx ...\nif (iter == 0) && (μx <= 0)\n    iter += 1\n    pAx, pBx = u.pAx_down, u.pBx_down\n    @goto start\nend\n\nSee Gârleanu-Panageas for this pattern in context.","title":"Pattern 3: general equilibrium (recompute the direction)"},{"category":"section","location":"pde_function/#Cross-derivatives","page":"Writing the PDE function","text":"With two or more states and a nonzero cross-diffusion term (correlated shocks), the same monotonicity logic applies to the cross derivative. u exposes two directional versions, vkl_up / vkl_down. Choose by the sign of whatever multiplies the cross derivative — the instantaneous covariance of the two states:\n\nvkl = (σk * σl >= 0) ? u.vkl_up : u.vkl_down","title":"Cross derivatives"},{"category":"section","location":"pde_function/#Checking-your-upwinding","page":"Writing the PDE function","text":"Upwinding on the sign of the raw drift is a shortcut that is guaranteed to produce a monotone scheme only for terms that are linear in the derivative. As an opt-in diagnostic, pdesolve(...; check_monotonicity = true) inspects the fully assembled scheme and warns about neighbor weights with the wrong sign — see the monotonicity diagnostic.","title":"Checking your upwinding"},{"category":"section","location":"examples/asset_pricing/bansal_yaron/#Bansal–Yaron-(2004):-long-run-risk-(two-states)","page":"Bansal–Yaron: long-run risk","text":"The long-run-risk model has two state variables, so it exercises the package's 2D machinery — a matrix of unknowns and a sparse Jacobian on a genuine grid. Consumption growth has a small, persistent expected-growth component mu and a stochastic variance v:\n\ndmu = kappa_mu(barmu - mu)dt + nu_musqrtvdW_mu qquad\ndv = kappa_v(bar v - v)dt + nu_vsqrtvdW_v\n\nWith Epstein–Zin preferences the unknown is the wealth–consumption ratio p(mu v), which solves a nonlinear elliptic PDE. The gradient-nonlinear risk terms sigma_mu^2 p_mu^2p and sigma_v^2 p_v^2p come from the market prices of long-run-risk and variance risk.","title":"Bansal–Yaron (2004): long-run risk (two states)"},{"category":"section","location":"examples/asset_pricing/bansal_yaron/#The-model","page":"Bansal–Yaron: long-run risk","text":"The parameters live in a struct:\n\nusing EconPDEs, Distributions, Plots\n\nBase.@kwdef struct BansalYaronModel\n    μbar::Float64 = 0.018    # long-run mean of expected consumption growth\n    vbar::Float64 = 0.00073  # long-run mean of consumption variance\n    κμ::Float64 = 0.252      # mean-reversion speed of expected growth\n    νμ::Float64 = 0.528      # volatility of expected growth\n    κv::Float64 = 0.156      # mean-reversion speed of variance\n    νv::Float64 = 0.00354    # volatility of variance (vol of vol)\n    ρ::Float64 = 0.024       # discount rate\n    γ::Float64 = 7.5         # relative risk aversion\n    ψ::Float64 = 1.5         # elasticity of intertemporal substitution\nend","title":"The model"},{"category":"section","location":"examples/asset_pricing/bansal_yaron/#The-state-space","page":"Bansal–Yaron: long-run risk","text":"We build the grid and the initial guess first, because they fix the names used everywhere else. This model has two state variables, so the grid is a NamedTuple with two keys (μ and v); the guess is a NamedTuple whose key is the unknown function (p, the wealth–consumption ratio), a matrix over the (mu v) grid. These names reappear inside the equation below — e.g. pμ_up is the forward finite difference of p in μ, and pμμ is the second derivative. The grid spans the ergodic ranges of mu (Normal) and v (Gamma). The sqrt v diffusion vanishes at v = 0, a degenerate boundary where no condition is imposed.\n\nm = BansalYaronModel()\nμn, vn = 30, 30\nμdistribution = Normal(m.μbar, sqrt(m.νμ^2 * m.vbar / (2 * m.κμ)))\nμs = range(quantile(μdistribution, 0.01), quantile(μdistribution, 0.99), length = μn)\nνdistribution = Gamma(2 * m.κv * m.vbar / m.νv^2, m.νv^2 / (2 * m.κv))\nvs = range(quantile(νdistribution, 0.0), quantile(νdistribution, 0.99), length = vn)\nstategrid = (; μ = μs, v = vs)\nguess = (; p = ones(μn, vn))","title":"The state space"},{"category":"section","location":"examples/asset_pricing/bansal_yaron/#The-equation","page":"Bansal–Yaron: long-run risk","text":"We now write the function encoding the HJB equation. Following the package convention, it takes the current state (a grid point) and u (each unknown together with its finite-difference derivatives there) and returns the time derivative of each unknown.\n\nfunction (m::BansalYaronModel)(state::NamedTuple, u::NamedTuple)\n    (; μbar, vbar, κμ, νμ, κv, νv, ρ, γ, ψ) = m\n    (; μ, v) = state\n    (; p, pμ_up, pμ_down, pv_up, pv_down, pμμ, pvv) = u\n\n    # drifts and volatilities of consumption, μ and v\n    μc = μ\n    σc = sqrt(v)\n    μμ = κμ * (μbar - μ)\n    σμ = νμ * sqrt(v)\n    μv = κv * (vbar - v)\n    σv = νv * sqrt(v)\n\n    # upwind each first derivative on the sign of its drift\n    pμ = (μμ >= 0) ? pμ_up : pμ_down\n    pv = (μv >= 0) ? pv_up : pv_down\n\n    σp_Zμ = pμ / p * σμ\n    σp_Zv = pv / p * σv\n    σp2 = σp_Zμ^2 + σp_Zv^2\n    μp = pμ / p * μμ + pv / p * μv + 0.5 * pμμ / p * σμ^2 + 0.5 * pvv / p * σv^2\n\n    # market prices of risk\n    κ_Zc = γ * σc\n    κ_Zμ = -(1 - γ * ψ) / (ψ - 1) * σp_Zμ\n    κ_Zv = -(1 - γ * ψ) / (ψ - 1) * σp_Zv\n\n    r = ρ + μc / ψ - (1 + 1 / ψ) / 2 * γ * σc^2 - (γ * ψ - 1) / (2 * (ψ - 1)) * σp2\n    pt = -p * (1 / p + μc + μp - r - κ_Zc * σc - κ_Zμ * σp_Zμ - κ_Zv * σp_Zv)\n    return (; pt)\nend\n\nWith the equation, grid, and guess in hand, pdesolve solves the stationary system:\n\nresult = pdesolve(m, stategrid, guess)","title":"The equation"},{"category":"section","location":"examples/asset_pricing/bansal_yaron/#The-solution","page":"Bansal–Yaron: long-run risk","text":"The wealth–consumption ratio increases in expected growth mu and falls with variance v: investors pay more for the claim when future growth is high, and less when the economy is riskier. Because p is defined over the (mu v) plane, we show the full state-space pattern as a three-dimensional graph.\n\np = result.zero[:p]\nsurface(vs, μs, p; xlabel = \"variance v\", ylabel = \"growth μ\", zlabel = \"p\", colorbar = false, size = (720, 460), left_margin = 8Plots.mm, bottom_margin = 8Plots.mm, right_margin = 6Plots.mm)\n\n\n\nThis page was generated using Literate.jl.","title":"The solution"},{"category":"section","location":"examples/asset_pricing/wachter/#Wachter-(2013):-time-varying-rare-disaster-risk","page":"Wachter: rare disasters","text":"Wachter asks whether a time-varying probability of rare consumption disasters can explain the high mean and volatility of the equity premium. Consumption follows a jump–diffusion: most of the time it grows smoothly, but at Poisson rate lambda_t a disaster strikes and lowers consumption by a random factor e^Z (with Z0). The single state is the disaster intensity lambda_t, which mean-reverts as a square-root (CIR) process:\n\ndlambda_t = kappa_lambda(barlambda - lambda_t)dt + nu_lambdasqrtlambda_tdB_t\n\nWith Epstein–Zin preferences the unknown is the wealth–consumption ratio p(lambda). Because disaster risk moves over time, so do risk premia and valuations — even though realized consumption is smooth outside disasters.","title":"Wachter (2013): time-varying rare-disaster risk"},{"category":"section","location":"examples/asset_pricing/wachter/#The-model","page":"Wachter: rare disasters","text":"The parameters live in a struct:\n\nusing EconPDEs, Distributions, Plots\n\nBase.@kwdef struct WachterModel{T<: Distribution}\n    # consumption process parameters\n    μ::Float64 = 0.025      # expected consumption growth\n    σ::Float64 = 0.02       # volatility of consumption growth\n\n    # disaster process parameters\n    λbar::Float64 = 0.0355  # long-run mean disaster intensity\n    κλ::Float64 = 0.08      # mean-reversion speed of disaster intensity\n    νλ::Float64 = 0.067     # volatility of disaster intensity\n    ZDistribution::T = Normal(-0.4, 0.25) # distribution of disaster jump size Z (log consumption drop); from Ian Martin higher order cumulants paper\n\n    # utility parameters\n    ρ::Float64 = 0.012      # discount rate\n    γ::Float64 = 3.0        # relative risk aversion\n    ψ::Float64 = 1.1        # elasticity of intertemporal substitution\n    ϕ::Float64 = 2.6        # leverage of dividends on consumption\nend","title":"The model"},{"category":"section","location":"examples/asset_pricing/wachter/#The-state-space","page":"Wachter: rare disasters","text":"We build the grid and the initial guess first, because they fix the names used everywhere else. The grid is a NamedTuple whose key is the state variable (λ, the disaster intensity); the guess is a NamedTuple whose key is the unknown function (p, the wealth–consumption ratio). These names reappear inside the equation below — e.g. pλ_up will be the forward finite difference of p in λ. The state lambda ranges over intensities from zero up to well above its long-run mean barlambda. The sqrtlambda diffusion vanishes at lambda = 0, a degenerate boundary where no condition is imposed.\n\nfunction initialize_stategrid(m::WachterModel; λn = 30)\n  (; λ = range(0.0, 0.1, length = λn))\nend\n\nfunction initialize_y(m::WachterModel, stategrid)\n    λn = length(stategrid[:λ])\n    (; p = ones(λn))\nend\n\nm = WachterModel()\nstategrid = initialize_stategrid(m)\nguess = initialize_y(m, stategrid)","title":"The state space"},{"category":"section","location":"examples/asset_pricing/wachter/#The-equation","page":"Wachter: rare disasters","text":"We now write the function encoding the HJB equation. Following the package convention, it takes the current state (a grid point) and u (each unknown together with its finite-difference derivatives there) and returns the time derivative of each unknown.\n\nfunction (m::WachterModel)(state::NamedTuple, u::NamedTuple)\n    (; μ, σ, λbar, κλ, νλ, ZDistribution, ρ, γ, ψ, ϕ) = m\n    (; λ) = state\n    (; p, pλ_up, pλ_down, pλλ) = u\n\n    # drift and volatility of λ and p\n    μλ = κλ * (λbar - λ)\n    σλ = νλ * sqrt(λ)\n    # upwind on the sign of the drift of λ\n    pλ = (μλ >= 0) ? pλ_up : pλ_down\n    μp = pλ / p * μλ + 0.5 * pλλ / p * σλ^2\n    σp_Zλ = pλ / p * σλ\n\n    # market prices of risk\n    κ_Zc = γ * σ\n    κ_Zλ = (γ * ψ - 1) / (ψ - 1) * σp_Zλ\n    η =  λ * (mgf(ZDistribution, 1) - 1 + mgf(ZDistribution, -γ) - 1 - (mgf(ZDistribution, 1 - γ) - 1))\n\n    # interest rate\n    r = ρ + μ / ψ - (1 + 1 / ψ) / 2 * γ * σ^2 - λ * (mgf(ZDistribution, -γ) - 1) + (1 / ψ - γ) / (1 - γ) * λ * (mgf(ZDistribution, 1 - γ) - 1) - (γ * ψ - 1) / (2 * (ψ - 1)) * σp_Zλ^2\n\n    # market pricing of the consumption claim\n    pt = - p * (1 / p  + μ + μp + λ * (mgf(ZDistribution, 1) - 1) - r - κ_Zc * σ - κ_Zλ * σp_Zλ - η)\n\n    return (; pt)\nend\n\nWith the equation, grid, and guess in hand, pdesolve solves the stationary system:\n\nresult = pdesolve(m, stategrid, guess)","title":"The equation"},{"category":"section","location":"examples/asset_pricing/wachter/#The-solution","page":"Wachter: rare disasters","text":"The wealth–consumption ratio falls steeply as the disaster intensity lambda rises: when a disaster is more likely, the representative agent discounts the consumption claim more heavily and valuations drop.\n\nλs = stategrid[:λ]\nplot(λs, result.zero[:p]; xlabel = \"disaster intensity λ\", ylabel = \"wealth–consumption ratio p\", legend = false)\n\n\n\nThis page was generated using Literate.jl.","title":"The solution"},{"category":"section","location":"examples/asset_pricing/gomez/#Gomez-(2025):-wealth-inequality-and-asset-prices","page":"Gomez: wealth inequality","text":"An asset-pricing model with a wealth-distribution state. Households and entrepreneurs differ in preferences and in the risk they bear; entrepreneurs hold a leveraged position in the risky asset, so shifts in the wealth distribution move the price of risk and the volatility of returns. The single state x is the entrepreneurs' consumption share, and the unknown is the households' wealth–consumption ratio p_H. Demographics (birth, death, and entry of entrepreneurs) keep the distribution stationary.","title":"Gomez (2025): wealth inequality and asset prices"},{"category":"section","location":"examples/asset_pricing/gomez/#The-model","page":"Gomez: wealth inequality","text":"The parameters:\n\nusing EconPDEs, Plots\n\nBase.@kwdef struct GomezModel\n    # utility, households\n    γ::Float64 = 10.3      # households' relative risk aversion\n    ψ::Float64 = 0.05      # households' elasticity of intertemporal substitution\n    ρ::Float64 = 0.1       # households' discount rate\n\n    # utility, entrepreneurs\n    ρE::Float64 = 0.022    # entrepreneurs' discount rate\n    αE::Float64 = 2        # entrepreneurs' risk aversion\n    ν::Float64 = 0.1       # entrepreneurs' preference parameter\n\n    # demography\n    η::Float64 = 0.015     # birth rate\n    δ::Float64 = 0.025     # death rate\n    ϕ::Float64 = 0.01      # demographic transition rate\n    πE::Float64 = 0.09     # entry rate into entrepreneurship\n\n    # endowment process\n    g::Float64 = 0.02      # expected consumption growth\n    σ::Float64 = 0.04      # consumption volatility\n    λ::Float64 = 2.3       # endowment-process parameter\n    τ::Float64 = 0.0       # tax rate\nend","title":"The model"},{"category":"section","location":"examples/asset_pricing/gomez/#The-state-space","page":"Gomez: wealth inequality","text":"We build the grid and the initial guess first, because they fix the names used everywhere else. The grid is a NamedTuple whose key is the state variable (x, the entrepreneurs' consumption share); the guess is a NamedTuple whose key is the unknown function (pH), holding one starting value at each grid point. These names are what reappear inside the equation below — e.g. pHx_up will be the forward finite difference of pH in x.\n\nOne state x in 0 1 on a squared grid of 300 points. The single unknown p_H is initialized flat at one.\n\nm = GomezModel()\nstategrid = (; x = range(0, 1, 300) .^ 2)\nguess = (; pH = ones(length(stategrid[:x])))","title":"The state space"},{"category":"section","location":"examples/asset_pricing/gomez/#The-equation","page":"Gomez: wealth inequality","text":"We now write the function encoding the equilibrium conditions. Following the package convention, it takes the current state (a grid point) and u — the local bundle holding the unknown and its finite-difference derivatives there — and returns the time derivative of the unknown (pHt).\n\nThe volatility of the state sigma_x is endogenous, feeding back through prices, so the upwind direction is picked from the drift mu_x: forward difference pHx_up unless the drift turns negative, then backward pHx_down.\n\nfunction (m::GomezModel)(state::NamedTuple, u::NamedTuple)\n  (; γ, ψ, ρ, ρE, αE, ν, η, δ, ϕ, πE, g, σ, λ, τ) = m\n  (; x) = state\n  (; pH, pHx_up, pHx_down, pHxx) = u\n  p = x / ρE + (1 - x) * pH\n  xW = x / ρE / p\n  # Effective entrepreneur exposure, capped when the leverage constraint xW * αE ≥ 1 binds.\n  αEeff = (xW * αE >= 1) ? 1 / xW : αE\n  iter = 0\n  pHx = pHx_up\n  @label start\n  px = 1 / ρE  - pH + (1 - x) * pHx\n  σx = x * (αEeff - 1) * σ / (1 - x * αEeff * px / p)\n  σpH = pHx / pH * σx\n  σp = px / p * σx\n  σCE = αEeff * (σ + σp)\n  σCH = x < 1 ? (σ - x * σCE) / (1 - x) : 0.0\n  σWH = σCH + σpH\n  κ = γ * σCH + (γ * ψ - 1) / (ψ - 1) * σpH\n  ΦE = αEeff * κ * (σ + σp)\n  ΦH = κ^2 * (1 + ψ) / (2 * γ) + (1 - ψ * γ) / (γ * (ψ - 1)) * κ * σpH - (1 - γ * ψ) / (2 * γ * (ψ - 1)) * σpH^2\n  arriving_wealth = (η + δ + ϕ + τ) / (η + δ) * p\n  wedge = (η + δ) * ((ρE * πE + (1 - πE) / pH) * arriving_wealth - 1)\n  Ψ = x + (1 - x) * ψ\n  P = (x * ρE + (1 - x) * ψ * ρ) / Ψ\n  r = P + 1 / Ψ * (g - x * ΦE - (1 - x) * ΦH - wedge) + τ\n  μCE = r - τ -  ρE + ΦE\n  μCH = ψ * (r - τ - ρ) + ΦH\n  μx = x * (μCE - g) + (η + δ) * (ρE * πE * arriving_wealth - x) - σ * σx\n  if (iter == 0) & (μx <= 0)\n    iter += 1\n    pHx = pHx_down\n    @goto start\n  end\n  μpH = pHx / pH * μx + 0.5 * pHxx / pH * σx^2\n  pxx = - 2 * pHx + (1 - x) * pHxx\n  μp = px / p * μx + 0.5 * pxx / p * σx^2\n  μWH = μCH + μpH + σCH * σpH\n  pHt = - pH * (1 / pH + μWH - r + τ - κ * σWH)\n  σR = σ + σp\n  μR = r + κ * σR\n  σWE = σCE\n  σErelative = (αEeff - 1) * (σ + σp)\n  μErelative = μCE - (g + μp + σ * σp) - σErelative * (σ + σp)\n  α = x < 1 ? (1 - xW * αEeff) / (1 - xW) : 0.0\n  σHrelative = (α - 1) * (σ + σp)\n  μHrelative = μWH - (g + μp + σ * σp) - σHrelative * (σ + σp)\n  μxW = xW * (μCE - (g + μp + σ * σp) - σErelative * (σ + σp)) + (η + δ) * (πE * (η + δ + ϕ) / (η + δ) - xW)\n  σxW = xW * (αEeff - 1) * (σ + σp)\n  μRλ = r + λ * (μR - r)\n  σRλ = λ * σR\n  return (; pHt), (; x, μx, σx, κ, r, σWH, μWH, σp, p, px, σR, μR, μRλ, σRλ, μErelative, σErelative, μHrelative, σHrelative, μCE, μxW, σxW, pH, μCH, wedge, xW, σWE, σCH, αE = αEeff, pE = 1 / m.ρE, μp = μp)\nend\n\nWith the equation, grid, and guess in hand, pdesolve solves the stationary system:\n\nresult = pdesolve(m, stategrid, guess)","title":"The equation"},{"category":"section","location":"examples/asset_pricing/gomez/#The-solution","page":"Gomez: wealth inequality","text":"We saved the volatility of the risky return sigma_R. It differs from the fundamental volatility sigma because prices themselves move with the wealth distribution — endogenous amplification — and the size of that wedge varies with the entrepreneurs' consumption share x.\n\nxs = stategrid[:x]\nplot(xs, result.saved[:σR]; xlabel = \"entrepreneurs' consumption share x\", ylabel = \"return volatility σR\", legend = false)\n\n\n\nThis page was generated using Literate.jl.","title":"The solution"},{"category":"section","location":"examples/asset_pricing/di_tella/#Di-Tella-(2017):-uncertainty-shocks-and-balance-sheet-recessions","page":"Di Tella: balance-sheet risk","text":"A two-state general-equilibrium model with a financial sector. Experts and households have recursive preferences and an agency friction (moral hazard) forces experts to bear idiosyncratic risk. There are two states: the experts' wealth share x and a stochastic idiosyncratic risk state nu — the uncertainty shock. Three functions are solved jointly: transformed value/consumption objects p_A p_B for experts and households, and the capital price p, which is pinned down by an algebraic market-clearing constraint (is_algebraic) rather than by its own time derivative.","title":"Di Tella (2017): uncertainty shocks and balance-sheet recessions"},{"category":"section","location":"examples/asset_pricing/di_tella/#The-model","page":"Di Tella: balance-sheet risk","text":"The parameters:\n\nusing EconPDEs, Plots, Printf\n\nBase.@kwdef struct DiTellaModel\n  # Utility Function\n  γ::Float64 = 5.0        # relative risk aversion\n  ψ::Float64 = 2.0        # elasticity of intertemporal substitution\n  ρ::Float64 = 0.0665     # rate of time preference (discount rate)\n  τ::Float64 = 1.15       # transition rate between experts and households\n\n  # Technology\n  A::Float64 = 54.0       # investment adjustment-cost parameter calibrated to the paper targets\n  δ::Float64 = 0.05       # depreciation rate in the investment technology\n  B::Float64 = (0.20 - A * (0.02 + δ)^2) / (0.02 + δ)  # matches i(0.02) = 0.20\n  σ::Float64 = 0.0125     # aggregate (fundamental) volatility of capital\n\n  # MoralHazard\n  ϕ::Float64 = 0.2        # moral-hazard idiosyncratic-risk retention parameter\n\n  # Idiosyncratic\n  νbar::Float64 = 0.25    # long-run mean of idiosyncratic risk state ν\n  κν::Float64 = 1.38      # mean-reversion speed of ν (uncertainty process)\n  σνbar::Float64 = -0.17  # volatility loading of the ν process\nend","title":"The model"},{"category":"section","location":"examples/asset_pricing/di_tella/#The-state-space","page":"Di Tella: balance-sheet risk","text":"We build the grid and the initial guess first, because they fix the names used everywhere else. The grid is a NamedTuple whose keys are the two state variables (x, the experts' wealth share, and ν, the idiosyncratic risk state); the guess is a NamedTuple whose keys are the unknown functions (pA, pB, p), holding one starting value at each grid point. The transformed objects pA and pB are convenient because 1 / pA and 1 / pB are the two agents' consumption rates, and Di Tella's relative investment opportunity measure is Q = (p_Ap_B)^1(psi-1). These names are what reappear inside the equation below — e.g. pAx_up will be the forward finite difference of pA in x.\n\nTwo helpers build a grid over (x nu) and a flat guess. The nu range matches the horizontal range in Di Tella's Figure 1, and the grid explicitly includes the slices shown there: x=005010020 and nu=010025060. The parameter values below use the paper's reported calibration. The investment technology is i(g)=A(g+delta)^2+B(g+delta) with delta=005. The default A and implied B make the solved benchmark point near x=010nu=025 reproduce the paper's growth/investment targets, g simeq 002 and i simeq 020.\n\nfunction initialize_stategrid(m::DiTellaModel; xn = 30, νn = 30)\n  xs = sort(unique(vcat(collect(range(0.01, 0.99, length = xn)), [0.05, 0.10, 0.20])))\n  νs = sort(unique(vcat(collect(range(0.01, 1.00, length = νn)), [0.10, 0.25, 0.60])))\n  (; x = xs, ν = νs)\nend\n\nfunction initialize_y(m::DiTellaModel, stategrid)\n  xn = length(stategrid[:x])\n  νn = length(stategrid[:ν])\n  (; pA = 20 * ones(xn, νn), pB = 20 * ones(xn, νn), p = 20 * ones(xn, νn))\nend\n\nm = DiTellaModel()\nstategrid = initialize_stategrid(m)\nguess = initialize_y(m, stategrid)","title":"The state space"},{"category":"section","location":"examples/asset_pricing/di_tella/#The-equation","page":"Di Tella: balance-sheet risk","text":"We now write the function encoding the equilibrium conditions. Following the package convention, it takes the current state (a grid point) and u — the local bundle holding each unknown and its finite-difference derivatives there — and returns the time derivative of each unknown (pAt, pBt, pt).\n\nWith two states, both first derivatives and the cross derivative are upwinded — the first derivatives on the sign of their drifts, the cross term on the sign of the x–nu covariance.\n\nfunction (m::DiTellaModel)(state::NamedTuple, u::NamedTuple)\n  (; γ, ψ, ρ, τ, A, δ, B, σ, ϕ, νbar, κν, σνbar) = m\n  (; x, ν) = state\n  (; pA, pAx_up, pAx_down, pAν_up, pAν_down, pAxx, pAxν_up, pAxν_down, pAνν, pB, pBx_up, pBx_down, pBν_up, pBν_down, pBxx, pBxν_up, pBxν_down, pBνν, p, px_up, px_down, pν_up, pν_down, pxx, pxν_up, pxν_down, pνν) = u\n\n  # drift and volatility of state variable ν\n  q = (p - B) / (2 * A)\n  g = q - δ\n  i = A * q^2 + B * q\n  μν = κν * (νbar - ν)\n  σν = σνbar * sqrt(ν)\n  pAν = (μν >= 0) ? pAν_up : pAν_down\n  pBν = (μν >= 0) ? pBν_up : pBν_down\n  pν = (μν >= 0) ? pν_up : pν_down\n\n  pAx, pBx, px = pAx_up, pBx_up, px_up\n  iter = 0\n  @label start\n  σX = x * (1 - x) * (1 - γ) / (γ * (ψ - 1)) * (pAν / pA - pBν / pB) * σν / (1 - x * (1 - x) * (1 - γ) / (γ * (ψ - 1)) * (pAx / pA - pBx / pB))\n  σpA = pAx / pA * σX + pAν / pA * σν\n  σpB = pBx / pB * σX + pBν / pB * σν\n  σp = px / p * σX + pν / p * σν\n  κ = (σp + σ - (1 - γ) / (γ * (ψ - 1)) * (x * σpA + (1 - x) * σpB)) / (1 / γ)\n  # κidio is the price of idiosyncratic risk (distinct from the parameter κν, the mean-reversion speed of ν).\n  κidio = γ * ϕ * ν / x\n  σA = κ / γ + (1 - γ) / (γ * (ψ - 1)) * σpA\n  νA = κidio / γ\n  σB = κ / γ + (1 - γ) / (γ * (ψ - 1)) * σpB\n\n  # Interest rate r\n  μX = x * (1 - x) * ((σA * κ + νA * κidio - 1 / pA - τ) - (σB * κ -  1 / pB + τ * x / (1 - x)) - (σA - σB) * (σ + σp))\n\n  # upwinding\n  if (iter == 0) && (μX <= 0)\n    iter += 1\n    pAx, pBx, px = pAx_down, pBx_down, px_down\n    @goto start\n  end\n\n  # upwind the cross derivative on the sign of its coefficient σX * σν (the x-ν covariance)\n  pAxν = (σX * σν >= 0) ? pAxν_up : pAxν_down\n  pBxν = (σX * σν >= 0) ? pBxν_up : pBxν_down\n  pxν = (σX * σν >= 0) ? pxν_up : pxν_down\n\n  μpA = pAx / pA * μX + pAν / pA * μν + 0.5 * pAxx / pA * σX^2 + 0.5 * pAνν / pA * σν^2 + pAxν / pA * σX * σν\n  μpB = pBx / pB * μX + pBν / pB * μν + 0.5 * pBxx / pB * σX^2 + 0.5 * pBνν / pB * σν^2 + pBxν / pB * σX * σν\n  μp = px / p * μX + pν / p * μν + 0.5 * pxx / p * σX^2 + 0.5 * pνν / p * σν^2 + pxν / p * σX * σν\n  r = (1 - i) / p + g + μp + σ * σp - κ * (σ + σp) - γ / x * (ϕ * ν)^2\n\n  # Market Pricing\n  pAt = - pA * (1 / pA  + (ψ - 1) * τ / (1 - γ) * ((pA / pB)^((1 - γ) / (1 - ψ)) - 1) - ψ * ρ + (ψ - 1) * (r + κ * σA + κidio * νA) + μpA - (ψ - 1) * γ / 2 * (σA^2 + νA^2) + (2 - ψ - γ) / (2 * (ψ - 1)) * σpA^2 + (1 - γ) * σpA * σA)\n  pBt = - pB * (1 / pB - ψ * ρ + (ψ - 1) * (r + κ * σB) + μpB - (ψ - 1) * γ / 2 * σB^2 + (2 - ψ - γ) / (2 * (ψ - 1)) * σpB^2 + (1 - γ) * σpB * σB)\n  # algebraic constraint\n  pt = - p * ((1 - i) / p - x / pA - (1 - x) / pB)\n  Q = (pA / pB)^(1 / (ψ - 1))\n  return (; pAt, pBt, pt), (; σx = σX, Q, r, κ, σp, growth = g, investment = i)\nend\n\np enters as an algebraic (constraint) variable:\n\nresult = pdesolve(m, stategrid, guess; is_algebraic = (; pA = false, pB = false, p = true))","title":"The equation"},{"category":"section","location":"examples/asset_pricing/di_tella/#The-solution","page":"Di Tella: balance-sheet risk","text":"We reproduce the three panels in Di Tella's Figure 1: the capital price p, the volatility of experts' wealth share sigma_x, and relative investment opportunities Q. Each object is plotted both as a function of uncertainty nu at fixed balance sheets and as a function of experts' wealth share x at fixed uncertainty. The key Di Tella mechanism is that an uncertainty shock raises the idiosyncratic risk experts must retain. When experts' balance sheets are weak (low x), their risk-bearing capacity is low, so the same increase in nu produces a larger fall in the capital price and a larger balance-sheet response.\n\nxs = stategrid[:x]\nνs = stategrid[:ν]\np = result.zero[:p]\nσx = result.saved[:σx]\nQ = result.saved[:Q]\nline_styles = [:solid, :dot, :dash]\nx_slices = [0.05, 0.10, 0.20]\nν_slices = [0.10, 0.25, 0.60]\n\npν = plot(; xlabel = \"risk state ν\", ylabel = \"capital price p\",\n          xlims = (0, 1), ylims = (5.6, 7.2), legend = :bottomleft)\nσxν = plot(; xlabel = \"risk state ν\", ylabel = \"volatility σx\",\n           xlims = (0, 1), ylims = (0, 0.01), legend = false)\nQν = plot(; xlabel = \"risk state ν\", ylabel = \"relative opportunities Q\",\n          xlims = (0, 1), ylims = (1.0, 2.7), legend = false)\nfor (x_slice, line_style) in zip(x_slices, line_styles)\n  x_index = findfirst(==(x_slice), xs)\n  plot!(pν, νs, p[x_index, :]; label = @sprintf(\"x = %.2f\", x_slice),\n        color = :black, linestyle = line_style)\n  plot!(σxν, νs, σx[x_index, :]; color = :black, linestyle = line_style)\n  plot!(Qν, νs, Q[x_index, :]; color = :black, linestyle = line_style)\nend\npx = plot(; xlabel = \"wealth share x\", ylabel = \"capital price p\",\n          xlims = (0, 1), ylims = (5.6, 7.2), legend = :bottomright)\nσxx = plot(; xlabel = \"wealth share x\", ylabel = \"volatility σx\",\n           xlims = (0, 1), ylims = (0, 0.01), legend = false)\nQx = plot(; xlabel = \"wealth share x\", ylabel = \"relative opportunities Q\",\n          xlims = (0, 1), ylims = (1.0, 2.7), legend = false)\nfor (ν_slice, line_style) in zip(ν_slices, line_styles)\n  ν_index = findfirst(==(ν_slice), νs)\n  plot!(px, xs, p[:, ν_index]; label = @sprintf(\"ν = %.2f\", ν_slice),\n        color = :black, linestyle = line_style)\n  plot!(σxx, xs, σx[:, ν_index]; color = :black, linestyle = line_style)\n  plot!(Qx, xs, Q[:, ν_index]; color = :black, linestyle = line_style)\nend\nplot(pν, σxν, Qν, px, σxx, Qx; layout = (2, 3), size = (940, 560),\n     left_margin = 8Plots.mm, bottom_margin = 8Plots.mm, right_margin = 4Plots.mm)\n\n\n\nThis page was generated using Literate.jl.","title":"The solution"},{"category":"section","location":"examples/neoclassical_growth/#Neoclassical-growth-model","page":"Neoclassical growth","text":"The deterministic neoclassical (Ramsey–Cass–Koopmans) growth model is the gentlest introduction: one state variable, one value function, and a familiar calibration. For validation, the example creates a second parameter instance that satisfies a closed-form restriction. A representative household chooses consumption to solve\n\nmax_c int_0^infty e^-rho t fracc^1-gamma1-gamma dt\nqquad textsubject to qquad dot k = A k^alpha - delta k - c\n\nand the value function v(k) solves the Hamilton–Jacobi–Bellman equation\n\nrho v(k) = max_c fracc^1-gamma1-gamma + v(k)bigl(A k^alpha - delta k - cbigr)\n\nwith first-order condition c = v(k)^-1gamma.\n\nThis is the same model solved step by step in Getting started. Here it is packaged the way the rest of the examples are — parameters in a struct, the equation as a callable on it — and the numerical solution is plotted against the closed-form benchmark.","title":"Neoclassical growth model"},{"category":"section","location":"examples/neoclassical_growth/#The-model","page":"Neoclassical growth","text":"The parameters live in a struct:\n\nusing EconPDEs, Plots, Printf\n\nBase.@kwdef struct NeoclassicalGrowthModel\n    A::Float64 = 0.5     # productivity\n    α::Float64 = 0.3     # capital share\n    δ::Float64 = 0.05    # depreciation\n    ρ::Float64 = 0.05    # discount rate\n    γ::Float64 = 2.0     # relative risk aversion\nend","title":"The model"},{"category":"section","location":"examples/neoclassical_growth/#The-state-space","page":"Neoclassical growth","text":"We build the grid and the initial guess first, because they fix the names used everywhere else. The grid is a NamedTuple whose key is the state variable (k); the guess is a NamedTuple whose key is the unknown function (v), holding one starting value at each grid point. These names are what reappear inside the equation below — e.g. vk_up will be the forward finite difference of v in k.\n\nThe default parameters above match the introductory calibration. To validate the full numerical solution, we solve a new model instance that imposes the restriction with a closed-form solution:\n\nrho = delta(alpha gamma - 1) qquad alphagamma  1\n\nIn that case the optimal policy and value function are\n\nc(k) = phi A k^alpha qquad\nv(k) = frac(phi A)^-gamma k^1-alphagamma1-alphagamma\nqquad phi = 1 - frac1gamma\n\nWe center the grid on the corresponding steady state bar k (where alpha A bar k^alpha-1 = rho + delta) and still start from the generic value of consuming gross output forever. The closed-form instance is used only as a validation benchmark.\n\nm = NeoclassicalGrowthModel()\nγ_benchmark = 4.0\nm_benchmark = NeoclassicalGrowthModel(;\n    γ = γ_benchmark,\n    ρ = m.δ * (m.α * γ_benchmark - 1),\n)\n(; A, α, δ, ρ, γ) = m_benchmark\nφ = 1 - 1 / γ\nk̄ = (α * A / (ρ + δ))^(1 / (1 - α))\nstategrid = (; k = range(0.1 * k̄, 5 * k̄, length = 1000))\nguess = (; v = [(A * k^α)^(1 - γ) / (1 - γ) / ρ for k in stategrid[:k]])\n\nclosed_form_consumption(k) = φ * A * k^α\nclosed_form_value(k) = (φ * A)^(-γ) * k^(1 - α * γ) / (1 - α * γ)","title":"The state space"},{"category":"section","location":"examples/neoclassical_growth/#The-equation","page":"Neoclassical growth","text":"We now write the function encoding the HJB equation. Following the package convention, it takes the current state (a grid point) and u — the local bundle holding each unknown and its finite-difference derivatives there (here v, vk_up, vk_down) — and returns the time derivative vt of each unknown.\n\nThe capital drift dot k can point either way, so the first derivative is upwinded: forward (vk_up) where the implied drift is positive, backward (vk_down) where it is negative, and the consumption that sets the drift to zero at the steady state in between. A second NamedTuple saves consumption c and the drift μk on the grid.\n\nThe return value follows the package's sign convention: vt = -(RHS - ρv) is the time derivative that makes the time-dependent equation rho v = textRHS + partial_t v hold, and pdesolve integrates this false transient until it stops moving — with this sign the iteration converges to the stationary solution; with the opposite sign it diverges.\n\nfunction (m::NeoclassicalGrowthModel)(state::NamedTuple, u::NamedTuple)\n    (; A, α, δ, ρ, γ) = m\n    (; k) = state\n    (; v, vk_up, vk_down) = u\n    c_up = vk_up >= 0 ? min(vk_up^(-1 / γ), 10 * A * k^α) : 10 * A * k^α\n    μk_up = A * k^α - δ * k - c_up\n    if μk_up > 0\n        c, vk, μk = c_up, vk_up, μk_up\n    else\n        c_down = vk_down >= 0 ? min(vk_down^(-1 / γ), 10 * A * k^α) : 10 * A * k^α\n        μk_down = A * k^α - δ * k - c_down\n        if μk_down < 0\n            c, vk, μk = c_down, vk_down, μk_down\n        else\n            μk = 0.0\n            c = A * k^α - δ * k\n            vk = c^(-γ)\n        end\n    end\n    vt = -(c^(1 - γ) / (1 - γ) + μk * vk - ρ * v)\n    return (; vt), (; c, μk)\nend\n\nWith the equation, grid, and guess in hand, pdesolve solves the stationary system:\n\nresult = pdesolve(m_benchmark, stategrid, guess)","title":"The equation"},{"category":"section","location":"examples/neoclassical_growth/#The-solution","page":"Neoclassical growth","text":"Since this calibration has an analytical solution, the example can check the whole numerical policy and value function, not just the steady state. The errors below are not zero because the PDE is solved on a finite-difference grid, but they should be small and shrink as the grid is refined.\n\nks = stategrid[:k]\nc_closed = closed_form_consumption.(ks)\nv_closed = closed_form_value.(ks)\n\npolicy_error = maximum(abs.(result.saved[:c] .- c_closed) ./ c_closed)\nvalue_error = maximum(abs.(result.zero[:v] .- v_closed) ./ abs.(v_closed))\n@printf(\"maximum relative policy error: %.2e\\n\", policy_error)\n@printf(\"maximum relative value error: %.2e\\n\", value_error)\n\nplot(ks, result.zero[:v]; xlabel = \"capital k\", ylabel = \"value v(k)\",\n     label = \"numerical\")\nplot!(ks, v_closed; linestyle = :dash, label = \"closed form\")\n\nConsumption rises with capital (left). The capital drift mu_k (right) is positive below the steady state and negative above it, crossing zero exactly at bar k — the saddle-path-stable steady state the economy is drawn toward.\n\np1 = plot(ks, result.saved[:c]; xlabel = \"capital k\", ylabel = \"consumption c(k)\",\n          label = \"numerical\")\nplot!(p1, ks, c_closed; linestyle = :dash, label = \"closed form\")\np2 = plot(ks, result.saved[:μk]; xlabel = \"capital k\", ylabel = \"capital drift μk\", legend = false)\nhline!(p2, [0.0]; color = :gray, linestyle = :dash)\nvline!(p2, [k̄]; color = :red, linestyle = :dot)\nplot(p1, p2; layout = (1, 2), size = (800, 300))\n\n\n\nThis page was generated using Literate.jl.","title":"The solution"},{"category":"section","location":"examples/consumption_saving/consumption_saving_diffusion_income/#Achdou–Han–Lasry–Lions–Moll:-consumption–saving-with-diffusion-income","page":"Consumption–saving: diffusion income","text":"This is the diffusive-income version of the Achdou–Han–Lasry–Lions–Moll (2022) consumption– saving problem, the standard building block of continuous-time heterogeneous-agent (HACT) models. A household earns stochastic labor income y, saves in a single riskless asset a at rate r, and cannot borrow past a limit a ge a_min. Income mean-reverts as a diffusion, and the value function v(y a) solves the Hamilton–Jacobi–Bellman equation\n\nrho v = max_c fracc^1-gamma1-gamma\n  + partial_a vbigl(y + r a - cbigr)\n  + partial_y vkappa_y(bar y - y)\n  + tfrac12 sigma_y^2 partial_yy v\n\nwith first-order condition c = (partial_a v)^-1gamma and the borrowing constraint a ge a_min enforced as a state constraint (saving cannot be negative at the limit).","title":"Achdou–Han–Lasry–Lions–Moll: consumption–saving with diffusion income"},{"category":"section","location":"examples/consumption_saving/consumption_saving_diffusion_income/#The-model","page":"Consumption–saving: diffusion income","text":"The parameters live in a struct:\n\nusing EconPDEs, Distributions, Plots\n\nBase.@kwdef struct AchdouHanLasryLionsMollModel_Diffusion\n    κy::Float64 = 0.1      # income mean-reversion speed\n    ybar::Float64 = 1.0    # mean income level\n    σy::Float64 = 0.07     # income volatility\n    r::Float64 = 0.03      # risk-free rate\n    ρ::Float64 = 0.05      # discount rate\n    γ::Float64 = 2.0       # relative risk aversion\n    amin::Float64 = 0.0    # borrowing limit\n    amax::Float64 = 100.0  # top of the asset grid\nend","title":"The model"},{"category":"section","location":"examples/consumption_saving/consumption_saving_diffusion_income/#The-state-space","page":"Consumption–saving: diffusion income","text":"We build the grid and the initial guess first, because they fix the names used everywhere else. The grid is a NamedTuple whose keys are the two state variables (y and a); the guess is a NamedTuple whose key is the unknown function (v), holding one starting value at each grid point. These names are what reappear inside the equation below — e.g. va_up will be the forward finite difference of v in a.\n\nIncome lives on a grid spanning the bulk of its ergodic (Gamma) distribution, and assets on a grid from the borrowing limit up to a_max. The initial guess is the value of consuming the annuity value of total wealth a + yr forever.\n\nm = AchdouHanLasryLionsMollModel_Diffusion()\ndistribution = Gamma(2 * m.κy * m.ybar / m.σy^2, m.σy^2 / (2 * m.κy))\nstategrid = (; y = range(quantile(distribution, 0.001), quantile(distribution, 0.999), length = 10),\n                        a =  range(m.amin, m.amax, length = 1000)\n                        )\nguess = (; v = [(m.ρ / m.γ + (1 - 1 / m.γ) * m.r)^(-m.γ) * (a + y / m.r)^(1 - m.γ) / (1 - m.γ) for y in stategrid[:y], a in stategrid[:a]])","title":"The state space"},{"category":"section","location":"examples/consumption_saving/consumption_saving_diffusion_income/#The-equation","page":"Consumption–saving: diffusion income","text":"We now write the function encoding the HJB equation. Following the package convention, it takes the current state (a grid point) and u (each unknown together with its finite-difference derivatives there) and returns the time derivative of each unknown.\n\nIncome drift is exogenous, so its derivative is upwinded on the sign of mu_y. The asset drift mu_a = y + r a - c is endogenous: we try the forward derivative, fall back to the backward one, and — when both fail or the borrowing constraint binds — set mu_a = 0 and consume out of income and interest. We save the resulting saving rate mu_a on the grid.\n\nfunction (m::AchdouHanLasryLionsMollModel_Diffusion)(state::NamedTuple, u::NamedTuple)\n    (; κy, σy, ybar, r, ρ, γ, amin, amax) = m\n    (; y, a) = state\n    (; v, vy_up, vy_down, va_up, va_down, vyy, vaa) = u\n    μy = κy * (ybar - y)\n    # Newton can try negative marginal values, so cap implied consumption instead of flooring derivatives.\n    cmax = 100.0 * (y + r * max(a, 0.0))\n\n    # upwinding for income direction (easy because exogenous income drift)\n    vy = (μy >= 0) ? vy_up : vy_down\n\n    # upwinding for asset direction (harder because endogeneous asset drift)\n    c_up = va_up > 0 ? min(va_up^(-1 / γ), cmax) : cmax\n    μa_up = y + r * a - c_up\n    if μa_up >= 0.0\n        va = va_up\n        c = c_up\n        μa = μa_up\n    else\n        c_down = va_down > 0 ? min(va_down^(-1 / γ), cmax) : cmax\n        μa_down = y + r * a - c_down\n        if (μa_down <= 0.0) && (a > amin)\n            va = va_down\n            c = c_down\n            μa = μa_down\n        else\n            # If the two candidates straddle zero OR drift is negative at minimum asset threshold\n            # (i.e. borrowing constraint), then, we must have drift μa = 0.\n            c = y + r * a\n            va = c^(-γ)\n            μa = 0.0\n        end\n    end\n    vt = - (c^(1 - γ) / (1 - γ) + μa * va + μy * vy + 0.5 * vyy * σy^2 - ρ * v)\n    return (; vt), (; μa, c)\nend\n\nWith the equation, grid, and guess in hand, pdesolve solves the stationary system:\n\nresult = pdesolve(m, stategrid, guess)","title":"The equation"},{"category":"section","location":"examples/consumption_saving/consumption_saving_diffusion_income/#The-solution","page":"Consumption–saving: diffusion income","text":"We plot policies against assets for three income levels (low, median, high). Consumption is recovered from the budget identity c = y + r a - mu_a.\n\nas = stategrid[:a]\nys = stategrid[:y]\nidx = 1:div(length(as), 3)          # left third of the asset grid, where the curvature is\niys = round.(Int, range(1, length(ys), length = 3))\nμa = result.saved[:μa]\nc = result.saved[:c]\n\np1 = plot(xlabel = \"assets a\", ylabel = \"consumption c\")\nfor iy in iys\n    plot!(p1, as[idx], c[iy, idx], label = \"y = $(round(ys[iy], digits = 2))\")\nend\np2 = plot(xlabel = \"assets a\", ylabel = \"saving μa\")\nfor iy in iys\n    plot!(p2, as[idx], μa[iy, idx], label = \"y = $(round(ys[iy], digits = 2))\")\nend\nhline!(p2, [0.0]; color = :gray, linestyle = :dash, label = \"\")\nplot(p1, p2; layout = (1, 2), size = (800, 300))\n\nConsumption rises with both assets and income (left). Saving (right) tells the story of the borrowing constraint: at the limit a = a_min a low-income household would like to borrow but cannot, so its saving is pinned at zero and it consumes all of its income and interest. Because households are impatient relative to the interest rate (rho  r), saving turns negative at high wealth — every income level has a finite target above which it runs its assets down — which is exactly what keeps the stationary wealth distribution from spreading without bound.\n\n\n\nThis page was generated using Literate.jl.","title":"The solution"},{"category":"section","location":"examples_overview/#Overview","page":"Overview","text":"Each example solves a published model end to end — build the grid and the guess, write the PDE function, solve, plot — and explains the solution economically. They are grouped by field and ordered roughly from the simplest to the most involved. The table flags what each example adds beyond the baseline workflow — nothing, for the simplest ones.\n\nExample What's special\nNeoclassical growth \nConsumption–saving: two income states borrowing constraint + Poisson switching (two unknown functions)\nConsumption–saving: diffusion income 2D + borrowing constraint\nConsumption–saving: risky asset 2D + borrowing constraint + custom boundary conditions (bc)\nWang–Wang–Yang: liquidity management borrowing constraint + custom boundary conditions (bc)\nCampbell–Cochrane: habit \nWachter: rare disasters \nBansal–Yaron: long-run risk 2D\nHaddad: endogenous volatility 2D\nTuckman–Vila: finite horizon time-dependent (finite horizon)\nGârleanu–Panageas: heterogeneous agents four unknown functions\nHe–Krishnamurthy: intermediaries \nBrunnermeier–Sannikov: macro-finance two unknown functions + inner static root-find\nDi Tella: balance-sheet risk 2D + three unknown functions + algebraic equations (is_algebraic)\nGomez: wealth inequality \nLeland: optimal default variational inequality (lower_bound)\nBolton–Chen–Wang: financing constraint free boundary (outer solve on bc)","title":"Overview"},{"category":"section","location":"time_dependent/#Time-dependent-problems","page":"Time-dependent problems","text":"For a time-dependent problem — a transition path, a finite horizon, a deterministic change in parameters — pass an increasing time grid as the fourth positional argument:\n\nτs = range(0, 100, length = 50)\nresult = pdesolve(f, stategrid, guess, τs)\n\npdesolve solves backward over this grid: guess is the terminal value at τs[end], and each step solves one implicit time step of partial_t v = textvt — the same equation, and the same sign convention, as the stationary false transient (see Writing the PDE function). The result is one solution per time point: result.zero[i][:v] is the value function at time τs[i], and result.residual_norm[i] the corresponding residual.","title":"Time-dependent problems"},{"category":"section","location":"time_dependent/#Time-varying-equations","page":"Time-dependent problems","text":"If the equation itself depends on time, define the PDE function with a third argument. For example, productivity following a deterministic path:\n\nfunction hjb(state::NamedTuple, u::NamedTuple, t)\n    A_t = A * (1 + 0.1 * exp(-0.05 * t))\n    # ... same as the stationary function, with A_t in place of A ...\n    return (; vt)\nend\n\nIf the PDE function has only two arguments, the same equation is used at every time on the grid. The third argument must be left untyped or typed loosely (e.g. t or t::Number) — pdesolve detects it with hasmethod(f, Tuple{NamedTuple, NamedTuple, Number}).","title":"Time-varying equations"},{"category":"section","location":"time_dependent/#Typical-uses","page":"Time-dependent problems","text":"Finite-horizon problems: the terminal value is known (e.g. zero, or a bequest value) and the object of interest is the whole path. See Tuckman-Vila.\nTransition dynamics: solve the stationary problem first, then use it as the terminal value of a long time grid to trace convergence from an initial condition.\nA robust path to a hard stationary solution: when the stationary solve fails from a poor guess, integrating the time-dependent equation over a long horizon is a natural continuation method — it follows the physically meaningful transient.","title":"Typical uses"},{"category":"section","location":"solver/#Solver-and-troubleshooting","page":"Solver and troubleshooting","text":"","title":"Solver and troubleshooting"},{"category":"section","location":"solver/#How-pdesolve-finds-the-solution","page":"Solver and troubleshooting","text":"pdesolve first turns the local PDE into a finite system. Start from a time-dependent equation such as\n\npartial_t V(x t) = f(x V V_x V_xx)\n\nAfter choosing a state grid, the values of V on the grid are stacked into a vector y. Finite differences approximate V_x and V_{xx}. Evaluating the user's equation at every grid point gives a vector field\n\ndot y = F(y)\n\nFor a stationary problem, the goal is simply\n\nF(y) = 0\n\npdesolve finds this zero in three steps:\n\nBuild the finite system with upwind finite differences.\nSolve backward implicit false-transient steps. In a linear valuation problem this is a linear M-matrix solve; when policies or equilibrium objects depend on the new value, the package solves the nonlinear equation with a nonlinear solver, Newton by default.\nAdapt Δ: grow it when the stationary residual falls, and shrink it when a step fails or the residual rises.\n\nThe basic equation in step 2 is a backward implicit time step:\n\nfracy_n+1 - y_nDelta = F(y_n+1)\n\nWhen this equation is nonlinear, pdesolve can use different nonlinear solvers. The default is Newton's method. With algebraic equations (is_algebraic = true), the corresponding rows skip the time-step term and are solved as static equations.\n\nThe size of Δ controls how aggressive the step is. Small Δ makes the equation close to y_n+1 = y_n, so the step is cautious but progress is slow. Large Δ makes the time-step term negligible; in nonlinear problems, the step approaches a direct Newton solve of F(y) = 0. The outer continuation loop in step 3 starts cautiously and adapts:\n\nStarting from the guess, solve one implicit step of size Δ.\nIf the inner solve converges, accept the step; grow Δ when the residual falls, and shrink it when the residual rises.\nIf the inner solve fails, reject the step, shrink Δ by 10, and retry. Regrowth is then capped below the failed Δ until the residual has clearly improved, so a Δ that just failed is not immediately retried.\nStop when the residual norm falls below maxdist.\n\nThis adaptive scheme is pseudo-transient continuation, imported from computational fluid dynamics; formal convergence conditions are given in Kelley and Keyes (1998).\n\nWith verbose = true (the default), pdesolve prints what it is solving, one line per outer iteration, and a convergence summary (this is the habit-formation model of the example gallery):\n\nSolving for 1 unknown (p) on a 998-point grid\nIter TimeStep Residual\n---- -------- --------\n   1 1.00e+00 2.51e-02\n   2 1.12e+01 1.21e-02\n   3 2.33e+03 8.01e-05\n   4 3.50e+08 3.45e-12\nConverged after 4 time steps (4ms)\n\nTimeStep is the current Δ and Residual the norm of the stationary residual at the accepted step. A step is kept even when its residual rises; the next Δ then shrinks (it grows when the residual falls). A ✗ in place of the residual means the inner nonlinear solve failed at that Δ: no step is taken, so there is no residual to show, and the step is retried with Δ/10 — the unusual causes, the model returned NaN or a singular Jacobian, are flagged in parentheses:\n\n   2 1.04e+01        ✗\n\nHealthy solves show Δ growing geometrically and the residual collapsing in the last few time steps.\n\nWith verbose = false a successful solve prints nothing, and convergence failures are still reported with @warn (with a hint at the likely cause) — so pdesolve can run inside a loop, e.g. an estimation, without flooding the log while failures still surface. Either way, the returned result records whether the solve converged:\n\njulia> result\nEconPDEResult\n  zero:          p (998)\n  residual_norm: 3.45e-12\n  converged:     true (tolerance 1.49e-08)","title":"How pdesolve finds the solution"},{"category":"section","location":"solver/#Why-the-method-works","page":"Solver and troubleshooting","text":"Upwinding makes the discretized problem an economy. With first derivatives taken from the side the drift points to, the discretized equation at a grid point reads\n\nrho v_i = u_i + lambda_i^+ (v_i+1 - v_i) + lambda_i^- (v_i-1 - v_i)\nqquad lambda_i^pm = fracmu_i^pmDelta x + fracsigma_i^22Delta x^2 ge 0\n\nwhich is the valuation equation of a continuous-time Markov chain on the grid: the drift becomes a directed jump intensity, the variance a symmetric one. A wrong-direction stencil makes some lambda negative — the discrete model assigns negative probabilities and the false transient explodes; that is what the monotonicity diagnostic detects. In the numerical-PDE literature this nonnegativity is called monotonicity, and monotone consistent stable schemes converge to the (viscosity) solution of the PDE as the grid is refined (Barles and Souganidis, 1991); the chain construction itself is Kushner and Dupuis's Markov chain approximation method.\n\nCase 1: Linear valuation problem. Take the law of motion, payoff, and discount rate as given while solving for the value function. Equivalently, write F(y) = u + Ay - rho y with A the transition-intensity matrix of the discretized state process. The implicit step is then a linear system, not a nonlinear problem. It has an exact probabilistic meaning:\n\ny_n+1(x) = mathbbEleftint_0^T e^-rho t u(X_t) dt + e^-rho T y_n(X_T)\nmiddle X_0 = xright qquad T sim textExponential(1Delta)\n\nOne step values the model exactly out to a random horizon with mean Δ, then hands over to the previous iterate as continuation value. The error therefore contracts by the average discount factor mathbbEe^-rho T = 1(1+rhoDelta) per step — Blackwell's value-function-iteration argument, with 1(1+rhoDelta) as the discount factor. The step is stable for any Δ because its matrix is an M-matrix. An explicit step y_n + Delta F(y_n) is instead one round of value function iteration with period Δ, and the period is capped by the grid (Delta lesssim Delta x^2sigma^2, the CFL condition) — on fine grids, that iteration crawls.\n\nSolving nonlinear steps. When F depends on the new candidate value through policies, prices, or equilibrium objects, the backward implicit step is a nonlinear equation. pdesolve can use different nonlinear solvers; the default is Newton's method, with a trust-region variant available for harder systems. Let the residual of one implicit step be\n\nR_Delta(WV_n) = fracW-V_nDelta - F(W)\n\nGiven an inner iterate W_k, Newton computes the residual and Jacobian, solves\n\nD R_Delta(W_k) delta_k = -R_Delta(W_k)\n\nand updates W_k+1=W_k+delta_k, with safeguards when needed. The inner loop stops when the residual of the implicit step is small. The outer loop then decides whether to accept the step and how to change Δ.\n\nCase 2: Pure HJBs. A pure HJB adds an optimizer to the linear valuation problem. Once a policy c is fixed, the equation is again a linear valuation equation; the nonlinearity comes from the fact that the optimal policy itself depends on the candidate value. Let A(c) denote the finite-difference operator obtained after fixing policy c. Given the previous outer iterate V_n, the fully implicit step asks for a new value W satisfying\n\nfracW - V_nDelta =\nmax_c leftu(c) + A(c)W - rho Wright\n\nThis is a nonlinear problem in W because a new candidate value changes marginal values and therefore the optimal policy c.\n\nFor this pure HJB, Newton has a familiar interpretation. At Newton iterate W_k, compute the policy c_k that is optimal for W_k. By the envelope theorem, the terms involving the derivative of c_k with respect to W_k drop from the Jacobian. The Newton step therefore holds the policy fixed and solves the linear policy-evaluation problem\n\nfracW_k+1 - V_nDelta =\nu(c_k) + A(c_k) W_k+1 - rho W_k+1\n\nThe policy is then recomputed at W_k+1, and the process repeats until the nonlinear implicit equation is solved. This is the policy-iteration logic inside the implicit step.\n\nThe comparison to Achdou et al. (2022) is now direct. Their update is semi-implicit for the nonlinear HJB: it computes the policy from the old value V_n, freezes it, and solves one linear system. Equivalently, it is the first frozen-policy Newton step on the fully implicit nonlinear equation, started from W_0 = V_n. pdesolve iterates the inner Newton solve to convergence, so the final policy is optimal for the new value function, not just for the old guess.\n\nIn discrete-time dynamic-programming terms, Achdou's semi-implicit step is one modified-policy-iteration step: choose the policy from V_n, then partially or fully evaluate it. Small Δ looks like value function iteration; intermediate Δ is modified policy iteration; and Delta=infty is Howard policy iteration.\n\nThis interpretation is useful for judging the comparison. Achdou's method is a strong semi-implicit policy-iteration baseline for clean pure HJBs. pdesolve can be more forgiving with poor guesses or aggressive Δ because it reoptimizes inside the implicit step and accepts a large step only after the policy is consistent with the new value. Still, the larger robustness gain appears in Case 3, where freezing equilibrium objects is no longer policy iteration.\n\nCase 3: Equilibrium systems. In asset-pricing and general-equilibrium models, the residual includes prices, risk premia, volatilities, market-clearing equations, and sometimes algebraic constraints. These objects are not maximized-away controls, so the envelope theorem does not remove their derivatives from the Jacobian. Freezing them gives a semi-implicit fixed-point iteration, not Newton's method. pdesolve instead treats the whole discretized equilibrium as a sparse nonlinear system and solves that system with Newton by default, with a trust-region variant available for harder cases.\n\nThe time step controls the regime. The role of Δ is separate from the three cases above. For finite Δ, each Newton or policy-evaluation step is anchored to the previous outer iterate V_n. As Delta to infty the anchor disappears. In a linear valuation problem, this is just the stationary linear system. In a pure stationary HJB, the Newton loop becomes Howard policy iteration: improve the policy, evaluate it, repeat. In an equilibrium system, the same limit is a direct Newton solve of the full stationary residual. Growing Δ adaptively therefore moves the solver from stabilized iteration toward a fast, local stationary solve.\n\nThe same ideas travel under different names across fields:\n\ningredient here numerical analysis / CFD discrete-time dynamic programming\nupwind differences monotone scheme Markov chain approximation\nexplicit step forward Euler (CFL-capped) value function iteration\nimplicit step backward Euler (stable for all Δ) valuation to a random horizon of mean Δ\nΔ = Inf stationary nonlinear solve (Newton by default) Howard policy iteration for HJBs; direct Newton for equilibrium systems\nadaptive Δ pseudo-transient continuation value iteration morphing into policy iteration or Newton\n\nIn the nonlinear cases, the extra Newton work is cheap enough to be useful because the Jacobian is sparse. For one-, two-, and three-state problems, pdesolve builds the sparsity pattern from the local stencil and computes the Jacobian by colored finite differences. Recomputing this Jacobian lets the solver take much larger accepted Δ steps, so the benefit is speed as well as robustness.\n\nA fuller writeup with references is the method note in the repository: examples/details.pdf.","title":"Why the method works"},{"category":"section","location":"solver/#Solver-options","page":"Solver and troubleshooting","text":"All of these are keyword arguments of pdesolve (forwarded to finiteschemesolve):\n\nKeyword Default Meaning\nΔ 1.0 initial pseudo-time step; Inf = stationary nonlinear solve, no continuation\nscale 10.0 growth factor of Δ after a successful step\nminΔ, maxΔ 1e-9, Inf bounds on Δ; the solve aborts when Δ < minΔ\niterations 100 maximum outer iterations\nmaxdist sqrt(eps()) convergence tolerance on the residual norm\ninner_iterations 10 nonlinear-solver iterations per implicit time step\ninnerdist sqrt(eps()) tolerance of the inner nonlinear solve\nmethod :newton :newton or :trust_region\nautodiff :forward :forward/:finite (forward differences) or :central; with 1-3 states the Jacobian always uses colored sparse finite differences\nverbose true print the problem summary, the iteration table, and a convergence summary; failures warn even when false\nlower_bound, upper_bound -Inf, Inf bounds for variational inequalities (see Optimal stopping)\nis_algebraic all false mark equations with no time derivative (static/algebraic conditions)","title":"Solver options"},{"category":"section","location":"solver/#When-a-solve-fails","page":"Solver and troubleshooting","text":"Roughly in order of likelihood:\n\nIt diverges immediately (residual grows from iteration 1). Check the sign convention: the PDE function must return vt = -(RHS - ρv), not the raw residual. See Writing the PDE function. pdesolve detects the typical signature automatically and warns (see the monotonicity diagnostic below).\nNaN at the initial value. pdesolve throws ArgumentError: G! returns NaN with the initial value: the PDE function hit an invalid operation at the guess (negative marginal value raised to a power, division by zero). Guard the FOC against nonpositive derivatives — e.g. cap the implied policy, as the examples do with cmax — and check the guess itself is in the function's domain.\nThe residual stalls and Δ keeps shrinking until minΔ. The classic symptom of a non-monotone scheme: an upwind direction chosen with the wrong sign, or a missing directional cross derivative. Run with check_monotonicity = true (below). If the scheme is clean, improve the initial guess — a closed-form limit of the model (no risk, log utility, unconstrained) is usually enough.\nThe inner nonlinear solve keeps failing (many rejected lines). Try method = :trust_region, a smaller initial Δ (e.g. 1e-2 for stiff systems with several coupled unknowns), or loosen inner_iterations.\nIt converges, but to something economically wrong. Check residual_norm is actually small; refine the grid (the solution should be stable under refinement); and inspect the solved policies at the boundaries — a misplaced boundary condition shows up there first.\nSlow. The cost is dominated by the sparse Jacobian. Keep the guess good (fewer outer iterations), start from a coarse grid and interpolate the solution onto the fine one, and pass Δ = Inf when the guess is already close (a stationary solve with no continuation).","title":"When a solve fails"},{"category":"section","location":"solver/#The-monotonicity-diagnostic","page":"Solver and troubleshooting","text":"As an opt-in diagnostic, pdesolve can check the assembled residual Jacobian for monotonicity violations:\n\npdesolve(f, grid, guess; check_monotonicity = true)\n\nThis checks the effective neighbor weights in the fully assembled equation, after endogenous policies, risk adjustments, and nonlinear terms are included. Under the vt = -(...) convention, a positive same-variable spatial off-diagonal entry usually means an _up/_down stencil was chosen with the wrong sign; the warning names the variable, the grid point, the offending neighbor, and the stencil that most likely caused it. For transformed equations or endogenous-control problems, treat it as a debugging aid rather than proof that the economic upwind rule is wrong. Tune with monotonicity_tol (default 1e-6) and monotonicity_max_warnings (default 5).\n\nA related check runs by default, without any option: under the vt = -(RHS - ρv) convention the diagonal of the residual Jacobian is positive (the discount rate plus the outflow rates of the discretized generator), so a diagonal that is negative at every grid point is the signature of a flipped sign convention — RHS - ρv returned instead of -(RHS - ρv). pdesolve then warns on the first iteration, since the false transient is unstable and the solve would otherwise just diverge. The check is skipped when Δ = Inf, where the solve is a stationary nonlinear solve and either sign is legitimate.","title":"The monotonicity diagnostic"},{"category":"section","location":"examples/asset_pricing/garleanu_panageas/#Gârleanu–Panageas-(2015):-heterogeneous-agents","page":"Gârleanu–Panageas: heterogeneous agents","text":"This is the package's most demanding kind of problem: a general-equilibrium model with endogenous volatility and several coupled value functions. Two types of agents differ in risk aversion and elasticity of intertemporal substitution — type A is \"bold\" (low gamma_A), type B \"conservative\" (high gamma_B). The single state x is the consumption share of type A. Four claims are priced jointly: the wealth–consumption ratios p_A p_B of the two agents and two human-capital claims phi_1 phi_2.\n\nWhat makes it hard is that the volatility of the state, sigma_x, is endogenous — it depends on the very derivatives p_A p_B we are solving for — so the upwind direction must be recomputed from the resulting drift mu_x each iteration.","title":"Gârleanu–Panageas (2015): heterogeneous agents"},{"category":"section","location":"examples/asset_pricing/garleanu_panageas/#The-model","page":"Gârleanu–Panageas: heterogeneous agents","text":"The parameters:\n\nusing EconPDEs, Plots\n\nBase.@kwdef struct GarleanuPanageasModel\n    γA::Float64 = 1.5      # relative risk aversion, type A (bold)\n    ψA::Float64 = 0.7      # elasticity of intertemporal substitution, type A\n    γB::Float64 = 10.0     # relative risk aversion, type B (conservative)\n    ψB::Float64 = 0.05     # elasticity of intertemporal substitution, type B\n    ρ::Float64 = 0.001     # rate of time preference (discount rate)\n    δ::Float64 = 0.02      # death (turnover) rate\n    νA::Float64 = 0.01     # share of type A among newborns\n    μ::Float64 = 0.02      # aggregate consumption growth (drift)\n    σ::Float64 = 0.041     # aggregate consumption volatility\n    B1::Float64 = 30.72    # loading on first component of labor-income profile\n    δ1::Float64 = 0.0525   # decay rate of first labor-income component\n    B2::Float64 = -30.29   # loading on second component of labor-income profile\n    δ2::Float64 = 0.0611   # decay rate of second labor-income component\n    ω::Float64 = 0.92      # labor share of aggregate output\n    function GarleanuPanageasModel(γA, ψA, γB, ψB, ρ, δ, νA, μ, σ, B1, δ1, B2, δ2, ω)\n        # Normalize the labor-income loadings once, at construction, so the newborn's human capital is one.\n        scale = δ / (δ + δ1) * B1 + δ / (δ + δ2) * B2\n        new(γA, ψA, γB, ψB, ρ, δ, νA, μ, σ, B1 / scale, δ1, B2 / scale, δ2, ω)\n    end\nend","title":"The model"},{"category":"section","location":"examples/asset_pricing/garleanu_panageas/#The-state-space","page":"Gârleanu–Panageas: heterogeneous agents","text":"We build the grid and the initial guess first, because they fix the names used everywhere else. The grid is a NamedTuple whose key is the state variable (x, the consumption share of type A); the guess is a NamedTuple whose keys are the unknown functions (pA, pB, ϕ1, ϕ2), holding one starting value at each grid point. These names are what reappear inside the equation below — e.g. pAx_up will be the forward finite difference of pA in x.\n\nOne state x in 0 1, four coupled unknowns, all initialized flat at one.\n\nm = GarleanuPanageasModel()\nstategrid = (; x = range(0.0, 1.0, length = 100))\nguess = (; pA = ones(length(stategrid[:x])), pB = ones(length(stategrid[:x])), ϕ1 = ones(length(stategrid[:x])), ϕ2 = ones(length(stategrid[:x])))","title":"The state space"},{"category":"section","location":"examples/asset_pricing/garleanu_panageas/#The-equation","page":"Gârleanu–Panageas: heterogeneous agents","text":"We now write the function encoding the equilibrium conditions. Following the package convention, it takes the current state (a grid point) and u — the local bundle holding each unknown and its finite-difference derivatives there — and returns the time derivative of each unknown (pAt, pBt, ϕ1t, ϕ2t).\n\nBecause sigma_x is endogenous, the loop recomputes the upwind direction from the drift mu_x: it starts with forward differences (*_up) and switches to backward (*_down) when the drift turns negative.\n\nfunction (m::GarleanuPanageasModel)(state::NamedTuple, u::NamedTuple)\n    (; γA, ψA, γB, ψB, ρ, δ, νA, μ, σ, B1, δ1, B2, δ2, ω) = m\n    (; x) = state\n    (; pA, pAx_up, pAx_down, pAxx, pB, pBx_up, pBx_down, pBxx, ϕ1, ϕ1x_up, ϕ1x_down, ϕ1xx, ϕ2, ϕ2x_up, ϕ2x_down, ϕ2xx) = u\n\n    # endogenous σx depends on the unknown derivatives, so pick the upwind direction from μx\n    pAx, pBx, ϕ1x, ϕ2x = pAx_up, pBx_up, ϕ1x_up, ϕ2x_up\n    iter = 0\n    @label start\n    Γ = 1 / (x / γA + (1 - x) / γB)\n    p = x * pA + (1 - x) * pB\n    σx = σ * x * (Γ / γA - 1) / (1 + Γ * x * (1 - x) / (γA * γB) * ((1 - γB * ψB) / (ψB - 1) * (pBx / pB) - (1 - γA * ψA) / (ψA - 1) * (pAx / pA)))\n    σpA = pAx / pA * σx\n    σpB = pBx / pB * σx\n    σϕ1 = ϕ1x / ϕ1 * σx\n    σϕ2 = ϕ2x / ϕ2 * σx\n    κ = Γ * (σ - x * (1 - γA * ψA) / (γA * (ψA - 1)) * σpA - (1 - x) * (1 - γB * ψB) / (γB * (ψB - 1)) * σpB)\n    σCA = κ / γA + (1 - γA * ψA) / (γA * (ψA - 1)) * σpA\n    σCB = κ / γB + (1 - γB * ψB) / (γB * (ψB - 1)) * σpB\n    mcA = κ^2 * (1 + ψA) / (2 * γA) + (1 - ψA * γA) / (γA * (ψA - 1)) * κ * σpA - (1 - γA * ψA) / (2 * γA * (ψA - 1)) * σpA^2\n    mcB = κ^2 * (1 + ψB) / (2 * γB) + (1 - ψB * γB) / (γB * (ψB - 1)) * κ * σpB - (1 - γB * ψB) / (2 * γB * (ψB - 1)) * σpB^2\n    r = ρ + 1 / (ψA * x + ψB * (1 - x)) * (μ - x * mcA - (1 - x) * mcB - δ * ((νA / pA + (1 - νA) / pB) * ω * (B1 * ϕ1 + B2 * ϕ2) - 1))\n    μCA = ψA * (r - ρ) + mcA\n    μCB = ψB * (r - ρ) + mcB\n    μx = x * (μCA - μ) + δ * (νA / pA * ω * (B1 * ϕ1 + B2 * ϕ2) - x) - σ * σx\n    if (iter == 0) && (μx <= 0)\n        iter += 1\n        pAx, pBx, ϕ1x, ϕ2x = pAx_down, pBx_down, ϕ1x_down, ϕ2x_down\n        @goto start\n    end\n\n    μpA = pAx / pA * μx + 0.5 * pAxx / pA * σx^2\n    μpB = pBx / pB * μx + 0.5 * pBxx / pB * σx^2\n    μϕ1 = ϕ1x / ϕ1 * μx + 0.5 * ϕ1xx / ϕ1 * σx^2\n    μϕ2 = ϕ2x / ϕ2 * μx + 0.5 * ϕ2xx / ϕ2 * σx^2\n\n    pAt = -pA * (1 / pA + μCA + μpA + σCA * σpA - r - δ - κ * (σpA + σCA))\n    pBt = -pB * (1 / pB + μCB + μpB + σCB * σpB - r - δ - κ * (σpB + σCB))\n    ϕ1t = -ϕ1 * (1 / ϕ1 + μ - δ - δ1 + μϕ1 + σ * σϕ1 - r - κ * (σϕ1 + σ))\n    ϕ2t = -ϕ2 * (1 / ϕ2 + μ - δ - δ2 + μϕ2 + σ * σϕ2 - r - κ * (σϕ2 + σ))\n\n    return (; pAt, pBt, ϕ1t, ϕ2t), (; r, κ)\nend\n\nWith the equation, grid, and guess in hand, pdesolve solves the stationary system:\n\nresult = pdesolve(m, stategrid, guess)","title":"The equation"},{"category":"section","location":"examples/asset_pricing/garleanu_panageas/#The-solution","page":"Gârleanu–Panageas: heterogeneous agents","text":"We saved the equilibrium interest rate r and the price of risk kappa on the grid. The price of risk is high when the conservative agents (type B, high gamma) hold most of the economy's wealth (low x) and falls as the bold agents (type A) come to dominate — risk is borne more willingly when the less risk-averse agents are wealthier.\n\nxs = stategrid[:x]\np1 = plot(xs, result.saved[:κ]; xlabel = \"consumption share of bold agents x\", ylabel = \"price of risk κ\", legend = false)\np2 = plot(xs, result.saved[:r]; xlabel = \"consumption share of bold agents x\", ylabel = \"interest rate r\", legend = false)\nplot(p1, p2; layout = (1, 2), size = (800, 300))\n\n\n\nThis page was generated using Literate.jl.","title":"The solution"},{"category":"section","location":"boundary_conditions/#Boundary-conditions","page":"Boundary conditions","text":"Finite-difference schemes need ghost-node values just outside the grid to construct some derivatives at the boundary: second derivatives whenever the state volatility is nonzero there, and first derivatives when the drift points toward the boundary. This page covers the default, the bc keyword, and the two boundary types that are really part of the economics — state constraints and free (optimal-stopping) boundaries.","title":"Boundary conditions"},{"category":"section","location":"boundary_conditions/#The-default:-reflecting-boundaries","page":"Boundary conditions","text":"By default, the ghost-node value equals the boundary-node value, so the first derivative is zero beyond the boundary — a reflecting boundary. This is the natural choice when the boundary is far enough out that little probability mass reaches it (e.g. the top of an asset grid), and it is exact when the state genuinely reflects.\n\nNote that pdesolve still applies the full PDE at the boundary node, using the ghost value only to build the stencil. It does not replace the equation at the boundary with the boundary condition — on coarse grids this is noticeably more accurate.","title":"The default: reflecting boundaries"},{"category":"section","location":"boundary_conditions/#The-bc-keyword","page":"Boundary conditions","text":"To impose a different boundary derivative, pass bc, a NamedTuple mapping Symbol(unknown, state) to a (lower, upper) tuple giving the value of ∂v/∂state at each end of the grid — both entries use the same increasing-state orientation (the lower entry is not the derivative along the outward normal):\n\npdesolve(f, grid, guess; bc = (; va = (0.0, 1.0)))\n\nScalars apply uniformly along the boundary; arrays matching the boundary slice are also accepted (e.g. in 2D, a vector giving one derivative per grid point of the other state). Entries may be given for any subset of (unknown, state) pairs; omitted pairs keep the reflecting default.\n\nA degenerate boundary — where the diffusion vanishes and the drift points inward, like a square-root process at zero — needs no condition at all: the PDE at the boundary node only uses one-sided derivatives there, which is exactly what the upwind scheme provides.","title":"The bc keyword"},{"category":"section","location":"boundary_conditions/#State-constraints-(borrowing-limits)","page":"Boundary conditions","text":"Borrowing constraints and other endogenous state constraints are usually part of the PDE, not a bc entry. Put the constrained quantity on the state grid, compute the policy inside the PDE function, and let the upwind logic impose feasibility at the constraint: at the lower bound, if the household would like to dissave past the limit, the drift is pinned at zero and consumption equals income — case 3 of the endogenous-drift pattern in Writing the PDE function. Alternatively, some models imply a known boundary derivative at the constraint (e.g. from the FOC at zero saving), which can be imposed directly through bc.\n\nSee the consumption-saving examples and Wang-Wang-Yang.","title":"State constraints (borrowing limits)"},{"category":"section","location":"boundary_conditions/#Optimal-stopping-(free-boundaries)","page":"Boundary conditions","text":"Optimal stopping problems — default, exercise, exit — are HJB variational inequalities. For a stopping payoff S(x):\n\nminleft\n    rho v(x) - f(x) - mu(x) v(x) - tfrac12sigma^2(x) v(x)\n    v(x) - S(x)\nright = 0\n\nPass the stopping payoff as a lower bound on the unknown with the lower_bound keyword (upper_bound for upper bounds in minimization problems):\n\nresult = pdesolve(f, grid, guess; lower_bound = vec(payoff_on_grid))\n\n(The Unicode keywords y̲ and ȳ from older versions still work, but are deprecated.)\n\nThe free boundary then comes out of the solution — the region where the bound binds is the stopping region, and value matching and smooth pasting hold automatically at its edge. Internally these bounded problems are solved as mixed complementarity problems with NLsolve.mcpsolve.\n\nSee Leland for a worked example (optimal default), and Ben Moll's notes on stopping-time problems for background.","title":"Optimal stopping (free boundaries)"},{"category":"section","location":"examples/asset_pricing/brunnermeier_sannikov/#Brunnermeier–Sannikov-(2014):-a-macroeconomic-model-with-a-financial-sector","page":"Brunnermeier–Sannikov: macro-finance","text":"Experts and households both hold capital, but experts are more productive and can issue equity only if they retain at least a fraction chi of it — a skin-in-the-game constraint. The state x is the experts' wealth share. The model has a crisis region (experts constrained, Psi  1) and a normal region (Psi = 1); the capital price q and the experts' capital share Psi are recovered with a root-find, sweeping x from the lower boundary upward so each step reuses the previous q to build q_x by finite difference. That order-dependent sweep is why the model struct is mutable and carries q_old.","title":"Brunnermeier–Sannikov (2014): a macroeconomic model with a financial sector"},{"category":"section","location":"examples/asset_pricing/brunnermeier_sannikov/#The-model","page":"Brunnermeier–Sannikov: macro-finance","text":"The parameters:\n\nusing EconPDEs, Roots, Plots\n\nBase.@kwdef mutable struct BrunnermeierSannikov\n  # Calibration uses Section 3.6 of the textbook chapter (the paper's r here appears to be a typo for ρH)\n  ρE::Float64 = 0.06      # experts' rate of time preference (discount rate)\n  ρH::Float64 = 0.05      # households' rate of time preference (discount rate)\n  γ::Float64 = 2.0        # relative risk aversion\n  ψ::Float64 = 0.5        # elasticity of intertemporal substitution\n  δ::Float64 = 0.05       # depreciation rate of capital\n  σ::Float64 = 0.1        # exogenous (fundamental) volatility of capital\n  κ::Float64 = 10.0       # investment adjustment-cost parameter\n  a::Float64 = 0.11       # experts' productivity (output per unit capital)\n  alow::Float64 = 0.03    # households' productivity (output per unit capital)\n  χlow::Float64 = 0.5     # minimum equity retention (skin-in-the-game floor)\n  q_old::Float64 = 1.0    # previous capital price q (carried across the sweep)\n  qx_old::Float64 = 0.0   # previous q_x (carried across the sweep)\n  Ψ_old::Float64 = 1.0    # previous experts' capital share Ψ (carried across the sweep)\nend","title":"The model"},{"category":"section","location":"examples/asset_pricing/brunnermeier_sannikov/#The-state-space","page":"Brunnermeier–Sannikov: macro-finance","text":"We build the grid and the initial guess first, because they fix the names used everywhere else. The grid is a NamedTuple whose key is the state variable (x, the experts' wealth share); the guess is a NamedTuple whose keys are the unknown functions (pE, pH), holding one starting value at each grid point. These names are what reappear inside the equation below — e.g. pEx_up will be the forward finite difference of pE in x.\n\nOne state x in (0 1) on 200 interior grid points. Because the sweep marches from the lower boundary upward, the grid spacing Δx and its minimum xmin are computed here and later passed into the model.\n\nm = BrunnermeierSannikov()\nxn = 200\nstategrid =  (; x = range(0, 1.0, length = xn+2)[2:(end-1)])\nΔx = step(stategrid[:x])\nxmin = minimum(stategrid[:x])\nguess = (; pE =  9 .* ones(xn), pH =   10 .* ones(xn))","title":"The state space"},{"category":"section","location":"examples/asset_pricing/brunnermeier_sannikov/#The-equation","page":"Brunnermeier–Sannikov: macro-finance","text":"We now write the function encoding the equilibrium conditions. Following the package convention, it takes the current state (a grid point) and u — the local bundle holding each unknown and its finite-difference derivatives there — plus the grid spacing Δx and lower bound xmin, and returns the time derivative of each unknown (pEt, pHt). Inside, a root-find recovers the capital price q and the experts' capital share Psi, distinguishing the crisis region (Psi  1) from the normal region (Psi = 1).\n\nfunction (m::BrunnermeierSannikov)(state::NamedTuple, u::NamedTuple, Δx, xmin)\n  (; ρE, ρH, γ, ψ, δ, σ, κ, a, alow, χlow) = m\n  (; x) = state\n  (; pE, pEx_up, pEx_down, pExx, pH, pHx_up, pHx_down, pHxx) = u\n  # Floor the wealth–consumption ratios before dividing by them: a Newton trial iterate can wander\n  # nonpositive, which would blow up the equilibrium conditions. Slack at the converged solution,\n  # where both stay well above 1e-3.\n  pE = max(pE, 1e-3)\n  pH = max(pH, 1e-3)\n  χ = max(χlow, x)\n  pEx, pHx = pEx_up, pHx_up\n  iter = 0\n  @label start\n  q_old = m.q_old\n  qx_old = m.qx_old\n  Ψ_old = m.Ψ_old\n\n  # First, solve for q, qx, and qxx\n  if abs(x - xmin) < 1e-9\n    # initial condition at lower boundary of x grid (point outside the grid technically)\n    # We must have that expert consumption approaches zero as xt approaches zero (linked to transversality condition)\n    Ψ_old = 0.0\n    q_old = (alow + 1 / κ) / (1 / pH + 1 / κ)\n    qx_old = 0.0\n  end\n  if Ψ_old < 1.0\n    # crisis region.\n    # in this case, we do not have Ψ = 1.0 but we have\n    # (a - alow) / q = (κE - κH) * (σ + σq)\n    # note that\n    out = find_zero(Ψ_old) do Ψ\n        local q = (Ψ * a + (1 - Ψ) * alow + 1 / κ) / (x / pE + (1 - x) / pH + 1 / κ)\n        local qx = (q - q_old) / Δx\n        local αE = χ * Ψ / x\n        local σx = x * (αE - 1) * σ / (1 - x * (αE - 1) * qx / q)\n        local σpE = pEx / pE * σx\n        local σpH = pHx / pH * σx\n        local σq = qx / q * σx\n        local σE = αE * (σ + σq)\n        local αH = (1 - αE * x) / (1 - x)\n        local σH = αH * (σ + σq)\n        local κE = γ * (σE - (1 / γ - 1) / (ψ - 1) * σpE)\n        local κH = γ * (σH - (1 / γ - 1) / (ψ - 1) * σpH)\n        return (a - alow) / q -  χ * (κE - κH) * (σ + σq)\n      end\n    Ψ = out[1]\n    q = (Ψ * a + (1 - Ψ) * alow + 1 / κ) / (x / pE + (1 - x) / pH + 1 / κ)\n    qx = (q - q_old) / Δx\n    if Ψ > 1\n      Ψ_old = 1.0\n    end\n  end\n  if Ψ_old ≥ 1.0\n    # normal region.\n    Ψ = 1.0\n    q = (a + 1 / κ) / ((x / pE + (1 - x) / pH) + 1 / κ)\n    qx = - (a + 1 / κ) / ((x / pE + (1 - x) / pH) + 1 / κ)^2 * (1 / pE - 1 / pH - x / pE^2 * pEx - (1-x) / pH^2 * pHx)\n  end\n  qxx = (qx - qx_old) / Δx\n  m.q_old = q\n  m.qx_old = qx\n  m.Ψ_old = Ψ\n\n\n  # having solved for q, do the time step\n  # Floor q the same way before it enters ι, Φ, μq, and r; slack at the converged solution (q ≈ 1).\n  q = max(q, sqrt(eps()))\n  ι = (q - 1) / κ\n  Φ = log(q) / κ\n  αE = χ * Ψ / x\n  σx = x * (αE - 1) * σ / (1 - x * (αE - 1) * qx / q)\n  σpE = pEx / pE * σx\n  σpH = pHx / pH * σx\n  σq = qx / q * σx\n  σE = αE * (σ + σq)\n  αH = (1 - αE * x) / (1 - x)\n  σH = αH * (σ + σq)\n  κE = γ * (σE - (1 / γ - 1) / (ψ - 1) * σpE)\n  κH = γ * (σH - (1 / γ - 1) / (ψ - 1) * σpH)\n  μx = x * (1 - x) * (κE * σE - κH * σH + 1 / pH - 1 / pE - (σE - σH) * (σ + σq))\n  μx2 = x * ((a - ι) / q - 1 / pE) + σx * (κE - σ - σq) + x * (1 - χ) * (κE - κH) * (σ + σq)\n  if (iter == 0) && (μx < 0)\n    # upwinding\n    iter += 1\n    pEx, pHx = pEx_down, pHx_down\n    @goto start\n  end\n  μpE = pEx / pE * μx + 0.5 * pExx / pE * σx^2\n  μpH = pHx / pH * μx + 0.5 * pHxx / pH * σx^2\n  μq = qx / q * μx + 0.5 * qxx / q * σx^2\n\n  # Clamp r to a wide band: a Newton trial iterate outside the economic domain can produce a wild\n  # interest rate that would overflow the exponential price updates below. Slack at the converged\n  # solution, where r stays within a few percent of zero.\n  r = clamp((a - ι) / q + Φ - δ + μq + σ * σq - (χ * κE + (1 - χ) * κH) * (σ + σq), -3.0, 3.0)\n  # Market Pricing\n  pEt = -  pE * (1 / pE - ψ * ρE + (ψ - 1) * (r + κE * σE) + μpE - (ψ - 1) * γ / 2 * σE^2 + (2 - ψ - γ) / (2 * (ψ - 1)) * σpE^2 + (1 - γ) * σpE * σE)\n  pHt = -  pH * (1 / pH - ψ * ρH + (ψ - 1) * (r + κH * σH) + μpH - (ψ - 1) * γ / 2 * σH^2 + (2 - ψ - γ) / (2 * (ψ - 1)) * σpH^2 + (1 - γ) * σpH * σH)\n  d = Ψ * a + (1 - Ψ) * alow - ι\n  μR = r + (χ * κE + (1 - χ) * κH) * (σ + σq)\n  (; pEt, pHt), (; x, r, μx, σx, Ψ, κ, κE, κH, σE, σH, σq, q, qx, qxx, μq, μx2, Φ, χ, d, μR)\nend\n\nThe sweep is order-dependent — it marches from the lower boundary xmin upward, each step reusing the previous q carried in the mutable model — so Δx and xmin are passed into the model and finite-difference autodiff is used:\n\nresult = pdesolve((state, u) -> m(state, u, Δx, xmin), stategrid, guess; autodiff = :finite)","title":"The equation"},{"category":"section","location":"examples/asset_pricing/brunnermeier_sannikov/#The-solution","page":"Brunnermeier–Sannikov: macro-finance","text":"We saved the capital price q. It is depressed when experts are poorly capitalized (low x) — the crisis region — and tends to rise with the experts' wealth share as the retention constraint slackens.\n\nxs = stategrid[:x]\nplot(xs, result.saved[:q]; xlabel = \"experts' wealth share x\", ylabel = \"capital price q\", legend = false)\n\n\n\nThis page was generated using Literate.jl.","title":"The solution"},{"category":"section","location":"api/#API-reference","page":"API reference","text":"","title":"API reference"},{"category":"function","location":"api/#EconPDEs.pdesolve","page":"API reference","text":"pdesolve(f, grid, guess [, τs]; kwargs...)\n\nSolve a system of nonlinear ODEs/PDEs — typically Hamilton–Jacobi–Bellman equations — by finite differences. Handles an arbitrary number of coupled unknown functions on a state grid of one, two, or three state variables.\n\nPositional arguments\n\nf: the local equation, a function (state, u) -> out (or (state, u, t) -> out for a time-dependent equation), where\nstate is a NamedTuple with the current grid point (one entry per state variable),\nu is a NamedTuple with each unknown function and its finite-difference derivatives at that point (e.g. v, vk_up, vk_down, vkk, …),\nout is a NamedTuple with one time derivative per unknown (e.g. (; vt)).\nf may also return a second NamedTuple of objects to save on the grid; these are returned in result.saved.\ngrid: a NamedTuple (or OrderedDict) mapping each state variable name to an AbstractVector (its grid), e.g. (; k = range(...)).\nguess: a NamedTuple (or OrderedDict) mapping each unknown name to an array of initial values with the same shape as the grid. For a time-dependent problem, it is the terminal value at τs[end].\nτs (optional): an increasing time grid. The equation is then solved backward from τs[end], and result.zero[i] is the solution at time τs[i].\n\nKeyword arguments\n\nbc: boundary derivatives, a NamedTuple (or OrderedDict) mapping Symbol(unknown, state) to a (lower, upper) tuple (scalars, or arrays matching the boundary slice). Entries may be given for any subset of (unknown, state) pairs; omitted pairs default to reflecting boundaries (zero first derivative at the boundary). Both entries are ∂v/∂state oriented in the increasing-state direction.\nis_algebraic: a NamedTuple (or OrderedDict) of Bools marking equations that are algebraic (no time derivative) rather than PDEs. Defaults to all false.\nlower_bound, upper_bound: lower/upper bounds on the unknowns for HJB variational inequalities (optimal stopping), solved as a mixed complementarity problem. Default to -Inf/Inf (unbounded). The Unicode keywords y̲/ȳ are deprecated aliases.\nmethod: :newton (default) or :trust_region.\nmaxdist: convergence tolerance on the residual. Defaults to sqrt(eps()).\niterations: maximum number of pseudo-transient iterations. Defaults to 100.\nΔ: initial pseudo-transient time step. Defaults to 1.0; pass Δ = Inf to solve the stationary residual in one nonlinear solve, with no continuation.\nautodiff: :forward (default), :finite, or :central. With one to three state variables the Jacobian is always computed by colored sparse finite differences (:forward and :finite both use forward differences; :central uses central differences); forward-mode AD is used only when no sparsity pattern is available (four or more states).\nverbose: print a one-line problem summary, convergence progress, and a final convergence summary. Defaults to true. With verbose = false a successful solve prints nothing — convergence failures are always reported with @warn — so pdesolve can run inside a loop (e.g. an estimation) without flooding the log.\nFurther solver knobs (scale, minΔ, maxΔ, inner_iterations, innerdist, inner_verbose, reformulation, autoscale) are forwarded to finiteschemesolve; see its docstring, especially when a solve stalls.\ncheck_monotonicity: if true, warn when the assembled residual Jacobian has same-variable spatial off-diagonal entries with the wrong monotonicity sign. Defaults to false. A flipped sign convention — returning RHS - ρv instead of -(RHS - ρv), which makes the Jacobian diagonal negative at every grid point — is detected and warned about regardless of this option (whenever Δ is finite).\nmonotonicity_tol: tolerance for the monotonicity check. Defaults to 1e-6.\nmonotonicity_max_warnings: maximum number of monotonicity warnings to print. Defaults to 5.\n\nReturns an EconPDEResult with fields zero (the solved unknowns), residual_norm, and saved (the saved objects, together with the solved unknowns). result.converged reports whether the residual met the tolerance (across all times, for a time-dependent problem). result.optional is kept as a backward-compatible alias for result.saved.\n\n\n\n\n\n","title":"EconPDEs.pdesolve"},{"category":"type","location":"api/#EconPDEs.EconPDEResult","page":"API reference","text":"EconPDEResult\n\nThe value returned by pdesolve, with fields:\n\nzero: the solved unknown functions (an OrderedDict, or a vector of them for a time-dependent problem).\nresidual_norm: the norm of the residual at the solution.\nsaved: objects saved by the PDE function, together with the solved unknowns.\n\nresult.converged reports whether the residual norm is below the solver tolerance (the maximum across times, for a time-dependent problem). For backward compatibility, result.optional is an alias for result.saved.\n\n\n\n\n\n","title":"EconPDEs.EconPDEResult"},{"category":"function","location":"api/#EconPDEs.finiteschemesolve","page":"API reference","text":"finiteschemesolve(F!, y0; kwargs...)\n\nLower-level nonlinear solver behind pdesolve. Finds y such that F!(ydot, y) writes the residual into ydot and returns zero, using a nonlinear solver (Newton by default) with pseudo-transient continuation from the initial guess y0.\n\npdesolve assembles F! (the finite-difference residual of the PDE) and calls this function, so most users should call pdesolve instead. It returns the tuple (y, residual_norm).\n\nKeyword arguments\n\nΔ = 1.0: initial pseudo-transient time step. Δ = Inf solves the stationary residual in one nonlinear solve, with no continuation.\nscale = 10.0: growth factor for the time step. After a successful step that reduces the residual, Δ is multiplied by scale * (old residual / new residual), and scale compounds across consecutive improving steps. After a failed inner solve, Δ is divided by 10 and its regrowth is capped at half the failed Δ until the residual falls to half its level at the failure, so a Δ that just failed is not immediately retried.\nminΔ = 1e-9, maxΔ = Inf: bounds on the time step. The solve stops when Δ < minΔ (typically a sign that the scheme is non-monotone or the initial guess is poor).\niterations = 100: maximum number of pseudo-transient (outer) iterations.\nmaxdist = sqrt(eps()): convergence tolerance on the residual norm.\ninner_iterations = 10, innerdist = sqrt(eps()), inner_verbose = false: iteration limit, tolerance, and verbosity for each implicit time step (the inner solve).\nmethod: :newton (default) or :trust_region.\nautodiff: :forward (default), :finite, or :central. When a sparsity pattern J0 is available, the Jacobian is computed by colored sparse finite differences (:forward/:finite forward differences, :central central differences); forward-mode AD is used only without a sparsity pattern.\nis_algebraic: Bool per entry of y0, marking algebraic (no time-derivative) equations.\nJ0 = nothing: sparsity pattern of the Jacobian.\ncolorvec = nothing: column coloring of J0 for the colored finite differences (no two columns of the same color may share a nonzero row). Defaults to a greedy coloring of J0; pass one when it is known in closed form, as pdesolve does for its stencil pattern.\nlower_bound, upper_bound: lower/upper bounds on y. When any bound is finite, the problem is solved as a mixed complementarity problem with NLsolve.mcpsolve, with reformulation (:smooth, the default, or :minmax) and autoscale = true passed through to it. The Unicode keywords y̲/ȳ are deprecated aliases.\nverbose = true: print one line per pseudo-transient time step (Iter, TimeStep, Residual) and a final convergence summary. A ✗ in place of the residual means the inner nonlinear solve failed at that Δ: no step is taken (so there is no residual to show) and it is retried with Δ/10 — the unusual causes, a NaN from the model or a singular Jacobian, are flagged in parentheses. With verbose = false a successful solve prints nothing; convergence failures are always reported with @warn.\n\n\n\n\n\n","title":"EconPDEs.finiteschemesolve"},{"category":"section","location":"examples/corporate_finance/leland/#Leland-(1994):-optimal-default","page":"Leland: optimal default","text":"Leland's model of a levered firm is a classic optimal-stopping problem. The full paper values risky debt and studies capital structure; this example isolates the equityholders' default decision. Cash flow (EBIT) delta follows a geometric Brownian motion ddelta = mudeltadt + sigmadeltadW. The firm has issued perpetual debt with coupon C and faces a tax rate tau; equity holders receive the after-tax flow delta-(1-tau)C but may default (walk away) whenever they choose. Equity value E(delta) is thus the value of the option to keep the firm alive, and solves the HJB variational inequality\n\nminBigl r E - bigl(delta-(1-tau)Cbigr) - mudeltaE(delta) - tfrac12sigma^2delta^2 E(delta) E(delta)Bigr = 0\n\nwith default payoff 0 (limited liability). This delivers the value-matching E(delta^*)=0 and smooth-pasting E(delta^*)=0 conditions at the endogenous default threshold delta^*.","title":"Leland (1994): optimal default"},{"category":"section","location":"examples/corporate_finance/leland/#The-model","page":"Leland: optimal default","text":"The parameters live in a struct:\n\nusing EconPDEs, Plots, Printf\n\nBase.@kwdef struct LelandModel\n    r::Float64 = 0.02    # risk-free rate\n    μ::Float64 = 0.0     # drift of cash flow (EBIT growth)\n    σ::Float64 = 0.1     # volatility of cash flow\n    C::Float64 = 0.05    # coupon rate\n    τ::Float64 = 0.2     # tax rate\nend","title":"The model"},{"category":"section","location":"examples/corporate_finance/leland/#The-state-space","page":"Leland: optimal default","text":"We build the grid and the initial guess first, because they fix the names used everywhere else. The grid is a NamedTuple whose key is the state variable (δ, the cash flow); the guess is a NamedTuple whose key is the unknown function (E, equity value), holding one starting value at each grid point. These names reappear inside the equation below — e.g. Eδ_up will be the forward finite difference of E in δ. We start the guess from the value of the cash-flow claim net of the after-tax coupon, floored at zero.\n\nm = LelandModel()\nstategrid = (; δ = range(0, 0.2, step = 0.005))\nguess = (; E = max.(stategrid[:δ] ./ (m.r - m.μ) .- (1 .- m.τ) .* m.C ./ m.r, 0.0))","title":"The state space"},{"category":"section","location":"examples/corporate_finance/leland/#The-equation","page":"Leland: optimal default","text":"We now write the function encoding the HJB equation. Following the package convention, it takes the current state (a grid point) and u (each unknown together with its finite-difference derivatives there) and returns the time derivative of each unknown.\n\nfunction (m::LelandModel)(state::NamedTuple, u::NamedTuple)\n    (; r, μ, σ, C, τ) = m\n    (; δ) = state\n    (; E, Eδ_up, Eδ_down, Eδδ) = u\n    μδ, σδ = μ * δ, σ * δ\n    Eδ = (μδ >= 0) ? Eδ_up : Eδ_down\n    f = δ - (1 - τ) * C\n    Et = -(f + Eδ * μδ + 0.5 * Eδδ * σδ^2 - r * E)\n    return (; Et)\nend\n\nThe stopping (default) region is imposed by passing zero as lower_bound: equity can never be worth less than zero. We also pin the slope at the top boundary to that of a debt-free claim, 1(r-mu). With these in hand, pdesolve solves the variational inequality:\n\nlower_bound = zeros(length(stategrid[:δ]))\nbc = (; Eδ = (0.0, 1 / (m.r - m.μ)))\nresult = pdesolve(m, stategrid, guess; lower_bound = lower_bound, bc = bc)","title":"The equation"},{"category":"section","location":"examples/corporate_finance/leland/#The-solution","page":"Leland: optimal default","text":"Equity is worthless below an endogenous default threshold delta^* and rises smoothly above it. This stripped-down case has the familiar closed-form trigger. If beta  0 is the negative root of\n\ntfrac12sigma^2beta(beta-1) + mubeta - r = 0\n\nthen\n\ndelta^* =\nfracbetabeta-1(r-mu)frac(1-tau)Cr\n\nThe finite-difference solution recovers the same boundary up to grid resolution. In the plot, the red dotted line is the closed-form trigger and the gray dashed line is the first grid point where the variational-inequality solution lifts off from zero.\n\nδs = stategrid[:δ]\nE = result.zero[:E]\n# Default boundary δ*: the first grid point where equity lifts off the E = 0 lower bound of the variational inequality.\nδstar_grid = δs[findfirst(>(1e-8), E)]\nroot_a = 0.5 * m.σ^2\nroot_b = m.μ - 0.5 * m.σ^2\nroot_c = -m.r\nβ = min((-root_b + sqrt(root_b^2 - 4 * root_a * root_c)) / (2 * root_a),\n        (-root_b - sqrt(root_b^2 - 4 * root_a * root_c)) / (2 * root_a))\nδstar = β / (β - 1) * (m.r - m.μ) * (1 - m.τ) * m.C / m.r\nclaim_slope = 1 / (m.r - m.μ)\ncoupon_value = (1 - m.τ) * m.C / m.r\noption_loading = -claim_slope * δstar^(1 - β) / β\ncontinuation = δs .>= δstar_grid\nE_closed = zeros(length(δs))\nE_closed[continuation] .= claim_slope .* δs[continuation] .- coupon_value .+\n                          option_loading .* δs[continuation].^β\n@printf(\"closed-form default threshold δ* = %.4f; grid lift-off = %.4f\\n\",\n        δstar, δstar_grid)\n@printf(\"maximum equity-value error on the continuation grid = %.2e\\n\",\n        maximum(abs.(E[continuation] .- E_closed[continuation])))\nplot(δs, E; xlabel = \"cash flow δ\", ylabel = \"equity value E(δ)\",\n     label = \"finite difference\", legend = :topleft)\nplot!(δs[continuation], E_closed[continuation]; color = :black, linestyle = :dash,\n      label = \"closed form\")\nvline!([δstar]; color = :red, linestyle = :dot, label = \"closed-form δ*\")\nvline!([δstar_grid]; color = :gray, linestyle = :dash, label = \"grid lift-off\")\n\n\n\nThis page was generated using Literate.jl.","title":"The solution"},{"category":"section","location":"examples/asset_pricing/he_krishnamurthy/#He–Krishnamurthy-(2013):-intermediary-asset-pricing","page":"He–Krishnamurthy: intermediaries","text":"Households cannot hold the risky asset directly — they invest only through specialist intermediaries, and their equity stake in an intermediary is capped at a multiple m of the intermediary's net worth. The single state x is the specialists' wealth share. When x falls the sector's risk-bearing capacity shrinks, the constraint binds, and the price of risk rises. The unknown is the specialists' wealth–consumption ratio p_S; goods-market clearing ties the aggregate price–consumption ratio p to it.","title":"He–Krishnamurthy (2013): intermediary asset pricing"},{"category":"section","location":"examples/asset_pricing/he_krishnamurthy/#The-model","page":"He–Krishnamurthy: intermediaries","text":"The parameters:\n\nusing EconPDEs, Plots\n\nBase.@kwdef struct HeKrishnamurthy\n  # See Table 2—Parameters and Targets\n  m::Float64 = 4  # intermediation multiplier\n  λ::Float64 = 0.6 # Debt/assets of intermediary sector\n  g::Float64 = 0.02 # dividend growth\n  σ::Float64 = 0.09 # dividend volatility\n  ρ::Float64 = 0.04 # time discount rate\n  γ::Float64 = 2.0 # risk aversion specialist\n  l::Float64 = 1.84 # labor income ratio\nend","title":"The model"},{"category":"section","location":"examples/asset_pricing/he_krishnamurthy/#The-state-space","page":"He–Krishnamurthy: intermediaries","text":"We build the grid and the initial guess first, because they fix the names used everywhere else. The grid is a NamedTuple whose key is the state variable (x, the specialists' wealth share); the guess is a NamedTuple whose key is the unknown function (pS), holding one starting value at each grid point. These names are what reappear inside the equation below — e.g. pSx_up will be the forward finite difference of pS in x.\n\nOne state x in (0 1) on a grid stretched toward the lower boundary where the constraint bites. The single unknown p_S is initialized flat at one.\n\nm = HeKrishnamurthy()\nxn = 100\nstategrid =  (; x = range(0, 1, length = xn+2)[2:(end-1)].^1.5)\nguess = (; pS = ones(length(stategrid[:x])))","title":"The state space"},{"category":"section","location":"examples/asset_pricing/he_krishnamurthy/#The-equation","page":"He–Krishnamurthy: intermediaries","text":"We now write the function encoding the equilibrium conditions. Following the package convention, it takes the current state (a grid point) and u — the local bundle holding the unknown and its finite-difference derivatives there (pS, pSx_up, pSx_down, pSxx) — and returns the time derivative pSt.\n\nGoods-market clearing expresses the aggregate price–consumption ratio p (and its derivatives) in terms of p_S. The state volatility sigma_x feeds back through prices, so the upwind direction is chosen from the drift mu_x: forward pSx_up unless the drift turns negative, then backward pSx_down.\n\nfunction (model::HeKrishnamurthy)(state::NamedTuple, u::NamedTuple)\n  # `m` here is the intermediation multiplier (a model parameter), so the model instance is `model`.\n  (; m, λ, g, σ, ρ, γ, l) = model\n  (; pS, pSx_up, pSx_down, pSxx) = u\n  (; x) = state\n  iter = 0\n  pSx = pSx_up\n  @label start\n  # market clearing for goods gives x / pS + (1 - x) * ρ = (1 + l) / p\n  # This allows to obtain p (and its derivatives) in terms of pS (and its derivatives)\n  p = (1 + l) / (x / pS + (1 - x) * ρ)\n  px = - (1 + l) / (x / pS + (1 - x) * ρ)^2 * (1 / pS - ρ - x / pS^2 * pSx)\n  pxx = - (1 + l) / (x / pS + (1 - x) * ρ)^2 * (- 1 / pS^2 * pSx - 1 / pS^2 * pSx - x / pS^2 * pSxx + 2 * x / pS^3 * pSx^2) + 2 * (1 + l) / (x / pS + (1 - x) * ρ)^3 * (1 / pS - ρ - x / pS^2 * pSx)^2\n\n  αI = 1 / (1 - λ * (1 - x))\n  if x * (1 + m) * αI < 1\n    αI = 1 / (x * (1 + m))\n  end\n  # we have the usual feeback loop between asset prices and wealth\n  # σx = x * (αI - 1) * (σ + σp)\n  σx = x * (αI - 1) * σ / (1 -  x * (αI - 1) * px / p)\n  σpS = pSx / pS * σx\n  σp = px / p * σx\n  σR = σ + σp\n  κ = γ * (αI * σR - σpS)\n  μx = x * (1 - x) * ((αI - (1 - αI * x) / (1-x)) * κ * σR - 1 / pS + ρ - (αI - (1 - αI * x) / (1 - x)) * σR^2 - l / ((1 - x) * p))\n  if (iter == 0) && (μx < 0)\n    iter += 1\n    pSx = pSx_down\n    @goto start\n  end\n  μpS = pSx / pS * μx + 0.5 * pSxx / pS * σx^2\n  μp = px / p * μx + 0.5 * pxx / p * σx^2\n  r = 1 / p + g + μp + σ * σp - κ * σR\n  σCS = κ / γ\n  μCS = (r - ρ) / γ + (1 + 1 / γ) / (2 * γ) * κ^2\n  pSt = - (1 / pS + μCS + μpS + σCS * σpS - r - κ * (σCS + σpS))\n  μR = r + κ * σR\n  σI = αI * σR\n  (; pSt), (; pS, p, r, κ, σp, μR, σR, αI, μx, σx, x, px, pxx, σI)\nend\n\nWith the equation, grid, and guess in hand, pdesolve solves the stationary system:\n\nresult = pdesolve(m, stategrid, guess)","title":"The equation"},{"category":"section","location":"examples/asset_pricing/he_krishnamurthy/#The-solution","page":"He–Krishnamurthy: intermediaries","text":"We saved the price of risk kappa on the grid. It tends to be elevated when specialists are poorly capitalized (low x) — exactly the region where the intermediation constraint binds and the sector can absorb little risk.\n\nxs = stategrid[:x]\nplot(xs, result.saved[:κ]; xlabel = \"specialists' wealth share x\", ylabel = \"price of risk κ\", legend = false)\n\n\n\nThis page was generated using Literate.jl.","title":"The solution"},{"category":"section","location":"examples/consumption_saving/consumption_saving_risky_asset/#Achdou–Han–Lasry–Lions–Moll:-portfolio-choice-with-two-assets","page":"Consumption–saving: risky asset","text":"This extends the Achdou–Han–Lasry–Lions–Moll consumption–saving problem to a portfolio choice between a riskless asset (return r) and a risky asset (return mu_R, volatility sigma_R). A household with total wealth a chooses consumption c and how much of its wealth k to hold in the risky asset, subject to no short-selling and no leverage (0 le k le a - a_min). Labor income y follows the same mean-reverting diffusion. Wealth evolves as da = bigl(y + r a + (mu_R - r) k - cbigr)dt + sigma_R k dW and the value function v(y a) solves\n\nrho v = max_ck fracc^1-gamma1-gamma\n  + partial_a vbigl(y + r a + (mu_R - r) k - cbigr)\n  + tfrac12 sigma_R^2 k^2 partial_aa v\n  + partial_y vkappa_y(bar y - y)\n  + tfrac12 sigma_y^2 partial_yy v\n\nThe first-order conditions are c = (partial_a v)^-1gamma for consumption and the Merton rule k = -tfracmu_R - rsigma_R^2partial_a v  partial_aa v for the risky holding, clamped to 0 a - a_min. Those formulas are used only when the local value derivatives make the Hamiltonian concave in the relevant control; otherwise the bounded Hamiltonian is maximized directly.","title":"Achdou–Han–Lasry–Lions–Moll: portfolio choice with two assets"},{"category":"section","location":"examples/consumption_saving/consumption_saving_risky_asset/#The-model","page":"Consumption–saving: risky asset","text":"The parameters live in a struct:\n\nusing EconPDEs, Distributions, Plots\n\nBase.@kwdef struct AchdouHanLasryLionsMollModel_RiskyAsset\n    κy::Float64 = 0.1        # mean-reversion speed of labor income\n    ybar::Float64 = 1.0      # long-run mean of labor income\n    σy::Float64 = 0.07       # volatility of labor income\n\n    r::Float64 = 0.03        # risk-free rate\n    μR::Float64 = 0.04       # expected return on the risky asset\n    σR::Float64 = 0.1        # volatility of the risky asset\n\n    ρ::Float64 = 0.05        # discount rate\n    γ::Float64 = 2.0         # relative risk aversion\n\n    amin::Float64 = 0.0      # borrowing limit (minimum wealth)\n    amax::Float64 = 300.0    # maximum wealth (grid upper bound)\nend\n\nfunction _merton_consumption_rate(m::AchdouHanLasryLionsMollModel_RiskyAsset)\n    prem = m.μR - m.r\n    return (m.ρ - m.r) / m.γ + m.r - (1 - m.γ) * prem^2 / (2 * m.γ^2 * m.σR^2)\nend","title":"The model"},{"category":"section","location":"examples/consumption_saving/consumption_saving_risky_asset/#The-state-space","page":"Consumption–saving: risky asset","text":"We build the grid and the initial guess first, because they fix the names used everywhere else. The grid is a NamedTuple whose keys are the two state variables (y and a); the guess is a NamedTuple whose key is the unknown function (v), holding one starting value at each grid point. These names are what reappear inside the equation below — e.g. va_up will be the forward finite difference of v in a.\n\nIncome spans the bulk of its ergodic (Gamma) distribution. Wealth uses a curved grid, because the borrowing constraint and the risky-share tilt both create most of the curvature near zero. The initial guess is the Merton-tail value of consuming out of financial plus human wealth.\n\nm = AchdouHanLasryLionsMollModel_RiskyAsset()\ndistribution = Gamma(2 * m.κy * m.ybar / m.σy^2, m.σy^2 / (2 * m.κy))\nys = collect(range(quantile(distribution, 0.001), quantile(distribution, 0.999), length = 5))\nas = m.amin .+ (m.amax - m.amin) .* collect(range(0.0, 1.0, length = 150)).^2\nstategrid = (; y = ys, a = as)\ncshare = _merton_consumption_rate(m)\nguess = (; v = [cshare^(-m.γ) * (a + y / m.r)^(1 - m.γ) / (1 - m.γ) for y in stategrid[:y], a in stategrid[:a]])","title":"The state space"},{"category":"section","location":"examples/consumption_saving/consumption_saving_risky_asset/#The-equation","page":"Consumption–saving: risky asset","text":"We now write the function encoding the HJB equation. Following the package convention, it takes the current state (a grid point) and u (each unknown together with its finite-difference derivatives there) and returns the time derivative of each unknown.\n\nAs in the one-asset model, income is upwinded on mu_y and the endogenous asset drift picks the forward/backward derivative that keeps its sign consistent. At each candidate we use the closed-form FOCs when they are well posed and otherwise maximize the bounded local Hamiltonian. The boundary branches impose the borrowing constraint at a_min and a homothetic tail at a_max. We save consumption c, the risky holding k, and the saving rate mu_a on the grid.\n\nfunction (m::AchdouHanLasryLionsMollModel_RiskyAsset)(state::NamedTuple, u::NamedTuple)\n    (; κy, σy, ybar, r, μR, σR, ρ, γ, amin, amax) = m\n    (; y, a) = state\n    (; v, vy_up, vy_down, va_up, va_down, vyy, vaa) = u\n    μy = κy * (ybar - y)\n    cash = y + r * a\n    prem = μR - r\n    kmax = max(a - amin, 0.0)\n    # Newton can try negative marginal values, so cap implied consumption instead of flooring derivatives.\n    cmax = max(100.0 * (y + r * max(a, 0.0)), sqrt(eps()))\n    cmin = sqrt(eps())\n\n    if (a ≈ amax)\n        va_tail = (isfinite(va_down) && va_down > 0.0) ? va_down : va_up\n        vaa = - m.γ * va_tail / a\n    end\n\n    # upwinding for income direction (easy because exogenous income drift)\n    vy = (μy >= 0) ? vy_up : vy_down\n\n    function policy_candidate(va_candidate)\n        c_candidate = va_candidate > 0.0 ? clamp(va_candidate^(-1 / γ), cmin, cmax) : cmax\n        # First compare the two portfolio constraints, then add the Merton interior point if it\n        # is well-defined. This keeps Newton's off-path trial values finite without hiding the FOC.\n        H(k_trial) = prem * va_candidate * k_trial + 0.5 * vaa * σR^2 * k_trial^2\n        k_candidate = H(kmax) > 0.0 ? kmax : 0.0\n        if va_candidate > 0.0 && vaa < 0.0\n            k_merton = clamp(-prem * va_candidate / (σR^2 * vaa), 0.0, kmax)\n            k_candidate = H(k_merton) >= H(k_candidate) ? k_merton : k_candidate\n        end\n        μa_candidate = cash + prem * k_candidate - c_candidate\n        return c_candidate, k_candidate, μa_candidate\n    end\n\n    # upwinding for asset direction (harder because endogeneous asset drift)\n    c_up, k_up, μa_up = policy_candidate(va_up)\n    if μa_up >= 0.0\n        va = va_up\n        c = c_up\n        k = k_up\n        μa = μa_up\n    else\n        c_down, k_down, μa_down = policy_candidate(va_down)\n        if (μa_down <= 0.0) && (a > amin)\n            va = va_down\n            c = c_down\n            k = k_down\n            μa = μa_down\n        else\n            # If the two candidates straddle zero OR drift is negative at minimum asset threshold\n            # (i.e. borrowing constraint), then, we must have drift μa = 0. When the zero-drift\n            # Hamiltonian is concave we solve its FOC by bisection; otherwise we compare endpoints.\n            k_hi = kmax\n            if prem < 0.0\n                k_hi = min(k_hi, max((cash - cmin) / (-prem), 0.0))\n            end\n\n            if k_hi == 0.0 || !(isfinite(cash) && isfinite(prem) && isfinite(vaa) && isfinite(σR))\n                k = 0.0\n            elseif vaa < 0.0 && σR > 0.0\n                dh0 = prem * max(cash, cmin)^(-γ)\n                dh1 = prem * max(cash + prem * k_hi, cmin)^(-γ) + vaa * σR^2 * k_hi\n                if isfinite(dh0) && isfinite(dh1)\n                    if dh0 <= 0.0\n                        k = 0.0\n                    elseif dh1 >= 0.0\n                        k = k_hi\n                    else\n                        lo, hi = 0.0, k_hi\n                        for _ in 1:40\n                            mid = (lo + hi) / 2.0\n                            dh = prem * max(cash + prem * mid, cmin)^(-γ) + vaa * σR^2 * mid\n                            if dh > 0.0\n                                lo = mid\n                            else\n                                hi = mid\n                            end\n                        end\n                        k = (lo + hi) / 2.0\n                    end\n                else\n                    k = 0.0\n                end\n            else\n                c0 = max(cash, cmin)\n                c1 = max(cash + prem * k_hi, cmin)\n                h0 = c0^(1 - γ) / (1 - γ)\n                h1 = c1^(1 - γ) / (1 - γ) + 0.5 * vaa * σR^2 * k_hi^2\n                k = h1 > h0 ? k_hi : 0.0\n            end\n            c = max(cash + prem * k, cmin)\n            va = c^(-γ)\n            μa = 0.0\n        end\n    end\n    σa = k * σR\n    vt = - (c^(1 - γ) / (1 - γ) + va * μa + 0.5 * vaa * σa^2 + vy * μy + 0.5 * vyy * σy^2 - ρ * v)\n    return (; vt), (; v, c, k, va, vaa, vy, y, a, μa)\nend\n\nWith the equation, grid, and guess in hand, pdesolve solves the stationary system. The upper derivative boundary is the homothetic Merton tail; this matters because the risky asset gives wealth a nonzero diffusion near the top of the grid.\n\nbc = (; va = ((stategrid.y .+ m.r * m.amin).^(-m.γ),\n              fill(cshare^(-m.γ) * m.amax^(-m.γ), length(stategrid.y))))\nresult = pdesolve(m, stategrid, guess; bc, Δ = 1e-3, method = :trust_region,\n                  inner_iterations = 50, iterations = 150)","title":"The equation"},{"category":"section","location":"examples/consumption_saving/consumption_saving_risky_asset/#The-solution","page":"Consumption–saving: risky asset","text":"We plot the two policies against wealth for three income levels (low, median, high).\n\nas = stategrid[:a]\nys = stategrid[:y]\nidx = 1:div(length(as), 3)          # left third of the wealth grid, where the curvature is\niys = round.(Int, range(1, length(ys), length = 3))\n\np1 = plot(xlabel = \"wealth a\", ylabel = \"consumption c\")\nfor iy in iys\n    plot!(p1, as[idx], result.saved[:c][iy, idx], label = \"y = $(round(ys[iy], digits = 2))\")\nend\np2 = plot(xlabel = \"wealth a\", ylabel = \"saving μa\")\nfor iy in iys\n    plot!(p2, as[idx], result.saved[:μa][iy, idx], label = \"y = $(round(ys[iy], digits = 2))\")\nend\nhline!(p2, [0.0]; color = :gray, linestyle = :dash, label = \"\")\nplot(p1, p2; layout = (1, 2), size = (800, 300))\n\nConsumption rises with both wealth and income (left); the saving rate (right) is highest for the wealth-poor and turns negative as households approach their target wealth. On the portfolio side (not shown), away from the constraint the household holds the Merton fraction (mu_R - r)(gammasigma_R^2) of wealth in the risky asset, and tilts toward the safe asset near the constraint, where it cannot lever and labor-income risk crowds out financial risk-taking.\n\n\n\nThis page was generated using Literate.jl.","title":"The solution"},{"category":"section","location":"examples/asset_pricing/campbell_cochrane/#Campbell–Cochrane-(1999):-habit-formation","page":"Campbell–Cochrane: habit","text":"Campbell and Cochrane explain the equity premium with an external habit. The single state is the log surplus consumption ratio s = logfracC-XC — how far consumption sits above the habit X — which mean-reverts around bar s. The price–consumption ratio p(s) solves a linear second-order ODE (a special case of the HJB machinery):\n\n0 = 1 + bigl(mu + mu_p(s) + sigma_p(s)sigma - r(s) - kappa(s)(sigma+sigma_p(s))bigr) p(s)\n\nwhere the drift mu_p, volatility sigma_p, price of risk kappa, and riskless rate r are all functions of s through the habit.","title":"Campbell–Cochrane (1999): habit formation"},{"category":"section","location":"examples/asset_pricing/campbell_cochrane/#The-model","page":"Campbell–Cochrane: habit","text":"The parameters live in a struct:\n\nusing EconPDEs, Plots\n\nBase.@kwdef struct CampbellCochraneModel\n    μ::Float64 = 0.0189    # mean consumption growth\n    σ::Float64 = 0.015     # volatility of consumption growth\n    γ::Float64 = 2.0       # utility curvature (relative risk aversion)\n    ρ::Float64 = 0.116     # discount rate\n    κs::Float64 = 0.138    # mean-reversion speed of log surplus consumption ratio\n    b::Float64 = 0.0       # sensitivity of riskless rate to surplus ratio (b=0 ⇒ constant r)\nend","title":"The model"},{"category":"section","location":"examples/asset_pricing/campbell_cochrane/#The-state-space","page":"Campbell–Cochrane: habit","text":"We build the grid and the initial guess first, because they fix the names used everywhere else. The grid is a NamedTuple whose key is the state variable (s, the log surplus consumption ratio); the guess is a NamedTuple whose key is the unknown function (p, the price–consumption ratio). These names reappear inside the equation below — e.g. ps_up will be the forward finite difference of p in s. The grid concentrates points near the reflecting upper bound s_max, where the surplus ratio is highest, and stretches far into the left tail.\n\nfunction initialize_stategrid(m::CampbellCochraneModel; sn = 1000)\n    (; μ, σ, γ, ρ, κs, b) = m\n    Sbar = σ * sqrt(γ / (κs - b / γ))\n    sbar = log(Sbar)\n    smax = sbar + 0.5 * (1 - Sbar^2)\n    shigh = log.(range(0.0, exp(smax), length = div(sn, 10)))\n    slow = range(-300.0, shigh[2], length = sn - div(sn, 10))\n    (; s = vcat(slow[1:(end-1)], shigh[2:end]))\nend\n\nm = CampbellCochraneModel()\nstategrid = initialize_stategrid(m)\nguess = (; p = ones(length(stategrid[:s])))","title":"The state space"},{"category":"section","location":"examples/asset_pricing/campbell_cochrane/#The-equation","page":"Campbell–Cochrane: habit","text":"We now write the function encoding the HJB equation. Following the package convention, it takes the current state (a grid point) and u (each unknown together with its finite-difference derivatives there) and returns the time derivative of each unknown.\n\nfunction (m::CampbellCochraneModel)(state::NamedTuple, u::NamedTuple)\n    (; μ, σ, γ, ρ, κs, b) = m\n    (; s) = state\n    (; p, ps_up, ps_down, pss) = u\n    Sbar = σ * sqrt(γ / (κs - b / γ))\n    sbar = log(Sbar)\n    λ = 1 / Sbar * sqrt(1 - 2 * (s - sbar)) - 1\n    μs = -κs * (s - sbar)\n    σs = λ * σ\n    # pricing uses the risk-adjusted drift of surplus consumption, not the physical one\n    μs_effective = μs + σs * (σ - γ * (σ + σs))\n    ps = (μs_effective >= 0) ? ps_up : ps_down\n    σp = ps / p * σs\n    μp = ps / p * μs + 0.5 * pss / p * σs^2\n    κ = γ * (σ + σs)\n    r = ρ + γ * μ - (γ * κs - b) / 2 + b * (sbar - s)\n    pt = -p * (1 / p + μ + μp + σp * σ - r - κ * (σ + σp))\n    return (; pt)\nend\n\nWith the equation, grid, and guess in hand, pdesolve solves the stationary system:\n\nresult = pdesolve(m, stategrid, guess)","title":"The equation"},{"category":"section","location":"examples/asset_pricing/campbell_cochrane/#The-solution","page":"Campbell–Cochrane: habit","text":"Plotted against the surplus consumption ratio S = e^s, the price–consumption ratio rises steeply in good times: as consumption pulls away from the habit, effective risk aversion falls, discount rates drop, and valuations climb.\n\nSbar = m.σ * sqrt(m.γ / (m.κs - m.b / m.γ))\nsbar = log(Sbar)\nss = stategrid[:s]\nmask = ss .>= (sbar - 4)\nplot(exp.(ss[mask]), result.zero[:p][mask]; xlabel = \"surplus consumption ratio S = exp(s)\", ylabel = \"price–consumption ratio p\", legend = false)\n\n\n\nThis page was generated using Literate.jl.","title":"The solution"},{"category":"section","location":"examples/corporate_finance/bolton_chen_wang/#Bolton–Chen–Wang-(2009):-investment-with-costly-external-finance","page":"Bolton–Chen–Wang: financing constraint","text":"Bolton, Chen, and Wang build a unified theory of investment, financing, and cash management. A firm faces costly external finance, so it hoards cash as a buffer against shocks. The single state is the cash–capital ratio w, and the unknown is firm value per unit of capital v(w) — a generalized Tobin's q. It solves the HJB equation\n\nrv(w) = max_i(i-delta)bigl(v - wvbigr) + bigl((r-lambda)w + A - i - tfractheta2i^2bigr)v + tfracsigma^22v\n\nwith optimal investment i = tfrac1thetaleft(tfracvv - w - 1right). Two endogenous boundaries close the model: a payout boundary, where the marginal value of cash v falls to one, and a refinancing boundary, where the firm issues equity at marginal cost gamma and fixed cost phi.","title":"Bolton–Chen–Wang (2009): investment with costly external finance"},{"category":"section","location":"examples/corporate_finance/bolton_chen_wang/#The-model","page":"Bolton–Chen–Wang: financing constraint","text":"The parameters:\n\nusing EconPDEs, NLsolve, Plots\n\nBase.@kwdef struct BoltonChenWangModel\n  r::Float64 = 0.06       # risk-free rate\n  δ::Float64 = 0.10       # depreciation rate of capital\n  A::Float64 = 0.18       # productivity (expected profitability per unit capital)\n  σ::Float64 = 0.09       # cash-flow (capital) volatility\n  θ::Float64 = 1.5        # investment adjustment-cost parameter\n  λ::Float64 = 0.01       # carrying cost of holding cash\n  l::Float64 = 0.9        # liquidation value of capital\n  γ::Float64 = 0.06       # marginal (proportional) cost of external finance\n  ϕ::Float64 = 0.01       # fixed cost of external finance\nend","title":"The model"},{"category":"section","location":"examples/corporate_finance/bolton_chen_wang/#The-state-space","page":"Bolton–Chen–Wang: financing constraint","text":"We build the grid and the initial guess first, because they fix the names used everywhere else. The grid is a NamedTuple whose key is the state variable (w, the cash–capital ratio); the guess is a NamedTuple whose key is the unknown function (v), holding one starting value at each grid point (firm value is initialized to the cash ratio w itself). These names are what reappear inside the equation below — e.g. vw_up will be the forward finite difference of v in w.\n\nm = BoltonChenWangModel()\nstategrid = (; w = range(0.0, 0.3, length = 100))\nguess = (; v = stategrid[:w])","title":"The state space"},{"category":"section","location":"examples/corporate_finance/bolton_chen_wang/#The-equation","page":"Bolton–Chen–Wang: financing constraint","text":"We now write the function encoding the HJB equation. Following the package convention, it takes the current state (a grid point) and u — the local bundle holding the unknown and its finite-difference derivatives there (v, vw_up, vw_down, vww) — and returns the time derivative vt.\n\nThe cash drift mu_w can point either way, so the marginal value of cash is upwinded: forward (vw_up) where the drift is positive, backward (vw_down) where it is negative.\n\nfunction (m::BoltonChenWangModel)(state::NamedTuple, u::NamedTuple)\n  (; r, δ, A, σ, θ, λ, l, γ, ϕ) = m\n  (; w) = state\n  (; v, vw_up, vw_down, vww) = u\n  vw = vw_up\n  iter = 0\n  @label start\n  i = 1 / θ * (v / vw - w - 1)\n  μw = (r - λ) * w + A - i - θ * i^2 / 2 - (i - δ) * w\n  if (iter == 0) && (μw <= 0)\n    iter += 1\n    vw = vw_down\n    @goto start\n  end\n  vt = - ((i - δ) * (v - vw * w) + ((r - λ) * w + A - i - θ * i^2 / 2) * vw + σ^2 / 2 * vww - r * v)\n  return (; vt), (; v, vw, vww, w)\nend\n\nWe first solve the HJB with guessed slopes for the marginal value of cash at the two boundaries.\n\nresult = pdesolve(m, stategrid, guess; bc = (; vw = (1.5, 1.0)))\n\nThe guessed slopes are only a starting point. We then use NLsolve to find the boundary slopes that satisfy the value-matching and optimality conditions of Bolton–Chen–Wang (2009) — one at the refinancing boundary (marginal value of cash 1+gamma) and one at the payout boundary (marginal value of cash 1) — and re-solve at the fixed point.\n\nfunction f(m, x, stategrid, y)\n  result = pdesolve(m, stategrid, y; bc = (; vw = (x[1], x[2])), verbose = false)\n  v, vw, w = result.saved[:v], result.saved[:vw], result.saved[:w]\n  out = zeros(2)\n  mi = argmin(abs.(1 + m.γ .- vw))\n  out[1] =  v[mi] - m.ϕ - (1 + m.γ) * w[mi] - v[1]\n  wi = argmin(abs.(1 .- vw))\n  i = 1 / m.θ * (v[wi] - w[wi] - 1)\n  out[2] = m.r * v[wi] - (i - m.δ) * (v[wi] - w[wi]) - ((m.r - m.λ) * w[wi] + m.A - i - m.θ * i^2 / 2)\n  return out\nend\n\nnewsol = nlsolve(x -> f(m, x, stategrid, guess), [1.0,  1.0])\nresult = pdesolve(m, stategrid, guess; bc = (; vw = (newsol.zero[1], newsol.zero[2])))","title":"The equation"},{"category":"section","location":"examples/corporate_finance/bolton_chen_wang/#The-solution","page":"Bolton–Chen–Wang: financing constraint","text":"Firm value v(w) is increasing and concave in the cash–capital ratio: an extra dollar of internal cash is worth more than one (v(w)1) because external finance is costly, and this marginal value declines as the buffer grows. Investment i(w) rises with cash, so a cash-poor firm underinvests — internal liquidity, not just fundamentals, drives real investment. Both panels use the solution at the boundary slopes pinned down by NLsolve.\n\nws = stategrid[:w]\nv = result.saved[:v]\nvw = result.saved[:vw]\ni = (v ./ vw .- ws .- 1) ./ m.θ\np1 = plot(ws, v; xlabel = \"cash–capital ratio w\", ylabel = \"firm value v(w)\", legend = false)\np2 = plot(ws, i; xlabel = \"cash–capital ratio w\", ylabel = \"investment rate i(w)\", legend = false)\nplot(p1, p2; layout = (1, 2), size = (800, 300))\n\n\n\nThis page was generated using Literate.jl.","title":"The solution"},{"category":"section","location":"examples/consumption_saving/consumption_saving_two_income/#Achdou–Han–Lasry–Lions–Moll:-consumption–saving-with-two-income-states","page":"Consumption–saving: two income states","text":"A household saves in a riskless asset a at rate r and earns a labor income that switches between a low value y_l and a high value y_h as a two-state Poisson process (intensities lambda_lh, lambda_hl). There is one continuous state (assets a) but two coupled value functions v_l(a) and v_h(a), one per income state, linked by the income transitions:\n\nrho v_j(a) = max_c fracc^1-gamma1-gamma + v_j(a)(y_j + r a - c) + lambda_jkbigl(v_k(a)-v_j(a)bigr) qquad j ne k\n\nA borrowing constraint a ge underline a is enforced by preventing the asset drift from turning negative at the lower bound.","title":"Achdou–Han–Lasry–Lions–Moll: consumption–saving with two income states"},{"category":"section","location":"examples/consumption_saving/consumption_saving_two_income/#The-model","page":"Consumption–saving: two income states","text":"The parameters live in a struct:\n\nusing EconPDEs, Plots\n\nBase.@kwdef mutable struct AchdouHanLasryLionsMollModel_TwoStates\n    yl::Float64 = 0.5        # low income state\n    yh::Float64 = 1.5        # high income state\n    λlh::Float64 = 0.2       # low → high transition intensity\n    λhl::Float64 = 0.2       # high → low transition intensity\n    r::Float64 = 0.03        # risk-free rate\n    ρ::Float64 = 0.04        # discount rate\n    γ::Float64 = 2.0         # relative risk aversion\n    amin::Float64 = -yl / r  # borrowing limit (natural limit)\n    amax::Float64 = 50.0     # top of the asset grid\nend","title":"The model"},{"category":"section","location":"examples/consumption_saving/consumption_saving_two_income/#The-state-space","page":"Consumption–saving: two income states","text":"We build the grid and the initial guess first, because they fix the names used everywhere else. The grid is a NamedTuple whose key is the single continuous state (a); the guess is a NamedTuple whose keys are the two unknown functions (vl and vh), one starting value per grid point. These names are what reappear inside the equation below — e.g. vla_up will be the forward finite difference of vl in a.\n\nThe asset grid is finer near the borrowing limit, and we start from an autarky-style guess. The borrowing limit itself is nudged just inside the natural limit -y_lr to keep consumption strictly positive there.\n\nm = AchdouHanLasryLionsMollModel_TwoStates()\nm.amin += 0.001\nstategrid = (; a = m.amin .+ range(0, (m.amax - m.amin)^(1 / 2), length = 200) .^ 2)\nguess = (;\n    vl = (m.ρ ./ m.γ .+ (1 .- 1 / m.γ) .* m.r)^(-m.γ) .* (stategrid[:a] .+ m.yl ./ m.r) .^ (1 - m.γ) ./ (1 - m.γ),\n    vh = (m.ρ ./ m.γ .+ (1 .- m.γ) .* m.r)^(-m.γ) .* (stategrid[:a] .+ m.yh ./ m.r) .^ (1 - m.γ) ./ (1 - m.γ),\n)","title":"The state space"},{"category":"section","location":"examples/consumption_saving/consumption_saving_two_income/#The-equation","page":"Consumption–saving: two income states","text":"We now write the function encoding the HJB equation. Following the package convention, it takes the current state (a grid point) and u (each unknown together with its finite-difference derivatives there) and returns the time derivative of each unknown.\n\nIn each income state we upwind the asset drift on its sign, capping the implied consumption rather than flooring the marginal value (Newton may try negative marginal values). At the borrowing constraint the drift is set to zero. We save consumption cl, ch to plot.\n\nfunction (m::AchdouHanLasryLionsMollModel_TwoStates)(state::NamedTuple, u::NamedTuple)\n    (; yl, yh, λlh, λhl, r, ρ, γ, amin, amax) = m\n    (; a) = state\n    (; vl, vla_up, vla_down, vh, vha_up, vha_down) = u\n    clmax = 100.0 * (yl + r * max(a, 0.0))\n    chmax = 100.0 * (yh + r * max(a, 0.0))\n\n    # upwind the low-income value function\n    cl_up = vla_up > 0 ? min(vla_up^(-1 / γ), clmax) : clmax\n    μla_up = yl + r * a - cl_up\n    if μla_up >= 0.0\n        vla, cl, μla = vla_up, cl_up, μla_up\n    else\n        cl_down = vla_down > 0 ? min(vla_down^(-1 / γ), clmax) : clmax\n        μla_down = yl + r * a - cl_down\n        if μla_down <= 0.0 && a > amin\n            vla, cl, μla = vla_down, cl_down, μla_down\n        else\n            cl = yl + r * a          # borrowing constraint binds: drift is zero\n            μla = 0.0\n            vla = cl^(-γ)\n        end\n    end\n    vlt = -(cl^(1 - γ) / (1 - γ) + μla * vla + λlh * (vh - vl) - ρ * vl)\n\n    # upwind the high-income value function\n    ch_up = vha_up > 0 ? min(vha_up^(-1 / γ), chmax) : chmax\n    μha_up = yh + r * a - ch_up\n    if μha_up >= 0.0\n        vha, ch, μha = vha_up, ch_up, μha_up\n    else\n        ch_down = vha_down > 0 ? min(vha_down^(-1 / γ), chmax) : chmax\n        μha_down = yh + r * a - ch_down\n        if μha_down <= 0.0 && a > amin\n            vha, ch, μha = vha_down, ch_down, μha_down\n        else\n            ch = yh + r * a\n            μha = 0.0\n            vha = ch^(-γ)\n        end\n    end\n    vht = -(ch^(1 - γ) / (1 - γ) + μha * vha + λhl * (vl - vh) - ρ * vh)\n\n    return (; vlt, vht), (; cl, ch, μla, μha)\nend\n\nWith the equation, grid, and guess in hand, pdesolve solves the stationary system:\n\nresult = pdesolve(m, stategrid, guess)","title":"The equation"},{"category":"section","location":"examples/consumption_saving/consumption_saving_two_income/#The-solution","page":"Consumption–saving: two income states","text":"Consumption rises with wealth in both income states and is higher when income is high (left). The saving rate (right) is pinned at zero at the borrowing limit for the low-income household — which is forced to consume its income y_l + r a — and turns positive as wealth rises.\n\nas = stategrid[:a]\nidx = 1:div(length(as), 3)          # left third of the asset grid, where the curvature is\np1 = plot(as[idx], [result.saved[:cl][idx] result.saved[:ch][idx]]; label = [\"low income\" \"high income\"], xlabel = \"assets a\", ylabel = \"consumption c(a)\", legend = :bottomright)\np2 = plot(as[idx], [result.saved[:μla][idx] result.saved[:μha][idx]]; label = [\"low income\" \"high income\"], xlabel = \"assets a\", ylabel = \"saving μa(a)\", legend = :topright)\nhline!(p2, [0.0]; color = :gray, linestyle = :dash, label = \"\")\nplot(p1, p2; layout = (1, 2), size = (800, 300))\n\n\n\nThis page was generated using Literate.jl.","title":"The solution"},{"category":"section","location":"getting_started/#Getting-started","page":"Getting started","text":"This page solves a first model end to end. The main function is pdesolve. A stationary problem needs three ingredients: a state grid, an initial guess, and a PDE function encoding the equation at one grid point.","title":"Getting started"},{"category":"section","location":"getting_started/#A-first-model","page":"Getting started","text":"Here is the deterministic neoclassical growth model,\n\nrho v(k) = max_c left fracc^1-gamma1-gamma\n    + v(k) left(A k^alpha - delta k - cright) right\n\nwhose first-order condition is c = v(k)^-1gamma.","title":"A first model"},{"category":"section","location":"getting_started/#The-state-grid-and-the-guess","page":"Getting started","text":"We build the grid and the guess first, because their names fix the names used everywhere else. The grid is a NamedTuple whose keys are the state variables (here just k); the guess is a NamedTuple whose keys are the unknown functions (here just v), holding one starting value per grid point. The full rules — accepted containers, name ordering, array shapes — are in Grids and guesses.\n\nusing EconPDEs\n\nconst A = 0.5\nconst α = 0.3\nconst δ = 0.05\nconst ρ = 0.05\nconst γ = 2.0\n\n# Steady-state capital satisfies α A k^(α - 1) = ρ + δ.\nkbar = (α * A / (ρ + δ))^(1 / (1 - α))\nstategrid = (; k = range(0.1 * kbar, 5.0 * kbar, length = 200))\n\nguess = (; v = [(A * k^α)^(1 - γ) / (1 - γ) / ρ for k in stategrid[:k]])","title":"The state grid and the guess"},{"category":"section","location":"getting_started/#The-PDE-function","page":"Getting started","text":"The PDE function receives the current grid point state and a NamedTuple u holding each unknown together with its finite-difference derivatives at that point. Derivative fields are named by concatenation: u.vk_up and u.vk_down are the forward and backward first derivatives of v in k (Derivative naming gives the full table). The model chooses between them by the sign of the drift — this is upwinding. Here the drift depends on consumption, which comes from a first-order condition on the very derivative being chosen, so the code tries each direction and keeps the one consistent with itself, pinning the drift at zero in between; the cmax cap keeps the first-order condition well-defined when Newton iterates visit regions with nonpositive marginal value. Upwinding explains why the direction matters and gives the patterns that cover most models.\n\nfunction hjb(state::NamedTuple, u::NamedTuple)\n    k = state.k\n    cmax = 10 * A * k^α\n\n    c_up = u.vk_up > 0 ? min(u.vk_up^(-1 / γ), cmax) : cmax\n    μk_up = A * k^α - δ * k - c_up\n\n    if μk_up >= 0\n        c, vk, μk = c_up, u.vk_up, μk_up\n    else\n        c_down = u.vk_down > 0 ? min(u.vk_down^(-1 / γ), cmax) : cmax\n        μk_down = A * k^α - δ * k - c_down\n        if μk_down <= 0\n            c, vk, μk = c_down, u.vk_down, μk_down\n        else\n            c = A * k^α - δ * k\n            vk, μk = c^(-γ), 0.0\n        end\n    end\n\n    vt = -(c^(1 - γ) / (1 - γ) + μk * vk - ρ * u.v)\n    return (; vt)\nend\n\nThe function returns one time derivative per unknown, named Symbol(unknown, :t) — here vt. For a stationary equation written as 0 = RHS - ρv, return the negative residual: vt = -(RHS - ρv). The minus sign is the package's core convention — with the opposite sign the iteration diverges; The return value explains why.","title":"The PDE function"},{"category":"section","location":"getting_started/#Solving","page":"Getting started","text":"result = pdesolve(hjb, stategrid, guess)\nresult.zero[:v]            # the solved value function, on the grid\nresult.residual_norm       # should be ≈ 0\n\npdesolve returns an EconPDEResult with the solved unknowns in result.zero, indexed by name. Check result.residual_norm is small before using the output; the full set of fields is described in The result.","title":"Solving"},{"category":"section","location":"getting_started/#Exploring-the-solution","page":"Getting started","text":"result.zero holds the solved unknowns. Objects computed inside the PDE function — the optimal policy, a drift, an interest rate — can also be stored on the grid by returning a second NamedTuple:\n\nfunction hjb(state::NamedTuple, u::NamedTuple)\n    # ... as above ...\n    vt = -(c^(1 - γ) / (1 - γ) + μk * vk - ρ * u.v)\n    return (; vt), (; c, μk)\nend\n\nresult = pdesolve(hjb, stategrid, guess)\nconsumption = result.saved[:c]     # same shape as the grid\n\nEach saved object has the same shape as the state grid, so it plots directly against it. The saved drift is often the most useful diagnostic: it shows where the state moves and where it stops.\n\nusing Plots\nplot(stategrid[:k], result.saved[:c]; xlabel = \"k\", ylabel = \"consumption\")\nplot(stategrid[:k], result.saved[:μk]; xlabel = \"k\", ylabel = \"capital drift\")\n\nSaved drifts are also the bridge to stationary distributions: once the HJB is solved, the states follow a known, policy-implied law of motion, and the companion package InfinitesimalGenerators.jl turns that law of motion into stationary distributions and expectations. See InfinitesimalGenerators for the workflow.","title":"Exploring the solution"},{"category":"section","location":"getting_started/#Where-to-go-next","page":"Getting started","text":"Writing the PDE function — the reference for the naming conventions, the sign convention of the return value, and the upwinding patterns.\nBoundary conditions, Time-dependent problems, and Solver and troubleshooting — what usually matters after the first model works.\nThe Examples in the sidebar, starting with Neoclassical growth — the same model as this page, packaged the way the other examples are. Each example solves a published model, plots the solution, and explains it economically.","title":"Where to go next"},{"category":"section","location":"examples/consumption_saving/wang_wang_yang/#Wang–Wang–Yang-(2016):-consumption-and-saving-with-recursive-utility","page":"Wang–Wang–Yang: liquidity management","text":"Wang, Wang, and Yang (2016) study optimal consumption and saving when labor income follows a geometric Brownian motion, dY = mu Ydt + sigma YdZ, and the household has recursive (Epstein–Zin) preferences with elasticity of intertemporal substitution psi and risk aversion gamma. Wealth accumulates as dX = (r X + Y - C)dt subject to a borrowing limit. Because income is a GBM and preferences are homothetic, the value function is homogeneous and the problem collapses to a single ODE in the wealth-to-income ratio w = XY. Writing p(w) for the scaled value function — equivalently, total wealth (financial plus human) per unit of income — it solves\n\n0 = left(frac(r + psi(rho - r))p^1-psi - psirhopsi - 1 + mu - tfracgammasigma^22right) p\n  + bigl((r - mu + gammasigma^2)w + 1bigr) p\n  + tfrac12 sigma^2 w^2left(p - gammafracp^2pright)\n\nwith the consumption–income ratio recovered from c = (r + psi(rho - r)) p p^-psi.","title":"Wang–Wang–Yang (2016): consumption and saving with recursive utility"},{"category":"section","location":"examples/consumption_saving/wang_wang_yang/#The-model","page":"Wang–Wang–Yang: liquidity management","text":"The parameters live in a struct:\n\nusing EconPDEs, Plots, Printf\n\nBase.@kwdef struct WangWangYangModel\n    μ::Float64 = 0.015       # expected growth rate of labor income\n    σ::Float64 = 0.1         # volatility of labor income\n    r::Float64 = 0.035       # risk-free rate\n    ρ::Float64 = 0.04        # discount rate\n    γ::Float64 = 3.0         # relative risk aversion\n    ψ::Float64 = 1.1         # elasticity of intertemporal substitution\n    wmin::Float64 = 0.0      # borrowing limit (minimum wealth-income ratio)\n    wmax::Float64 = 1000.0   # maximum wealth-income ratio (grid upper bound)\nend","title":"The model"},{"category":"section","location":"examples/consumption_saving/wang_wang_yang/#The-state-space","page":"Wang–Wang–Yang: liquidity management","text":"We build the grid and the initial guess first, because they fix the names used everywhere else. The grid is a NamedTuple whose key is the state variable (w, the wealth-to-income ratio); the guess is a NamedTuple whose key is the unknown function (p), one starting value per grid point. These names are what reappear inside the equation below — e.g. pw_up will be the forward finite difference of p in w.\n\nThe state w runs from the borrowing limit to a large upper bound on a simple linear grid. The initial guess p = 1 + w reflects total wealth as financial plus one unit of human wealth.\n\nm = WangWangYangModel()\nstategrid = (; w = range(m.wmin, m.wmax, length = 1001))\nguess = (; p = 1 .+ stategrid[:w])","title":"The state space"},{"category":"section","location":"examples/consumption_saving/wang_wang_yang/#The-equation","page":"Wang–Wang–Yang: liquidity management","text":"We now write the function encoding the HJB equation. Following the package convention, it takes the current state (a grid point) and u (each unknown together with its finite-difference derivatives there) and returns the time derivative of each unknown.\n\nWe upwind on the physical wealth drift mu_w of the controlled state, falling back to mu_w = 0 when the borrowing constraint binds.\n\nfunction (m::WangWangYangModel)(state::NamedTuple, u::NamedTuple)\n    (; μ, σ, r, ρ, γ, ψ, wmin, wmax) = m\n    (; w) = state\n    (; p, pw_up, pw_down, pww) = u\n    mstar = r + ψ * (ρ - r)\n    p_for_trial = max(p, eps())\n    # Newton can try negative marginal values, so cap implied consumption instead of flooring derivatives.\n    cmax = 100.0 * (1 + max((r - μ + σ^2) * w, 0.0))\n\n    # Upwind on the physical wealth drift of the controlled state process.\n    c_up = pw_up > 0 ? min(mstar * p * pw_up^(-ψ), cmax) : cmax\n    μw_up = (r - μ + σ^2) * w + 1 - c_up\n    if μw_up >= 0\n        # Keep the branch condition about upwinding. If a Newton trial made the selected\n        # marginal value nonpositive, use the marginal value implied by capped consumption\n        # only to keep the residual's fractional power well-defined.\n        pw = pw_up > 0 ? pw_up : (c_up / (mstar * p_for_trial))^(-1 / ψ)\n        c = c_up\n        μw = μw_up\n    else\n        c_down = pw_down > 0 ? min(mstar * p * pw_down^(-ψ), cmax) : cmax\n        μw_down = (r - μ + σ^2) * w + 1 - c_down\n        if (μw_down <= 0) && (w > wmin)\n            pw = pw_down > 0 ? pw_down : (c_down / (mstar * p_for_trial))^(-1 / ψ)\n            c = c_down\n            μw = μw_down\n        else\n            # If the two candidates straddle zero OR drift is negative at minimum asset threshold\n            # we impose drift μw = 0.\n            μw = 0.0\n            c = 1 + (r - μ + σ^2) * w\n            pw = (c / (mstar * p_for_trial))^(-1 / ψ)\n        end\n    end\n    # At the top, we use the solution of the unconstrained problem, i.e. pw = 1 (a reflecting boundary would also work but is less elegant).\n    pt = - ((((r + ψ * (ρ - r)) * pw^(1 - ψ) - ψ * ρ) / (ψ - 1) + μ - γ * σ^2 / 2) * p + ((r - μ + γ * σ^2) * w + 1) * pw + σ^2 * w^2 / 2  * (pww - γ * pw^2 / p))\n    return (; pt), (; c, μw)\nend\n\nWith the equation, grid, and guess in hand, pdesolve solves the stationary system. The bc entry supplies one-sided ghost derivatives at the edges of the grid. At the borrowing constraint, the PDE still uses the inward derivative when the drift points into the state space, so the economically relevant p(0) is determined by the solution; at the upper edge, the condition pins the asymptotic complete-markets marginal value p(w) to 1.\n\nresult = pdesolve(m, stategrid, guess, bc = (; pw = (1.0, 1.0)))","title":"The equation"},{"category":"section","location":"examples/consumption_saving/wang_wang_yang/#The-solution","page":"Wang–Wang–Yang: liquidity management","text":"The paper reports the baseline solution on w in 020. On the full grid w in 01000, the level of p(w) is visually close to the linear complete-markets benchmark, so the economically relevant curvature is easier to see on the paper's scale. We compute finite-difference slopes for p(w) and compare the solution with the complete-markets benchmarks p^*(w) = w + h and c^*(w) = m^* (w + h).\n\nws = stategrid[:w]\np = result.zero[:p]\nc = result.saved[:c]\nh = 1 / (m.r - m.μ)\nmstar = m.r + m.ψ * (m.ρ - m.r)\nwmid = (ws[1:(end - 1)] .+ ws[2:end]) ./ 2\npprime = diff(p) ./ diff(ws)\n\np0 = p[1]\npprime0 = pprime[1]\n@printf(\"p(0) = %.2f, versus complete-markets p*(0) = %.2f; p(0)/p*(0) = %.1f%%\\n\",\n        p0, h, 100 * p0 / h)\n@printf(\"p'(0) = %.2f, so one dollar of liquid wealth is worth %.1f%% more than its accounting value at the constraint\\n\",\n        pprime0, 100 * (pprime0 - 1))\n\nidx = ws .<= 20\ndidx = wmid .<= 20\np1 = plot(ws[idx], p[idx]; xlabel = \"wealth-income ratio w\", ylabel = \"p(w)\",\n          label = \"model\", xlims = (0, 20), ylims = (20, 70))\nplot!(p1, ws[idx], ws[idx] .+ h; label = \"complete markets\", linestyle = :dash)\np2 = plot(wmid[didx], pprime[didx]; xlabel = \"wealth-income ratio w\", ylabel = \"p'(w)\",\n          label = \"model\", xlims = (0, 20), ylims = (1.0, 1.5))\nhline!(p2, [1.0]; label = \"complete markets\", linestyle = :dash)\np3 = plot(ws[idx], c[idx]; xlabel = \"wealth-income ratio w\", ylabel = \"c(w)\",\n          label = \"model\", xlims = (0, 20), ylims = (0.5, 2.5))\nplot!(p3, ws[idx], mstar .* (ws[idx] .+ h); label = \"complete markets\", linestyle = :dash)\nplot(p1, p2, p3; layout = (1, 3), size = (900, 420),\n     left_margin = 5Plots.mm, bottom_margin = 8Plots.mm)\n\nThe level p(0) is the certainty-equivalent value, in units of current income, of an agent who has no liquid wealth but still receives stochastic labor income. The gap between p(0) and p^*(0) = h is the welfare cost of incomplete markets and the borrowing constraint. The slope p(0) is the marginal certainty-equivalent value of one more dollar of liquid wealth at the constraint; values above one are a liquidity premium. As wealth rises, self-insurance becomes more effective, so p(w) falls toward the complete-markets value one and p(w) approaches the linear benchmark w + h.\n\n\n\nThis page was generated using Literate.jl.","title":"The solution"},{"category":"section","location":"infinitesimal_generators/#InfinitesimalGenerators","page":"InfinitesimalGenerators","text":"EconPDEs.jl and InfinitesimalGenerators.jl are complementary. EconPDEs.jl solves nonlinear problems — optimal policies, equilibrium prices: equations in which drifts, volatilities, or the upwind direction itself depend on the unknown solution. InfinitesimalGenerators.jl solves linear problems — stationary distributions, expectations, tail indices: given a known Markov process, it builds the generator matrix and works with it directly.\n\nThat gives two ways to combine them:\n\nAfter pdesolve. Once the nonlinear problem is solved, the model reduces to a known Markov process — the states follow the policy-implied law of motion — and InfinitesimalGenerators.jl turns it into stationary distributions and expectations.\nInstead of pdesolve. Skip the high-level interface and use InfinitesimalGenerators.jl's derivative objects to write the nonlinear finite scheme yourself, calling the underlying solver directly.\n\nThis page shows both on the consumption–saving model with two income states. The HJB tutorial in the InfinitesimalGenerators documentation solves the same model with the same parameters from the opposite direction — building the implicit finite-difference method by hand from generator matrices, and ending with pdesolve — so the two pages can be read as mirrors of each other.","title":"InfinitesimalGenerators"},{"category":"section","location":"infinitesimal_generators/#From-pdesolve-to-a-Markov-process","page":"InfinitesimalGenerators","text":"In the two-income model, a household saves in a riskless asset a while its labor income switches between y_l and y_h as a two-state Poisson process. The model, the grid, the guess, and the solve are exactly those of the example page and are not repeated here; the only thing that matters is that the equation returns the policy-implied asset drifts μla and μha as saved objects, so that pdesolve saves them on the grid:\n\nusing EconPDEs\nusing InfinitesimalGenerators\n\nBase.@kwdef mutable struct AchdouHanLasryLionsMollModel_TwoStates # hide\n    yl::Float64 = 0.5 # hide\n    yh::Float64 = 1.5 # hide\n    λlh::Float64 = 0.2 # hide\n    λhl::Float64 = 0.2 # hide\n    r::Float64 = 0.03 # hide\n    ρ::Float64 = 0.04 # hide\n    γ::Float64 = 2.0 # hide\n    amin::Float64 = -yl / r # hide\n    amax::Float64 = 50.0 # hide\nend # hide\nfunction (m::AchdouHanLasryLionsMollModel_TwoStates)(state::NamedTuple, u::NamedTuple) # hide\n    (; yl, yh, λlh, λhl, r, ρ, γ, amin, amax) = m # hide\n    (; a) = state # hide\n    (; vl, vla_up, vla_down, vh, vha_up, vha_down) = u # hide\n    clmax = 100.0 * (yl + r * max(a, 0.0)) # hide\n    chmax = 100.0 * (yh + r * max(a, 0.0)) # hide\n    cl_up = vla_up > 0 ? min(vla_up^(-1 / γ), clmax) : clmax # hide\n    μla_up = yl + r * a - cl_up # hide\n    if μla_up >= 0.0 # hide\n        vla, cl, μla = vla_up, cl_up, μla_up # hide\n    else # hide\n        cl_down = vla_down > 0 ? min(vla_down^(-1 / γ), clmax) : clmax # hide\n        μla_down = yl + r * a - cl_down # hide\n        if μla_down <= 0.0 && a > amin # hide\n            vla, cl, μla = vla_down, cl_down, μla_down # hide\n        else # hide\n            cl = yl + r * a # hide\n            μla = 0.0 # hide\n            vla = cl^(-γ) # hide\n        end # hide\n    end # hide\n    vlt = -(cl^(1 - γ) / (1 - γ) + μla * vla + λlh * (vh - vl) - ρ * vl) # hide\n    ch_up = vha_up > 0 ? min(vha_up^(-1 / γ), chmax) : chmax # hide\n    μha_up = yh + r * a - ch_up # hide\n    if μha_up >= 0.0 # hide\n        vha, ch, μha = vha_up, ch_up, μha_up # hide\n    else # hide\n        ch_down = vha_down > 0 ? min(vha_down^(-1 / γ), chmax) : chmax # hide\n        μha_down = yh + r * a - ch_down # hide\n        if μha_down <= 0.0 && a > amin # hide\n            vha, ch, μha = vha_down, ch_down, μha_down # hide\n        else # hide\n            ch = yh + r * a # hide\n            μha = 0.0 # hide\n            vha = ch^(-γ) # hide\n        end # hide\n    end # hide\n    vht = -(ch^(1 - γ) / (1 - γ) + μha * vha + λhl * (vl - vh) - ρ * vh) # hide\n    return (; vlt, vht), (; cl, ch, μla, μha) # hide\nend # hide\n\nm = AchdouHanLasryLionsMollModel_TwoStates() # hide\nm.amin += 0.001 # hide\nstategrid = (; a = m.amin .+ range(0, (m.amax - m.amin)^(1 / 2), length = 200) .^ 2) # hide\nguess = (; # hide\n    vl = (m.ρ ./ m.γ .+ (1 .- 1 / m.γ) .* m.r)^(-m.γ) .* (stategrid[:a] .+ m.yl ./ m.r) .^ (1 - m.γ) ./ (1 - m.γ), # hide\n    vh = (m.ρ ./ m.γ .+ (1 .- m.γ) .* m.r)^(-m.γ) .* (stategrid[:a] .+ m.yh ./ m.r) .^ (1 - m.γ) ./ (1 - m.γ), # hide\n) # hide\nresult = pdesolve(m, stategrid, guess; verbose = false)\n@assert result.residual_norm <= 1e-6 # hide\n\nas = stategrid[:a]\nμla = result.saved[:μla]\nμha = result.saved[:μha]\nnothing # hide\n\nThe solved model is a Markov process on (a, y): income is a two-state Markov chain, and within each income state assets drift deterministically at the policy-implied rate. This is exactly what SwitchingProcess represents — a ContinuousTimeMarkovChain plus one continuous process per income state, here two DiffusionProcesses with zero volatility:\n\nZ = ContinuousTimeMarkovChain([m.yl, m.yh], [-m.λlh m.λlh; m.λhl -m.λhl])\nXl = DiffusionProcess(as, μla, zeros(length(as)))\nXh = DiffusionProcess(as, μha, zeros(length(as)))\nX = SwitchingProcess(Z, [Xl, Xh])\nsize(X)\n\ngenerator(X) is the transition-rate matrix of the discretized process, built with the same upwind convention pdesolve uses (forward differences where the drift is positive, backward where it is negative, with the same default reflecting boundaries). That consistency matters below.","title":"From pdesolve to a Markov process"},{"category":"section","location":"infinitesimal_generators/#Stationary-distribution","page":"InfinitesimalGenerators","text":"The stationary distribution solves the Kolmogorov forward equation — the left null vector of the generator. stationary_distribution returns the probability mass at each grid point, shaped like the state space:\n\ng = stationary_distribution(X)\nsize(g)\n\nColumn 1 is the distribution of assets among low-income households, column 2 among high-income households. Dividing the masses by the cell widths gives a density:\n\nusing Plots\n\nΔa = (as[[2:end; end]] .- as[[1; 1:(end - 1)]]) ./ 2\nidx = as .<= 10\nplot(as[idx], (g ./ Δa)[idx, :];\n    label = [\"low income\" \"high income\"], xlabel = \"assets a\", ylabel = \"stationary density\")\n\nLow-income households dissave toward the borrowing limit; high-income households accumulate. Any moment of the stationary distribution is now a weighted sum:\n\ncl = result.saved[:cl]\nch = result.saved[:ch]\nmean_assets = sum(g .* as)\nshare_borrowers = sum(g[as .< 0, :])\naggregate_consumption = sum(g .* [cl ch])\naggregate_income = sum(g .* [fill(m.yl, length(as)) fill(m.yh, length(as))]) + m.r * mean_assets\n(; mean_assets, share_borrowers, aggregate_consumption, aggregate_income)\n\nAggregate consumption equals aggregate income to machine precision: in the stationary distribution, average saving Ey + ra - c is zero. Getting this identity for free — rather than up to discretization error — is a consequence of computing the distribution from the same discretized generator that solved the HJB.\n\n@assert abs(sum(g) - 1) <= sqrt(eps()) # hide\n@assert abs(aggregate_consumption - aggregate_income) <= 1e-6 # hide\nnothing # hide\n\nThe division of labor is deliberate: EconPDEs.jl solves the nonlinear policy problem; InfinitesimalGenerators.jl turns the solved law of motion into a linear generator and computes distributions and expectations under it (see the expectations tutorial for feynman_kac: forecasts, present values, state-dependent discounting).","title":"Stationary distribution"},{"category":"section","location":"infinitesimal_generators/#Writing-the-nonlinear-finite-scheme-yourself","page":"InfinitesimalGenerators","text":"pdesolve is the high-level interface. It creates the derivative bundle u, applies boundary conditions, assembles the vector residual, builds a sparse Jacobian pattern, and hands the resulting nonlinear system to finiteschemesolve. If you want a lower-level version, you can skip pdesolve and construct each derivative yourself with InfinitesimalGenerators.jl's FirstDerivative, writing directly into the residual array — here for the same two-income model, with the two value functions stored as the columns of a matrix. (The HJB tutorial in the InfinitesimalGenerators documentation shows the same construction driving the classic fixed-Delta linear iteration of Achdou et al. instead of finiteschemesolve.)\n\nfunction two_income_residual!(vt, m, as, v)\n    (; yl, yh, λlh, λhl, r, ρ, γ, amin) = m\n    for (j, y, λ) in ((1, yl, λlh), (2, yh, λhl))\n        k = 3 - j\n        va_up = FirstDerivative(as, view(v, :, j); direction = :forward, bc = (0, 0))\n        va_down = FirstDerivative(as, view(v, :, j); direction = :backward, bc = (0, 0))\n        for i in eachindex(as)\n            a = as[i]\n            # Guard the policy implied by the FOC; Newton can try nonpositive marginal values.\n            cmax = 100.0 * (y + r * max(a, 0.0))\n            c_up = va_up[i] > 0 ? min(va_up[i]^(-1 / γ), cmax) : cmax\n            μa_up = y + r * a - c_up\n            if μa_up >= 0.0\n                va, c, μa = va_up[i], c_up, μa_up\n            else\n                c_down = va_down[i] > 0 ? min(va_down[i]^(-1 / γ), cmax) : cmax\n                μa_down = y + r * a - c_down\n                if μa_down <= 0.0 && a > amin\n                    va, c, μa = va_down[i], c_down, μa_down\n                else\n                    c = y + r * a          # borrowing constraint binds: drift is zero\n                    μa = 0.0\n                    va = c^(-γ)\n                end\n            end\n            vt[i, j] = -(c^(1 - γ) / (1 - γ) + μa * va + λ * (v[i, k] - v[i, j]) - ρ * v[i, j])\n        end\n    end\n    return vt\nend\nnothing # hide\n\nfiniteschemesolve is the nonlinear solver underneath pdesolve: it takes a function that fills in the time derivative dot y = F(y) of the stacked system — here y is the matrix whose columns are the two value functions — and finds the zero of F by Newton's method with pseudo-transient continuation, the algorithm described in How pdesolve finds the solution. Called on the hand-written residual, it converges to the same fixed point as the pdesolve version:\n\nv, residual_norm = finiteschemesolve(\n    (ydot, y) -> two_income_residual!(ydot, m, as, y),\n    [guess.vl guess.vh];\n    verbose = false,\n)\n\nmaximum(abs, v .- [result.zero[:vl] result.zero[:vh]])\n\n@assert residual_norm <= 1e-6 # hide\n@assert maximum(abs, v .- [result.zero[:vl] result.zero[:vh]]) <= 1e-6 # hide\nnothing # hide\n\nThe tradeoff is transparency for bookkeeping: you choose how to construct each derivative and write directly into the residual array, but you also lose the named derivative bundle, automatic saving of outputs, multidimensional stencil assembly, and the sparse Jacobian pattern that pdesolve constructs for one-, two-, and three-state problems.","title":"Writing the nonlinear finite scheme yourself"},{"category":"section","location":"#EconPDEs.jl","page":"Home","text":"EconPDEs.jl solves nonlinear ODEs and PDEs that arise in economic models, especially Hamilton–Jacobi–Bellman equations. You write the local equation; the package supplies the finite-difference derivatives, upwinding, sparse Jacobians, and pseudo-transient Newton iteration.\n\nRepository: github.com/matthieugomez/EconPDEs.jl","title":"EconPDEs.jl"},{"category":"section","location":"#Installation","page":"Home","text":"The package is registered in the Julia General registry:\n\n] add EconPDEs\n\nCurrent versions require Julia 1.10 or later.","title":"Installation"},{"category":"section","location":"#Where-to-start","page":"Home","text":"Start with Getting started, which solves a first model end to end. Writing the PDE function is the reference for the package's naming conventions and the upwinding patterns used in most models. Boundary conditions, Time-dependent problems, and Solver and troubleshooting cover what usually matters after the first model works. The pdesolve reference lists every solver option.\n\nThe Examples in the sidebar each solve a model, plot the solution, and explain what it means economically. The overview lists them all, flagging what each demonstrates beyond the baseline workflow. Start with Neoclassical growth. The InfinitesimalGenerators page explains when to combine EconPDEs with InfinitesimalGenerators.jl for stationary distributions or lower-level finite-difference residuals.","title":"Where to start"},{"category":"section","location":"#Why-EconPDEs","page":"Home","text":"Julia already has good PDE packages. EconPDEs.jl has a narrower goal: solve the continuous-time HJBs that come up in economic models.\n\nThese equations have a structure that is easy to lose in a generic PDE interface. The state drift and volatility are often chosen by the agent, so the differential operator depends on the current guess for the value function. At the same time, the equation is strongly forward-looking: today's value depends on future values, future policies, and the way the state distribution moves under those policies.\n\nEconPDEs.jl keeps that feedback loop explicit. You write the local economic equation, including the policy rules and the implied drift or volatility. The package then builds the finite-difference derivatives, boundary treatment, sparse Jacobian, and nonlinear solve needed to turn that equation into a stationary or time-dependent solution.","title":"Why EconPDEs"}]
}
